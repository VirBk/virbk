{
  "sitting": 10,
  "note": "Look, not law. Absorb or drop next sitting. A spark is not a trap.",
  "items": [
    {
      "id": "S13",
      "kind": "trap",
      "claim": "The qwen-code seat recipe passes the packet as an argv string, and on Windows a 33 KB packet exceeds the command-line limit: the seat dies with 'Argument list too long' before it reads a byte.",
      "object": "factory/tools/seat.mjs",
      "whyNotLaw": "Observed once on F33's launch. The fix is stdin rather than argv, but no check yet proves a recipe can carry a packet of cap size on the platform the control plane sits on.",
      "status": "absorbed",
      "openedOn": 10,
      "absorbedAs": "T72"
    },
    {
      "id": "S14",
      "kind": "trap",
      "claim": "An issuance commit pushed to main without a skip-ci marker turns the gate red and launches the GHA writer on the lane just issued, onto the same branch a local worktree already holds.",
      "object": ".github/workflows/writer.yml",
      "whyNotLaw": "On F33 the cloud seat started headless with yolo on writer/F33 and was stopped only by a 401 on a stale DashScope secret. The F31 sitting knew this and wrote it as a ledger sentence, not a check.",
      "status": "absorbed",
      "openedOn": 10,
      "absorbedAs": "T74"
    },
    {
      "id": "S15",
      "kind": "trap",
      "claim": "A router exists and one caller ignores it: ghaWriter.mjs hardcodes the DashScope base URL and maps the model through its own HOSTED table, so the cloud seat cannot take the native path route.mjs resolves for the local seat.",
      "object": "factory/tools/ghaWriter.mjs",
      "whyNotLaw": "It also counts DEEPSEEK_API_KEY as having a writer secret but never passes it to the call, so a repo holding only that secret stops idling and then fails on an empty key, which is the loud skip T55 requires turned into a loud failure.",
      "status": "absorbed",
      "openedOn": 10,
      "absorbedAs": "T75"
    },
    {
      "id": "S16",
      "kind": "trap",
      "claim": "A writer key read from a process environment is a value carried from an earlier act, not read from the store that owns it: after a rotation the User scope answers 200 and every already-open shell keeps answering 401.",
      "object": "factory/tools/seat.mjs",
      "whyNotLaw": "Cost this sitting two wrong diagnoses and nearly a handover command that could not work. T04 names the shape and no recipe reads the key from its store in the same act as the launch.",
      "status": "absorbed",
      "openedOn": 10,
      "absorbedAs": "T73"
    },
    {
      "id": "S17",
      "kind": "trap",
      "claim": "The seat census matches any command line containing qwen, aider, goose or opencode as a word, so an unrelated process reads as a running writer seat.",
      "object": "factory/tools/seat.mjs",
      "whyNotLaw": "Caught live during F37's review: the reviewer's own 'pip show aider-chat' was reported as two running aider seats. A census that over-reports is a census that gets ignored, and no check distinguishes a harness process from a mention of one.",
      "status": "open",
      "openedOn": 10
    },
    {
      "id": "S18",
      "kind": "trap",
      "claim": "A seat cited evidence that does not exist: a lane log named a git ref as the source of a price claim, and the ref appears nowhere in the repository except the sentence claiming it.",
      "object": "docs/log",
      "whyNotLaw": "The underlying claim was true and derivable from route.mjs, so a reviewer reading for plausibility passes it. T02 covers a copied number and T56 a missing command; neither covers an invented reference, and nothing checks that a ref or path a log cites resolves.",
      "status": "open",
      "openedOn": 10
    }
  ]
}
