ENVELOPE: F43 — A print-mode recipe names its approval mode, or the seat writes nothing
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make every seat recipe state the approval mode its carrier line needs, and check it.

Acceptance
  This is paid-for, not theoretical. Sitting 11 launched two seats from the qwen-code recipe
  exactly as written. Both read their packet, did correct analysis, wrote NOTHING, and said so
  only because they thought to: the qwen approval mode defaults to requiring approval for
  every file edit and shell command, and a print-mode seat has no terminal to approve at. The
  recipe has never named a mode, so no seat launched from that line could write unless
  something outside the line supplied it, and the failure is invisible until a seat tries.

  Three items. DICTATED. Do not widen.

  Item 1 — the qwen-code carrier line names a VERIFIED approval mode.
  The carrier for qwen-code gains the long-form approval-mode option set to its
  accept-all choice, placed before `-p -`, keeping the packet on stdin exactly as T72
  requires. The control plane read this off `qwen --help` on this machine: the option is
  `--approval-mode`, its choices are plan, default, auto-edit, auto and the accept-all one,
  and the default is documented there as requiring approval for file edits or shell commands.
  Run `qwen --help` yourself and copy the spelling it prints. Do not invent a shorthand.

  Item 2 — an UNVERIFIED harness says so instead of guessing.
  aider, opencode and goose are not installed on the control plane's machine and their
  equivalent options were NOT verified. Do NOT invent one. Each of those recipes gains a
  one-line comment saying a print-mode launch needs that harness's own auto-approve option,
  that it is unverified here, and that the operator confirms it from that harness's own help
  output before launching. A guessed option is worse than a named gap: it fails silently in
  the same way the qwen line just did.

  Item 3 — a check that would have caught this.
  `node factory/tools/seat.mjs --self-test` asserts, for every recipe the `list` command
  prints, that its carrier line either names an approval option or carries the unverified
  comment from item 2. A recipe with neither fails.
  MUTATION THAT MUST TURN IT RED, at the object the gate runs: delete the approval option
  from the qwen-code carrier. `--self-test` must fail and name the recipe and the property,
  that a print-mode carrier names no approval mode. Drive it a second time by deleting the
  unverified comment from one of the other three, which must red the same way.

Base
  cafac161eacdada5edca231051b2057a86d938b7
  Worktree from that SHA. Read `git ls-remote origin refs/heads/main` before you push; if it
  moved, rebase onto it. Push the branch only. Branch writer/F43

Holds
  factory/tools/seat.mjs
  docs/log/f43.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours:
  the control plane writes the trap row at landing.
  F42 is your sibling on factory/tools/alumni.mjs, proved disjoint; do not touch it.

Must stay true
  T72: the packet reaches a seat on STDIN, never as one argv string. Your option goes beside
  `-p -`, it does not replace the stdin carrier. T73: the Windows lines read the writer key
  from the user store in the same act as the launch. Both live in the file you are editing
  and both must survive your change with their meaning intact.
  T77 and T78 landed in this same file this sitting: the census program-position rule, its
  harness-label assertion, and the disjoint argument-count refusal all keep passing.
  A false sentence in the diff is a blocking defect — and in THIS lane a false sentence would
  be an option you did not read from a help output you ran.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs recipe qwen-code
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  The hazard here is the one T07 names: a recipe remembered rather than read. You are writing
  the line a future seat is launched from, and a wrong option fails the way this one already
  did, silently and only at the moment someone needs it. If you cannot run a harness's help,
  item 2 is the answer — say it is unverified. Naming the gap is the deliverable, not a
  failure to deliver.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY.
  Do not remap the control-plane session. Hands never call a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  30 tool calls, or USD 0.45, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive both mutations yourself, restore the file byte for byte, and confirm `git hash-object
  factory/tools/seat.mjs` matches before and after. Record in docs/log/f43.md what it found.

Return
  docs/log/f43.md: what changed, the command and what it printed, both mutations pasted going
  red, and the help output you read the option from, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
