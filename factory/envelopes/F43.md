ENVELOPE: F43 — CORRECTION 1 — A print-mode recipe names its approval mode
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Already approved — do not touch
  Items 1 and 2 are APPROVED by an independent review that ran the harness's own help output
  and matched it against your recipe comment word for word: the option name, its choices and
  its documented default all check out, and no flag was invented for the three uninstalled
  harnesses. The mode sits beside the stdin form rather than replacing it, so T72 holds. Do
  not re-open any of that, do not guess a flag for aider, opencode or goose.

Work
  Make item 3's check cover every recipe item 2 names, not only those with a stdin carrier.

Acceptance
  The defect is a SCOPE the check reads more narrowly than the envelope meant. The check
  decides what is in scope by whether a recipe renders a stdin carrier line. goose renders
  none — it never had a `packetStdin()` call, which predates this lane — so goose is invisible
  to the check, even though item 2 names goose as one of the three that must declare the gap
  and your diff duly added the note to it. Deleting that note from goose leaves
  `node factory/tools/seat.mjs --self-test` GREEN, which the control plane drove and confirms.
  The same deletion on aider or opencode correctly reds. So the dictated mutation does not
  fire on one of the three objects the envelope named.

  The envelope's own words "its carrier line" invited that reading and D-62 records the
  control plane's share of this. Your log named the hole in plain words rather than hiding
  it, which is the behaviour to keep — but disclosing a hole in a dictated item is not
  closing it, and the lane should have returned for a split rather than shipped around it.

  One item. DICTATED.

  Item 1 — scope is print-mode, not stdin-carrier.
  A recipe is in scope for the approval assertion when its rendered text marks it as a
  print-mode launch, which every affected recipe already states in its own words, rather than
  when it happens to render a stdin carrier. Under the new scope, every recipe that declares
  itself print-mode must either name an approval mode or carry the unverified note. goose is
  in scope and passes today on the note it already has; nothing about goose's text changes.
  Do NOT give goose a stdin carrier to sneak it into the old scope: that would change a launch
  line nobody has run, on an uninstalled harness, which is the invented-evidence hazard T07
  and T76 both name.
  Correct the code comment too. It currently states, as a premise, that a recipe with no
  stdin carrier is not a print-mode launch line and has no mode to state. That sentence is
  false and goose is the counterexample sitting inside your own diff. A false sentence in the
  diff is a blocking defect.
  MUTATIONS THAT MUST TURN IT RED, at the object the gate runs, and there are THREE because
  there are three named objects and a scope claim is only proved at the member most likely to
  escape it (D-62). Delete the unverified note from goose; delete it from aider; delete it
  from opencode. Each, on its own, must fail `--self-test` naming that recipe. The goose one
  is green today and is the whole point. Then drive the qwen mutation once more to show the
  rescoping did not lose it.

Base
  Rebase onto live main; read `git ls-remote origin refs/heads/main` yourself and do not take
  a sha from this sentence. Keep your commit, rebase, push the branch only.
  Branch writer/F43

Holds
  factory/tools/seat.mjs
  docs/log/f43.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours.
  F42 is in correction on factory/tools/alumni.mjs; do not touch it.

Must stay true
  T72: the packet reaches a seat on STDIN. T73: the Windows lines read the writer key from
  the user store in the same act as the launch. T77 and T78, the census program-position rule
  with its harness-label assertion and the disjoint argument-count refusal, all landed in this
  same file this sitting and all keep passing.
  The qwen-code carrier keeps the verified mode it already has, and the three uninstalled
  harnesses keep the generic note naming no specific flag.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs recipe goose
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Whatever you choose as the in-scope marker, ask which recipe it EXCLUDES and whether that
  exclusion is right. That question is the one this correction exists for: the previous scope
  had a defensible-sounding premise and a counterexample already inside the diff. Then ask
  what happens when a recipe is added later — does it default into the check or out of it?
  Defaulting out is how this hole was dug.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY.
  Do not remap the control-plane session. Hands never call a model.

Spend cap
  20 tool calls, or USD 0.30, then stop and return. This is a correction, not a lane.
  This is the ONLY correction: a sweep-tier seat gets one, then the work splits.

Fuse
  Split on correction done, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive all four mutations yourself, restore byte for byte, and confirm `git hash-object
  factory/tools/seat.mjs` matches before and after.

Return
  docs/log/f43.md: what the correction changed, the command and what it printed, all four
  mutations pasted going red, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
