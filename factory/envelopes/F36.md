ENVELOPE: F36 — a trap names its object
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Give every trap an object, prove the object resolves, and stop shipping the prose of a trap the gate already enforces.

Scope, and what is NOT in it
  D-58 narrowed this lane before it was issued. The board's title also promises a seat packet scoped to its Holds. That half is DEFERRED and you must not build it: a per-lane traps digest makes the bytes before the envelope differ by lane, which is the assertion F31 landed and X01 was reverted for nearly switching off, and it would silently retire the proof F34 rested on. The digest every seat gets stays the same digest. Read D-58 before you start.

Acceptance
  1. Every trap in factory/traps.yaml carries an `object:` field, after `kind:`. Its value is either the single character `*`, meaning the trap binds everywhere and is not about one file, or a repository-relative path or glob naming what it binds to — `factory/tools/seat.mjs`, `docs/log/*.md`. Judge by the rule's own text: a trap that names a file or tool takes that path; a trap about how a session behaves takes `*`. Do not invent paths to avoid `*`; `*` is the honest answer for most of them and nothing is scored on the count.
  2. packet.mjs --self-test asserts, with each failing on its own revert (T13), pasted:
     a. every trap has a non-empty `object`;
     b. every object that is not `*` resolves in the tree — an exact path exists, or a glob matches at least one file. A trap pointing at a file that is not there is the shape this lane exists to stop.
  3. trapsDigest compresses a trap that already names a check. A trap whose rule contains a sentence beginning "Check is " emits its id, its title, and only the FIRST sentence of the rule. Every other trap is unchanged. Fifteen rows qualify today; find them by the text, not by a list you carry. D-52 measured about 2.7 KB, and writing a check now removes a trap's prose from every future packet, which is the point.
  4. No row leaves the digest. The existing assertion that the trap count equals the digest count keeps passing, and you do not weaken it. The digest emits no `object` — the field is for the gate and the control plane, not for the packet.
  5. node factory/tools/packet.mjs cost shows the seat packet smaller than 25.3 KB, and node factory/tools/sizeBudget.mjs stays green. Paste both.

Base
  2c1159c25aaae48d2c450b901d2a2005d2b25dc7
  Worktree from that base. Rebase onto live main before push and prove it with git range-diff. Push the branch writer/F36 only.

Holds
  factory/traps.yaml
  factory/tools/packet.mjs
  docs/log/f36.md
  Changed set equals this list; a stray file is a stop. budgets.json, landing-checks.json and kitCheck.mjs are NOT yours. No new gate step is needed: packet.mjs --self-test is already one.

Must stay true
  No assertion may be satisfied by construction. Two consecutive reviews of the last lane found exactly that, twice. For every assertion you write, name the mutation that makes it fail, make it, watch it go red, paste it.
  F31's guarantees survive untouched: the envelope part is last, and the bytes before it are identical for two different lanes. If a change of yours would alter either, you have built the deferred half — stop and return.
  The rule text in factory/traps.yaml is not edited. You add a field and change what the DIGEST emits. A trap's stored rule keeps every sentence it has.
  A false sentence in the diff is a blocking defect. No copied numbers (T02).

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/packet.mjs cost
  node factory/tools/sizeBudget.mjs
  node factory/tools/kitCheck.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed (T56), plus a revert proof for 2a, 2b and 3. The gate is 21 steps; Writer Action is red while an envelope is live, and that one is expected. Everything else is green at base and must stay green.

Look hardest at
  An object that is a guess. A path that does not exist passes nothing and teaches nothing; 2b is the check that catches you, so run it before you believe your own table.
  Compressing a trap whose first sentence is not the claim. If the first sentence is "Check is ..." or a fragment, the row needs the sentence that carries the rule — say so in the log for any row you judged rather than cut mechanically.
  Building the deferred half by accident.
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 60 — seventy-one rows take a pass, and the digest change is small.
  USD 0.20, then stop.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f36.md what it changed. If it found nothing, say that.

Return
  docs/log/f36.md: what changed, the command that measured it, and the count of traps you gave `*` against a path, pasted from a command. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
