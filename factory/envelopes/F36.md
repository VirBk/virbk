ENVELOPE: F36 — correction 1, the only one
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Replace two assertions that cannot fail. Fix four false or missing sentences in the log. Nothing else.

What is NOT wrong
  The object field, the resolve check and the compression itself are good and reviewed: 71 objects, 19 non-star all resolving, 52 star rows judged honest against their rule text, no stored rule edited, F31's prefix byte-identical across lanes with the envelope still last, seat packet 25.3 to 23.6 KB. Do not redesign any of it. Do not touch factory/traps.yaml.

Acceptance
  This envelope DICTATES. It offers no choice.
  1. BLOCKING. The compression assertion at packet.mjs is satisfied by construction: `want` is built by calling firstSentence() and ruleNamesACheck(), the same two functions trapsDigest calls to build the line it is compared against. It pins a ternary and nothing else. Two one-line mutations gut compression and the gate stays green, both demonstrated by the review:
       firstSentence -> `return text;`        digest 10559 -> 12261 bytes, self-test rc=0
       ruleNamesACheck -> `return true;`      digest 10559 -> 6923 bytes, all 71 rows truncated, self-test rc=0
     Assert a literal property of the digest, computed WITHOUT those two functions:
       a. for every trap whose STORED rule contains the text "Check is ", its digest line is strictly shorter than `id + " " + title + ": " + rule`, and does not contain "Check is";
       b. for every OTHER trap, its digest line equals `id + " " + title + ": " + rule` exactly. Acceptance 4 of the first envelope said "every other trap is unchanged" and nothing asserted it, so fifty-six rows could lose their prose in silence.
     Paste both mutations above going red under the new assertions.
  2. BLOCKING. The object-resolves claim has no vacuity guard, where its sibling has one. Set every object to "*" and the claim vanishes silently:
       perl -pi -e 's/^  object: .*$/  object: "*"/' factory/traps.yaml   ->  self-test rc=0
     Fail when no trap carries a non-star object, the way the compression claim already fails when no rule names a check. Paste the mutation going red.
  3. docs/log/f36.md, four fixes, no other edit to that file:
       a. the pasted `size budget ok` line says 5 in the band; the command prints 6, the sixth being this log itself, grown past 80 per cent after the paste. Re-run and paste what it prints now. The file already handles this self-reference correctly for gate step 14, twelve lines later — do the same here.
       b. "Two files" is over a numstat that prints three rows.
       c. the 1664-byte delta subtracts a full digest, header and trailer included, from a rows-only join. Digest to digest is 1702. State the comparison you are making and give the command for it.
       d. the envelope's Self-check clause is unanswered: record what the self-check changed, or say it found nothing.

Base
  c9143b7a5728e61484a3e8c5087749a52f1f4db5
  Your worktree is on writer/F36 at 5c8f2ce. Rebase onto that base, then correct. Prove the rebase with git range-diff. Push the branch only.

Holds
  factory/tools/packet.mjs
  docs/log/f36.md
  Narrower than the first envelope on purpose: factory/traps.yaml is finished and is NOT yours this round. budgets.json, landing-checks.json and kitCheck.mjs are not yours either.

Must stay true
  An assertion may not be checked against the code that produced the thing it asserts. That is what a literal property means in item 1, and it is the third time in two lanes this repository has shipped an assertion satisfied by construction.
  Every claim the review confirmed survives: 71 objects, the resolve check with real teeth, the fifteen compressed rows, the stored rules unedited, F31's envelope-last and identical-prefix, no row dropped from the digest.
  No copied numbers (T02). A false sentence in the diff is a blocking defect; item 3 is three of them.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/packet.mjs cost
  node factory/tools/sizeBudget.mjs
  node factory/tools/landingGate.mjs
  Paste what each printed (T56), plus the three mutations in items 1 and 2 going red. The gate is 21 steps with Writer Action red while an envelope is live; that one is expected.

Look hardest at
  Writing the new assertion in terms of firstSentence or ruleNamesACheck again. If your check calls either, it is the same defect wearing a different shape. Compute the expected line from the stored rule text and string operations alone.
  A guard that is itself vacuous.
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 40 — two assertions and four sentences. USD 0.15, then stop.
  This is correction 1 and at sweep tier it is the only one. A return that misses these items does not get another envelope on this tier.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record it in the log. That record is item 3d.

Return
  Append to docs/log/f36.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
