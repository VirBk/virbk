ENVELOPE: F46 — The packet band names the context that reaches a writer (S21)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make factory/tools/sizeBudget.mjs say what it measures, and report the packet a seat ACTUALLY receives, so the number a control plane reads before issuing is the number that reaches the writer.

What you cannot see from the file
  The band printed as "seat packet" measures seatPacket(root, null) - the packet with NO lane, so without the envelope. It is the byte-stable cacheable prefix, a real object: budgets.json carries seatPacketKb 26 AND envelopeKb 6 separately. But the LABEL says seat packet, and a seat's packet is the prefix plus its envelope. Measured this sitting: the band printed 99.7 per cent of 26 KB while node factory/tools/packet.mjs seat F47 printed 31.9 KB. One label, two numbers 5.7 KB apart: a control plane reading the green one can hand a seat 22 per cent more context than it checked. That is S21.
  This is NOT a request to change a cap. seatPacketKb and envelopeKb stay; D-57 set them tight on purpose and S27 is measuring the prefix. Your lane makes the measurement honest, not looser.

The rule to implement
  The existing band is relabelled as the PREFIX - the cacheable bytes every seat shares - and keeps measuring exactly what it measures today, against seatPacketKb, failing over cap as it does today.
  A SECOND report is added: for each envelope in factory/envelopes/, the total a seat of that lane receives, against seatPacketKb plus envelopeKb read from budgets.json and never hardcoded. It must equal what packet.mjs seat <LANE> prints for the same lane; that agreement is the property, not the formatting.
  When no envelope is issued - which is every moment between landings, because a landing deletes the envelope - the report says so and PASSES. A check that cannot pass is T09; so is one that fails for having nothing to measure.

Acceptance
  Six properties. Each gets its own fixture and mutation, and each must red for its property ALONE. If one reds two, one is untested. Paste what every mutation printed and the property it reds for.
  A FIXTURE THAT EXPECTS NO ERROR MUST STILL BE ABLE TO FAIL. F48 shipped one whose expected result was the empty list, and a mutation that made the code DISCARD the input entirely left it green, because "no error" and "never examined" look the same. Where you expect a pass, also assert the positive fact - the lane named, the byte count derived.
  1 With an envelope issued, the reported total for that lane EQUALS what packet.mjs seat <LANE> prints. Derive both in the fixture and compare rather than pasting a number.
  2 With NO envelope issued, the report says so and the command still exits 0. This is the ordinary case at every landing, and it needs a fixture that could fail.
  3 A total over seatPacketKb plus envelopeKb fails and the message NAMES THE LANE. With several envelopes issued, only the offending lane is named.
  4 The prefix band still measures what it measures today, against seatPacketKb, and still fails over cap. Its printed byte count at this commit is unchanged by your lane; paste it before and after.
  5 Both caps are read from factory/budgets.json. Change either in a fixture tree and the verdict moves with it.
  6 The label no longer calls the prefix the seat packet, and the total is labelled as what a seat receives; a reader must not mistake one for the other.

Base
  The commit the work is based on is live main at issue. Your worktree is cut from the commit that carries this envelope. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/sizeBudget.mjs
  docs/log/f46.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  seatPacketKb and envelopeKb are NOT changed. A cap moves only through a dated row in factory/decisions.json, which is not yours to write.
  Every other band sizeBudget prints keeps its object, its cap and its verdict.
  node factory/tools/sizeBudget.mjs exits 0 at your commit, and the landing gate is 21 of 21.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/sizeBudget.mjs
  node factory/tools/packet.mjs seat <a lane with an envelope> — to show the two agree
  node factory/tools/landingGate.mjs
  Six mutations, one per property, each pasted with its output.

Look hardest at
  Asserting the total by writing the same arithmetic twice: if the fixture computes it the way the tool does, it proves nothing. Derive one side from packet.mjs and the other from the tool.
  The empty case. Between landings there are no envelopes at all, so the path with nothing to measure is the path that runs most often, and a silent pass there is indistinguishable from a check that never ran.
  If the gate reds ALL 21 steps at any commit, your bash is the WSL launcher and the red is not yours (T70). Say so in the log, use git-bash, do not spend calls proving it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate).
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  40 tool calls. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). On a decay sign: stop, print the signs, return.

Self-check
  Before returning, ask of each fixture whether it COULD have failed, and prove it with the mutation you pasted. Record in docs/log/f46.md what the self-check changed.

Return
  A few lines in docs/log/f46.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
