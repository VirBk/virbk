ENVELOPE: F21 — Shallow checkout for the landing gate
Authorization: issued by the control plane following the owner's review: issue F21, then land through review and ff-only. Read AGENTS.md, BOARD.md, factory/traps.yaml once, then this envelope.

Work / acceptance
  Change only fetch-depth: 0 to fetch-depth: 1 in .github/workflows/landing.yml; no YAML comment. Keep default PR merge checkout and all gate checks. No timing claim. PR #3 is reference only; do not cherry-pick it or copy its nameless log.

Base / branch
  323b1a06cfb7f01b090e8eee5d57f05e60cc209b from git ls-remote origin refs/heads/main.
  The following main records commit adds this envelope and F21 board state only. Use the supplied isolated worktree of that live issuance commit, writer/F21. Read live main again before push; if it moved, stop and report. Push writer/F21 only, never main.

Holds
  .github/workflows/landing.yml
  docs/log/f21.md
  Changed file set equals these paths. No board, envelope, gate, runtime or reference-repository edits. docs/log/landing-checkout.md is absent from this base and stays absent. Control plane owns envelope retirement, restamping and landing.

Must stay true / look hardest at
  Triggers, permissions, concurrency, Node version, archive isolation and checks remain identical. landingGate.mjs needs HEAD, not ancestry. Keystone: none; neither closed-list keystone changes.

Verification / self-check
  git diff --check; git diff -- .github/workflows/landing.yml; node factory/tools/sizeBudget.mjs; node factory/tools/renderBoard.mjs --check; node factory/tools/sitting.mjs check.
  Read landingGate.mjs and compare changed paths with Holds. Fix at most one self-check defect and record it.
  Final gate: node factory/tools/landingGate.mjs --sha HEAD on Ubuntu in the PR. Existing ghaWriter --self-test requires no live envelopes; control plane retires this envelope in its own records commit before the final gate. Do not alter checks or claim this final gate ran in your log.

Runtime
  pc-dashscope recipe: factory/tools/seat.mjs. From project.json: qwen-code, deepseek-v4.1-flash, dashscope, dashscope-payg, worktree, ff-only, owner-account, provider-prefix, helpMode=none. Owner-selected control plane launches the seat; stored controlPlane=grok-cloud stays unchanged.
  Token in process environment only; never print keys. Prefix: AGENTS, BOARD, envelope, tools first; variable work last. No other seats.

Spend cap / fuse
  12 session turns, 24 tool calls; one correction then return. Stop on completion or a named decay sign in factory/sessions.json. No resume from prose.

Return
  Brief docs/log/f21.md: changed line, measured commands/results, self-check changes, model, spend/cache usage if reported or unavailable. No invented counts/cost or own commit id.
  Commit subject: F21: shallow checkout for the landing gate. Push writer/F21; return commit, paths and commands. Never review or land yourself.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
