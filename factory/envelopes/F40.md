ENVELOPE: F40 — CORRECTION 1 — The census names a running seat, not a mention of one (S17, S20)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Already approved — do not touch
  Both original acceptance items are APPROVED by an independent review that drove them at the
  CLI: the census counts a harness in program position and gives exactly three seats on the
  five dictated rows, and `disjoint` refuses three lanes while leaving the two-lane wording
  and exit code unchanged. Both named mutations red at the entry point. Do not re-open them,
  do not re-word them, do not widen the harness list. `disjointReport()` stays byte-untouched.

Work
  Cover the one property of the shipping census that no assertion reaches, and delete a
  sentence from the lane log that the envelope does not support.

Acceptance
  Two items. DICTATED. Do not widen, do not offer an alternative.

  Item 1 — the census self-test asserts each row's HARNESS LABEL, not just the count.
  The lane correctly deleted base main's row-equality check, because that check reached the
  helper rather than the CLI. What replaced it tests only the seat COUNT, the presence of a
  pid, and the absence of rows 9001 and 9002. Nothing anywhere asserts the VALUE of a row's
  `harness` field, so the harness-identity logic in the shipping CLI block has zero coverage.
  Assert, through the CLI as the existing cases already do, that the three counted rows carry
  harness labels qwen, goose, goose, for pids 8324, 4521 and 17 respectively.
  MUTATION THAT MUST TURN IT RED, at the production entry point: in the `if (cmd ===
  "census")` block, replace `harness: m[1].toLowerCase()` with `harness: "goose"`. Today
  `node factory/tools/seat.mjs --self-test` prints `seat self-test ok` and exits 0 with pid
  8324's qwen seat rendered as goose; after this item it must fail and name the property, that
  a row's harness label was wrong. The control plane drove this mutation and confirms it is
  green today.

  Item 2 — the lane log cites only what this envelope says.
  `docs/log/f40.md` states: "All twenty-one steps green — including step 13, which the
  envelope expected red while the envelope is live." This envelope says nothing about step 13,
  nothing about the gate being red, and nothing about envelope liveness. The historical fact
  behind it is real but belongs to F39, which landed that fix before F40 was issued. Delete
  the attribution or replace it with what is true and citable: the gate passed 21 of 21 with
  the envelope live, and if F39's history is worth a clause it names F39, not this envelope.
  This is the S18 family, absorbed this sitting as T76, and T76 does NOT catch it: T76 checks
  that cited PATHS resolve and that DATE FIELDS are not ahead of UTC. A claim attributed to a
  document that the document does not make is outside it. Say so in the log's own words.

Base
  Rebase onto live main, which has moved past this branch's base: F41 landed at 8ccc04d and
  its records at f54031c. Read `git ls-remote origin refs/heads/main` yourself; do not take
  that sha from this sentence. Keep your two commits, rebase, push the branch only.
  Branch writer/F40

Holds
  factory/tools/seat.mjs
  docs/log/f40.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours:
  the control plane writes the trap rows for S17 and S20 at landing.

Must stay true
  T72's stdin carrier and T73's key-from-the-store lines stay byte-identical to main.
  The census still reads the process table through the injected lister and reads no file.
  Every existing self-test case keeps passing. The gate stays 21 of 21.
  A false sentence in the diff is a blocking defect — item 2 IS that rule applied to a
  sentence you already wrote.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs census
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Item 1 is the second time this lane's coverage has been measured and found to test a
  different object than the one that ships (D-59, widened by D-61). When you write the
  assertion, ask what it would still pass if the harness logic were deleted entirely.
  Note for your own fixture: building one through a shell heredoc can silently collapse
  backslashes and corrupt the row you are testing. Verify the fixture's BYTES before
  believing its verdict — this cost the control plane a nearly-false finding.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY. The earlier pc-dashscope stamp was the
  control plane's error and is corrected here.
  Do not remap the control-plane session. Hands never call a model.

Spend cap
  20 tool calls, or USD 0.30, then stop and return. This is a correction, not a lane.
  This is the ONLY correction: a sweep-tier seat gets one, then the work splits.

Fuse
  Split on correction done, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope, never a transcript.

Self-check
  Drive item 1's mutation yourself, restore the file byte for byte, and confirm
  `git hash-object factory/tools/seat.mjs` matches before and after. Record what changed.

Return
  docs/log/f40.md: what the correction changed, the command and what it printed, item 1's
  mutation pasted going red at the CLI block, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
