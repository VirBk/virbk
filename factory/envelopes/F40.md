ENVELOPE: F40 — The census names a running seat, not a mention of one (S17, S20)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make both answering commands in seat.mjs answer the question they were asked.

Acceptance
  Two items. This envelope DICTATES both; do not offer an alternative, do not widen.

  Item 1 — census counts a harness that is RUNNING, not a command line that mentions one.
  A row is a writer seat only when the harness token is the program being run: the first
  token of the command line, or the first token after an interpreter (node, node.exe,
  python, python3, py). A harness token anywhere later is a mention and is not counted.
  These five lines, driven through `node factory/tools/seat.mjs census --lister <cmd>`,
  must give exactly three seats:
    8324 "C:\node\qwen-code\cli.js" -p -            -> seat   (program is the qwen path)
    4521 goose run --recipe factory/recipes/x.json  -> seat   (program is goose)
    17 node C:/tools/goose/run.py --once            -> seat   (script after an interpreter)
    9001 pip show aider-chat                        -> NOT    (mention in an argument)
    9002 powershell -NoProfile -Command Get-Ciminstance Win32_Process -- qwen
                                                    -> NOT    (mention in an argument)
  The last two are the live defect: F37's reviewer ran `pip show aider-chat` and the census
  reported two running aider seats.
  MUTATION THAT MUST TURN IT RED, at the production entry point: in the `if (cmd ===
  "census")` block, and NOT in a helper — restore the old whole-line test by replacing the
  program-position test with `SEAT_HARNESS.test(line)`. `node factory/tools/seat.mjs
  --self-test` must fail and name the property: a mention was counted as a seat.

  Item 2 — `disjoint` refuses more lanes than it can compare.
  `node factory/tools/seat.mjs disjoint F40 F41 F42` today prints a verdict about F40/F41
  and exits 0. It must instead exit non-zero and say that disjoint compares two lanes and
  was given three, naming them. Two lanes keep today's behaviour and today's wording
  exactly; the existing self-test cases must still pass unchanged.
  MUTATION THAT MUST TURN IT RED, at the production entry point: in the `if (cmd ===
  "disjoint")` block, drop the argument-count refusal so `rest[3]` is ignored again.
  `--self-test` must fail and name the property: a three-lane call was answered as a pair.

Base
  758ab128d808f75ddcedb4b5e915a23bb1fdbfd7
  Worktree from that SHA. Rebase onto live main before push. Push the branch only.
  Branch: writer/F40

Holds
  factory/tools/seat.mjs
  docs/log/f40.md
  Changed file set equals this list. A stray file is a stop.
  factory/traps.yaml is NOT yours: the control plane writes the trap row at landing.
  factory/sparks.json is NOT yours. F41 is running as your sibling on
  factory/tools/kitCheck.mjs; proved disjoint from you, so do not touch it.

Must stay true
  The census reads the process table through the injected lister and reads no file (T04).
  The lister stays injectable: `--lister <cmd>` is how every fixture drives it.
  The packet reaches a seat on stdin, never as one argv string (T72); the Windows recipe
  lines read the writer key from the user store in the same act as the launch (T73). You
  are editing the same file those live in — leave them byte-identical.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs census
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never typed from memory.

Look hardest at
  This lane's whole family of failure is a check that answers a different question than the
  one asked — that is what both items ARE. So the way this lane lies is by testing a helper
  the fixture reaches instead of the command that ships: five lanes this factory has run
  did exactly that (D-59, widened by D-61). Both mutations above are specified at the CLI
  block on purpose. If you find yourself mutating `census()` or `disjointReport()` to make
  a check go red, you have tested the wrong object and the item is not done.
  Do not widen the harness list. Do not make census read a file.

Runtime
  pc-dashscope topology, writer seat, print mode in the worktree.
  From factory/project.json: harness qwen-code, model deepseek-flash, isolate worktree.
  From factory/tools/route.mjs: path native, base https://api.deepseek.com/v1,
  key DEEPSEEK_API_KEY, providerModel deepseek-flash.
  Do not remap the control-plane session. Hands never call a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.
  Paste prompt_cache_hit_tokens or cached_tokens.

Spend cap
  40 tool calls, or USD 0.60, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope on a stronger seat.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope. Never a transcript.

Self-check
  Before returning, drive both mutations above yourself, restore the file byte for byte,
  and confirm `git diff` shows only your intended change. Record in docs/log/f40.md what
  the self-check changed.

Return
  docs/log/f40.md: what changed, the measuring command and what it printed, both mutations
  pasted going red at the CLI block, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
