ENVELOPE: F41 — A record cites only what resolves, and dates only what UTC has reached (S18)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Add two checks to kitCheck: an invented citation and a future date fail the gate.

Acceptance
  Two items, both inside `check(root)` in factory/tools/kitCheck.mjs, so `--self-test`
  runs them live against the tree under test as it already does for case A onward.
  DICTATED. Do not widen the scope to other files.

  Item 1 — a path a record cites resolves in the commit under test.
  Scan `docs/log/*.md`. A backticked token is a PATH CITATION when it contains `/`, has no
  whitespace, ends in .mjs .json .yaml .yml .md .js or .ts, and holds none of * < > ?
  (globs and placeholders are not citations). Every path citation must exist in the
  commit under test, OR appear in an allowlist you add to kitCheck.mjs: an array of
  {path, why} where `why` is one line saying why it does not resolve. Seed the allowlist
  with exactly these five and no others, each with its own reason:
    factory/envelopes/F33.md        an envelope is deleted at landing (AGENTS sec 3)
    factory/help.json               named by T47, written only when helpMode is not none
    docs/log/landing-checkout.md    cited by f21.md and never written
    factory/not-in-the-tree.md      a deliberate absent-file example in f36.md's prose
    tools/check.yml                 a path in a CHILD's tree, not this one
  An allowlist entry whose path DOES resolve must also fail: a stale exemption is a lie.
  MUTATION THAT MUST TURN IT RED, at the entry point the gate runs: add a sixth citation
  to a real record, `docs/log/f41.md` citing `factory/tools/ghost.mjs`, then run
  `node factory/tools/kitCheck.mjs --self-test`. It must fail and name file and token.
  Restore, and paste the restored run going green.

  Item 2 — a date field is never ahead of UTC.
  Check the `at` field of every row in factory/decisions.json and every row in
  factory/board.json's `ledger`. Each must be a YYYY-MM-DD no later than the UTC date read
  at check time — `new Date().toISOString().slice(0,10)`, read in the same act as the
  comparison (T04). FIELDS ONLY. Prose is NOT scanned: board.json's ledger already
  contains the string 2026-09-21 inside a row that NARRATES the mis-dating incident, and
  that row is correct English and must stay green. A check that reds on it is wrong.
  MUTATION THAT MUST TURN IT RED, at the entry point the gate runs: set one decisions.json
  row's `at` one day ahead of UTC, run `node factory/tools/kitCheck.mjs --self-test`, and
  it must fail naming the row id and both dates. Repeat for one board ledger row. Restore
  both byte for byte and paste `git diff --exit-code` printing nothing.

Base
  758ab128d808f75ddcedb4b5e915a23bb1fdbfd7
  Worktree from that SHA. Rebase onto live main before push. Push the branch only.
  Branch writer/F41

Holds
  factory/tools/kitCheck.mjs
  docs/log/f41.md
  Changed file set equals this list. A stray file is a stop. You may READ any record; you
  may WRITE only these two. factory/traps.yaml is NOT yours: the control plane writes the
  trap row at landing. F40 is your sibling on factory/tools/seat.mjs; do not touch it.
  If item 1 reds on a record outside your holds and outside the five seeded entries: STOP,
  do not edit it, do not allowlist it to silence it, return with the list. Repairing a
  record is the control plane's act at landing.

Must stay true
  factory/landing-checks.json is not in your holds and needs no edit: gate step 14 runs
  `kitCheck.mjs --self-test`, which already calls check() live, so putting your checks
  inside check() is what puts them in the gate.
  Every existing case in check() and in the fixture self-test keeps passing unchanged.
  The gate must still print GATE PASSED at 21 of 21. A false sentence is a blocking defect.

Verification
  node factory/tools/kitCheck.mjs --self-test
  node factory/tools/kitCheck.mjs
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  This lane exists because a log cited a git ref that never existed as evidence for a claim
  that happened to be true, and a review reading for plausibility passed it (S18). The way
  THIS lane lies is by building its assertion from its own scanner: a fixture whose expected
  list is produced by the function under test cannot fail. Both mutations above are specified
  against the real tree through the real command for that reason (D-59, widened by D-61).
  Do not scan prose for dates, nor files other than docs/log/*.md for citations.

Runtime
  pc-dashscope, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, providerModel deepseek-flash.
  Do not remap the control-plane session. Hands never call a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.
  Paste prompt_cache_hit_tokens or cached_tokens.

Spend cap
  40 tool calls, or USD 0.60, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Before returning, drive all four mutations yourself, restore every file byte for byte,
  and confirm `git diff` shows only your change. Record in docs/log/f41.md what it found.

Return
  docs/log/f41.md: what changed, the command and what it printed, each mutation pasted
  going red through the real command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
