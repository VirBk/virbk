ENVELOPE: F52 — CORRECTION 1 — a sentence went false in the log, and a fold leaked into a shipped path
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Correct the log sentence the review falsified, restore the invariant seatReturn.mjs states, and close the token gap the scan still has.

Acceptance
  node factory/tools/seatReturn.mjs --self-test  (step 20) prints ok at exit 0,
  steps 08 and 09 stay ok, and the full gate passes 22 of 22.
  Your six properties are PROVED and are not reopened: the review drove its
  own mutation for each and every one red. This is what it found after.

  C1  BLOCKING, and the collision behind it is the control plane's. The log
      says "The qwen-code harness, its desktop-qwen alias and every `qwen
      --auth-type openai` line are untouched." That is false: the count goes 10
      at 008a309 to 9 at your tip, and the line removed is
      `#   qwen --auth-type openai --model qwen3-coder`. REMOVING IT WAS RIGHT -
      P2 forbids a dropped id in a rendered recipe - and the first envelope's
      Must-stay-true demanded every such line stay, so that envelope told you
      two contradictory things about one line. You resolved it right and then
      reported the opposite. AGENTS section 7 asks a writer meeting a
      conflict mid-lane to PRINT the decision and what it observed. Say in the
      log that exactly one launch line went, name it, name the collision, and
      state that nine lines, the alias, the topology id, the contract enum and
      the meter are intact. No code change.
  C2  The null fold reaches a SHIPPED path, not only fixtures. providerId now
      answers null for an unrecognised id, so sameModel calls any two unknowns
      equal, and seatReturn.mjs line 79 says in its own words "An id the table
      does not list passes through unchanged and agrees only with itself."
      Your diff made that sentence false. It is not confined to the self-test:
      record() at line 477 is exported and line 494 does
      `meter.ids.find((id) => sameModel(id, row.model))`, so a return claiming
      an unknown model would be accepted against a meter that recorded a
      DIFFERENT unknown model. Restore the stated invariant under D-71: an
      unrecognised id has no canonical form, so it agrees with itself and with
      nothing else. Do NOT fix this by deleting the assertion at line 796 -
      under the restored invariant that line becomes a real regression test for
      this defect and must stay and must pass. Mutation: let two unrecognised
      ids fold together again. Step 20 reds at 796.
  C3  Drop the pair row ["qwen3-coder","qwen3-coder-plus"] at 786 and make the
      comment at 782 name the count it then has. Both ids are dropped by D-68,
      and the row is ALREADY vacuous at your tip: all four clauses pass on
      null === null, proving nothing. That is T13, and F48's shape. Name in
      the log which live pair still proves the fold.
  C4  The dropped-id scan misses an id in trailing-punctuation position.
      ID_SEPARATORS at route.mjs:106 keeps `.` `_` `-` inside a token, so
      `qwen3-coder.` is a different token and
      `# Previously this seat ran qwen3-coder.` leaves step 09 ok at exit 0
      with a dropped id in a rendered recipe. Test each token again with
      trailing [._-] trimmed. This is the opposite edge of the bug your own
      self-check item 2 closed; close both. Mutation: that comment line. Step
      09 reds.
  C5  Log accuracy, no code. P4 reds fourteen clauses, not the five pasted;
      the four CLI ones are the D-71 proof, so paste them. And "the comment at
      781-783" cites what is not there: the phrase is on 782 (T76).

Correction
    Pushing after a correction
      The rebase has diverged your branch from the tip you pushed. You are
      authorised to run
        git push --force-with-lease origin writer/F52
      on YOUR LANE BRANCH ONLY. main is never forced and no other branch is
      yours to rewrite. If force-with-lease is itself rejected, stop and
      return: that means something else moved your branch.

Base
  The commit CARRYING this envelope on live main; your worktree is rebased onto
  it before you start (T81). Rebase onto live main before push.

Holds
  factory/tools/seatReturn.mjs
  factory/tools/route.mjs
  docs/log/f52.md
  Changed file set equals this list. factory/tools/seat.mjs is DONE this round;
  a diff against it is a stray file and a stop. Do not edit writer-paths.json.

Must stay true
  - Everything the first envelope required still holds. Steps 08 and 09 stay ok
    and every property stays proved.
  - Reading a meter row that NAMES a dropped model still works. A meter written
    last sitting legitimately names a model since dropped, and refusing to parse
    one would lose spend history. D-68 did not ask for that.
  - A false sentence in the diff is a blocking defect. Comments count: this
    correction exists because two went false.

Verification
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/route.mjs --self-test
  node factory/tools/seat.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste the mutation, command and failing line for C2, C3 and C4.

Look hardest at
  The fix that deletes an assertion instead of restoring an invariant. Turning
  step 20 green by removing line 796 leaves sameModel willing to call two
  strangers the same model in record(), which is D-71's own hazard one file
  over, and a green bought by removing the only thing that could fail is what
  D-61 named. C4 has the same trap: widening the separator class instead of
  trimming the token re-opens the bug your self-check already closed.

Runtime
  desktop-qwen. Writer seat only. Worktree only. Do not remap the control plane.

Spend cap
  Tool calls: 25. A cap is a return, not a hang.

Self-check
  Before returning, drive the list a reviewer would be given and fix what you find.

Return
  Append to docs/log/f52.md: what changed, the measuring command, no commit id.
