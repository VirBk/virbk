ENVELOPE: F52 — No tool offers a model the catalog dropped (D-68)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Take every dropped id and every hardcoded route out of route.mjs and seat.mjs; make a check refuse their return.

Acceptance
  node factory/tools/route.mjs --self-test  (gate step 08) and
  node factory/tools/seat.mjs --self-test   (gate step 09)
  both print ok at exit 0, and each property reds its named step under its own
  mutation.

  P1  No dropped id in route.mjs. The ids are READ from
      factory/writer-paths.json dropped.ids — six today — never copied into a
      list of your own (T29, T75). Mutation: put "qwen3-coder" back into
      HOSTED_IDS. Step 08 reds.
  P2  No dropped id in any RENDERED seat recipe — rendered, never the source
      text that produced it (D-59). Mutation: put ollama/qwen3-coder back into
      the aider recipe. Step 09 reds. The fixture want("aider carrier", ...)
      pins that exact string and must move WITH the recipe; moving the fixture
      alone is this lane's defect.
  P3  The check examines a list it did not write, and reports how many ids it
      examined. Drive it through a seam with your own fixture lists, INCLUDING
      the empty one, which must FAIL naming that it examined nothing — a pass
      there cannot be told from a check that never ran (F49). Then prove the
      wiring at the production call site, not the helper (F50): a fixture reads
      factory/writer-paths.json itself and asserts the live check reports that
      file's own dropped.ids count. Mutation: hand the seam a hardcoded six.
      The wiring fixture reds while P1 and P2 stay green — that gap IS D-68.
  P4  An id the router does not recognise is refused, not routed. Proved at the
      objects before issue: the typo deepsek-flash resolves to path dashscope,
      key DASHSCOPE_API_KEY, reason qwen-has-no-native — every clause false,
      and it buys a launch at the wrong base with the wrong key. Ruling D-71:
      the resolver names the id unrecognised and the CLI exits non-zero.
      familyOf's "not deepseek means qwen" is the defect; a family comes from
      the catalog, not a prefix. Mutation: restore the fallthrough. Step 08 reds.
  P5  seat.mjs holds no model map of its own. Its HOSTED is byte-identical to
      route.mjs's HOSTED_IDS today: T75 surviving in the file D-67 did not
      reach. Import a PURE exported mapper from route.mjs — never resolveNow,
      which probes the network; route.mjs's entry guard means the import runs
      no CLI. Mutation: change route.mjs's map alone; seat.mjs must follow with
      no edit of its own, or step 09 reds.
  P6  No recipe hardcodes a base. Five sites carry the dashscope-intl URL that
      writer-paths.json bases.dashscope already owns — T75, in the file a
      launcher reads. Render it from the spec and assert BOTH directions: every
      rendered dashscope base equals the spec's, and no rendered recipe carries
      a dashscope URL that is not it. Mutation: hardcode one back. Step 09 reds.

Base
  1ad224bc561e2cb971eaf1c9e1414da598786ee7 — the commit the work is based on.
  The worktree is cut from the commit that CARRIES this envelope: that one or a
  later one (T81). Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/route.mjs
  factory/tools/seat.mjs
  docs/log/f52.md
  Changed file set equals this list. A stray file is a stop.
  factory/writer-paths.json is NOT yours. P3 and P6 READ it; neither edits it.
  A diff against it is a stray file and a stop.

Must stay true
  - The qwen-code HARNESS stays. D-68 drops MODELS. The topology id
    desktop-qwen, the contract enum entry, the qwen usage_record.jsonl meter
    and every `qwen --auth-type openai` launch line stay. Deleting the harness
    is the failure this lane is most likely to produce.
  - T48's native/DashScope id map stays and is still exercised.
  - seatReturn.mjs is out of scope by decision: its Qwen ids are METER
    fixtures, and a meter is not a tool offering a model. Do not touch it.
  - A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/route.mjs --self-test
  node factory/tools/seat.mjs --self-test
  node factory/tools/landingGate.mjs
  For each of P1-P6 paste the mutation, the command, and the failing line.

Look hardest at
  D-69's question: WHICH OBJECT DOES THIS MUTATION EDIT, AND IS IT THE OBJECT
  THE GATE INVOKES. Three lanes last sitting shipped a defect proved at a
  helper while nothing drove the line the gate reaches. Every mutation above
  edits route.mjs or seat.mjs, and its red is read from the exact command step
  08 or 09 runs; a red from a fixture, a helper or a stripped copy proves
  nothing. P3's wiring half and P5 are where this lane will lie: a check
  carrying its own hardcoded six passes P1 and P2 and IS the defect D-68 named.
  A tool-policy refusal is not a reason this envelope waits.

Runtime
  desktop-qwen. Writer seat only. Do not remap the control-plane session.
  Worktree only. Hands: node factory/tools/hands.mjs, never a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first; variable
  work last. Paste prompt_cache_hit_tokens or cached_tokens.

Spend cap
  Tool calls: 45 — six properties, two files. A cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope —
  never a transcript, never a compaction.

Self-check
  Before returning, drive the list a reviewer of this lane would be given and
  fix what you find.

Return
  A few lines in docs/log/f52.md: what changed, the measuring command, what the
  self-check changed, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
