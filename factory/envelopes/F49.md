ENVELOPE: F49 — A correction envelope authorises the force its own rebase forces (S26)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  The envelope template gains the clause a correction needs, and a NEW checker with a gate step proves every correction envelope carries it. Absorbing a lesson without a check is T24; nothing in this repository reads factory/templates/ENVELOPE.md today.

What you cannot see from the file
  T81 makes a seat read its envelope from its own worktree and refuse when that copy differs from live main. So a correction is written to main and the control plane must REBASE the seat's worktree before relaunching. That rebase diverges the lane branch from the tip it already pushed, and AGENTS section 3 says only push the branch only - nothing authorises the force-with-lease that then needs. F44's seat hit it: it finished the correction, was rejected non-fast-forward, correctly refused to rewrite a shared ref on its own authority and handed the call up. A control plane finished that return by hand. That is S26.
  The control plane hand-wrote this clause into F47's and F48's correction envelopes this sitting rather than wait for you. Both envelopes are deleted now those lanes have landed, so the shape survives only in their landing commits.
  A SECOND falsehood in the same file, found at the objects by the F48 seat and confirmed: the template says Base <sha> then "Worktree from that SHA". Since T81 landed that is impossible - the envelope is committed AFTER that sha, so a worktree cut there does not hold the envelope and packet.mjs seat refuses with empty stdout. Correct it: Base names the commit the work is based on, and the worktree is cut from the commit that CARRIES this envelope.

The rule to implement
  An envelope whose first line contains CORRECTION must carry, in its own text, an authorisation to force-with-lease ITS OWN lane branch and a sentence saying main is never forced. A non-correction envelope need carry neither.
  The checker reads factory/envelopes/*.md as text. It calls no git and needs no repository: the gate runs it from an unpacked archive. Add the step to factory/landing-checks.json.

Acceptance
  Six properties. Each gets its own fixture and its own mutation, and each mutation must red for its property ALONE. If one mutation reds two, one of them is untested. Paste what every mutation printed, naming the property it reds for.
  A FIXTURE THAT EXPECTS NO ERROR MUST STILL BE ABLE TO FAIL. F48 shipped one that stayed green when the checker discarded its input, because a pass and a never-examined look the same. Where you expect a pass, assert the positive fact too - the lane parsed, the clause found.
  1 A correction envelope carrying the clause for its own lane passes, and the checker names the lane it parsed.
  2 A correction envelope with no clause fails, and the message names the lane.
  3 A correction envelope whose clause names a DIFFERENT lane's branch fails. This is the member most likely to escape, because a correction envelope is written by copying the last one.
  4 An envelope that is not a correction and carries no clause passes.
  5 An envelope authorising a force on main fails, whatever else it carries.
  6 The checker runs with no repository present, calls no git, and an EMPTY envelopes directory is a pass and not a failure (T09: a check that cannot pass is not a check).

Base
  The commit the work is based on is live main at issue. Your worktree is cut from the commit that carries this envelope, which is that commit or a later one. Rebase onto live main before push. Push the branch only.

Holds
  factory/templates/ENVELOPE.md
  factory/tools/envelopeCheck.mjs
  factory/landing-checks.json
  docs/log/f49.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  The template stays one page and every envelope built from it stays under 6 KB; the size band fails the gate.
  The new step passes on Ubuntu in the public landing Action, which runs with no secrets.
  Adding a step changes the gate's count: the board, this envelope and any record saying 21 must say what the new number is, and you must paste the gate output that shows it.
  The checker does not read, write or require .git.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/envelopeCheck.mjs --self-test
  node factory/tools/landingGate.mjs
  Six mutations, one per property, each pasted with its output.

Look hardest at
  A rule that passes because it examined nothing. Point it at a directory holding no correction envelope and check the fixtures still tell a pass from a skip.
  Matching the clause so exactly that rewording the template breaks the check, or so loosely that the word force anywhere on the page satisfies it. Say in the log which you chose.
  If the gate reds ALL 21 steps at any commit, your bash is the WSL launcher and the red is not yours: that is S24. Say so in the log, use git-bash, do not spend calls proving it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate).
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  40 tool calls. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a decay sign (factory/sessions.json). On one: stop, print the signs, return.

Self-check
  Before returning, ask of each fixture whether it COULD have failed, and prove it with the mutation you pasted. Record in docs/log/f49.md what the self-check changed.

Return
  A few lines in docs/log/f49.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
