ENVELOPE: F42 — CORRECTION 1 — The TRAPS parser (S19)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Already approved — do not touch
  All three items are APPROVED by an independent review that drove them and probed their
  boundaries: last-wins and case-sensitivity red separately; unquoting strips one matching
  pair at the two ends only, proved against an apostrophe inside a rule, mismatched quote
  characters, a doubly quoted value and a quote at one end only; a reject row is held and
  named; and the claim that `reject` is already in the intake contract's enum is true at the
  file. Do not re-open any of that. Do not add a contract field.

Work
  Give two claimed properties of item 3 a fixture that could fail them.

Acceptance
  The defect is this lane repeating, one level up, the defect it existed to correct: F35
  proved last-wins and left case-sensitivity unproven; this lane proved that a reject row is
  held and left two of item 3's other properties unproven. The control plane's envelope is
  the cause and D-62 now says so — a mutation per PROPERTY, not per item. A fixture proves a
  property only if the fixture could fail it.

  Two items. DICTATED. Both are fixtures plus their reds; the shipping logic is already right
  and must not change.

  Item 1 — the case-fold on a refusal is proved.
  `portableOf` lowercases the kind, and every fixture row writes `reject` already lowercase,
  so nothing tests the fold. Add a fixture row whose kind is mixed case — `Reject` — and
  which must still be held with reason `kind reject`.
  MUTATION THAT MUST TURN IT RED, at the object the gate runs: in `portableOf`, drop the
  `.toLowerCase()` so `const k = collapse(kind);`. `node factory/tools/alumni.mjs --self-test`
  is GREEN today under that mutation, which the control plane drove and confirms. After this
  item it must fail and name the property, that a mixed-case refusal was not honoured.

  Item 2 — "the refusal is read before the floors" is proved.
  Your own comment states this ordering as a property. The only reject fixture clears both
  the check floor and the rule floor, so nothing distinguishes reading the refusal first from
  reading it last. Add a fixture row that is BOTH a refusal AND would fail a floor — a
  `kind: reject` row that names no check — and assert it is held with reason `kind reject`,
  not with `names no check`. The reason is the evidence of the ordering.
  MUTATION THAT MUST TURN IT RED: in `trapsReport`, swap the order so the check floor is
  tested before the refusal. `--self-test` is GREEN today under that swap, which the control
  plane drove and confirms. After this item it must fail and name which reason was given.

  Each mutation must red for ITS OWN item alone. If one reds both, one property is untested
  and the item is not done.

Base
  Rebase onto live main; read `git ls-remote origin refs/heads/main` yourself and do not take
  a sha from this sentence. Keep your two commits, rebase, push the branch only.
  Branch writer/F42

Holds
  factory/tools/alumni.mjs
  docs/log/f42.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours.
  contracts/intake.v1.json is NOT yours. F43 is in correction on factory/tools/seat.mjs.

Must stay true
  The shipping behaviour does not change in this correction: `portableOf` keeps its fold and
  `trapsReport` keeps its order. You are adding fixtures that can fail, and nothing else.
  The scan stays READ-ONLY on a child tree. A row naming no check is still held and named.
  Every one of this repository's own traps.yaml rows the scan emits still passes
  `node factory/tools/intake.mjs` unedited.

  One more, and it is a correction of the log rather than the code: docs/log/f42.md pastes
  `18388 bytes` for the emitted rows with no command behind it, and neither natural
  reproduction matches — the full intakes output is 18434 bytes and the trap rows alone are
  17997. AGENTS section 4 is absolute here: a count is pasted from the command that produced
  it, with the command, or omitted. Either paste the command that prints your number or
  delete the figure. The substantive claim behind it, that the emitted rows are byte-identical
  at base and now, was independently reproduced and is true; it is the uncommanded number that
  must go.

Verification
  node factory/tools/alumni.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Ask of each new fixture: could this row fail the property if the logic were deleted? A
  lowercase-only fixture cannot prove a case-fold. A reject row that clears every floor cannot
  prove an ordering against the floors. That question is the whole content of D-62 and of this
  correction.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY.
  Do not remap the control-plane session. Hands never call a model.

Spend cap
  20 tool calls, or USD 0.30, then stop and return. This is a correction, not a lane.
  This is the ONLY correction: a sweep-tier seat gets one, then the work splits.

Fuse
  Split on correction done, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive both mutations yourself, restore byte for byte, and confirm `git hash-object
  factory/tools/alumni.mjs` matches before and after.

Return
  docs/log/f42.md: what the correction changed, the command and what it printed, each
  mutation pasted going red and reding for its own item alone, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
