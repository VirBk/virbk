ENVELOPE: F50 — CORRECTION 1 — The entry-point fixture runs where the gate runs
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Where this lane stands
  Five of your six properties are APPROVED and the lane's central claim is proved. An independent review and the control plane both drove it: in a bare copy with no .git, removing the CLI wiring now REDS the self-test, which is exactly what F47 left open and what this lane exists to close. Also cleared: the checkout context still reds the same way, the EBUSY cleanup line reports without masking a verdict, a disabled cleanup reds [7 cleanup] alone, spawning the launch tree's file instead of the throwaway copy reds [7 own copy] alone, GATE_SHELL unset still defaults to bash, every [N label] tag is unique, the gate is 21 of 21 with the step count unchanged, and factory/landing-checks.json is untouched. Your honesty disclosures - the first gate run defaulting to --sha HEAD and so archiving the base, and the compaction mid-lane - were checked and cost the lane nothing. Do not redo any of it. This is your ONE correction: sweep tier gets one, then the lane splits.

The defect - property 4 is proved at the helper, not at the object that ships
  fixture10 calls makeTempRepo directly with an injected refusing spawn and asserts on the returned error string. It never drives fixture7. fixture7 calls makeTempRepo with no injected failure, so on every ordinary run repo.error is empty and fixture7's OWN error branch never executes. Nothing standing protects that branch.
  Driven at the objects, in a bare non-repository copy with git hidden from PATH so the failure is real and not mocked. The shipped code is correct and fails loudly:
    [7 entry point] the throwaway repository could not be created - git init failed: spawnSync git ENOENT
  Delete the single errors.push line inside that branch, keeping the return, and the same command prints
    landingGate self-test ok (unpack only; no repository here)
  at exit 0. That is the silent skip this lane was issued to delete, reintroduced in the lane that deleted it, with no fixture catching it. D-61 in its own words: the mutation must red at the production object, not at a helper a fixture happens to reach.

Correction A - fixture7's own error branch gets a fixture that could fail
  Drive fixture7's CONSUMPTION of a failed repository, not makeTempRepo alone: inject a refusing spawn into the path fixture7 takes, and assert it reports and names the reason. It must not require git to be absent from the real environment, because the gate and the Action both have git.
  Prove the fixture COULD fail: delete that errors.push line, run the self-test, paste it red, restore, paste it green.

Base
  Your worktree has been rebased by the control plane onto live main, which has moved: F46 landed. Read the first line of this file before you start: if it does not say CORRECTION 1, stop and say so (T81).

Holds
  factory/tools/landingGate.mjs
  docs/log/f50.md
  Changed file set equals this list. A stray file is a stop.

Pushing after a correction
  The rebase has diverged your branch from the tip you pushed. You are authorised to run
    git push --force-with-lease origin writer/F50
  on YOUR LANE BRANCH ONLY. main is never forced and no other branch is yours to rewrite. If force-with-lease is itself rejected, stop and return: something else moved your branch.

Must stay true
  All existing fixtures stay green and keep their meaning. Do not weaken one to make room.
  Every [N label] tag stays unique; do not collide with [4 diagnostic], [5 own form], [6 status], [7 entry point], [7 cleanup], [7 own copy], [8 spawn cause], [9 default], [10 repo failure] or [11 cleanup].
  The gate stays 21 steps and factory/landing-checks.json is untouched.
  --self-test passes from the worktree AND from a copy in a directory with no .git.
  GATE_SHELL keeps its default of bash when unset.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/landingGate.mjs --self-test, from the worktree AND from a bare copy with no .git
  node factory/tools/landingGate.mjs
  The new mutation pasted red and then green after restore.

Look hardest at
  Asserting on makeTempRepo again instead of on fixture7's use of it. That is the whole finding; a second fixture on the helper changes nothing.
  A fixture that needs the real environment to lack git. The gate has git, so such a fixture would skip there - which is the defect class this lane is named for.
  Your mutation harness left scratch directories under the OS temp dir - f50-M1, f50-M2, f50-M4, f50-M5, f50-M5b, f50-bare, f50-mut and two .mjs copies. Put this round's scratch under one directory and remove it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate). The route is the DeepSeek direct api: writerPath is pinned native (D-67) and the launcher asks route.mjs.
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  25 tool calls for one correction in one file. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a decay sign (factory/sessions.json). Your last session compacted; if this one does, stop and return rather than reconstructing.

Self-check
  Before returning, ask of the NEW fixture whether it could have failed, and prove it with the mutation you pasted. Record in docs/log/f50.md what the self-check changed.

Return
  A few lines added to docs/log/f50.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
