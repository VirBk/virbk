ENVELOPE: F50 — The entry-point fixture runs where the gate runs, not only from a checkout
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make the entry-point fixture in factory/tools/landingGate.mjs build its own throwaway repository, so the fixture that proves the shell probe is actually wired into the shipped CLI runs where the GATE runs, and not only from a checkout.

What you cannot see from the file
  F47 landed this sitting after one correction whose blocking defect was that nothing exercised the lines wiring the probe into the command a person types. Its fix, fixture 7, spawns the SHIPPED FILE as a process with GATE_SHELL set and asserts the probe ran before the archive was unpacked. From a checkout that works and the mutation reds.
  But fixture 7 needs a repository to resolve a commit, so it SKIPS when there is none. Gate step 15 runs node factory/tools/landingGate.mjs --self-test with cwd inside the unpacked archive, which is not a repository. The control plane verified the consequence at the objects: copy landingGate.mjs and experiment.mjs into a directory with no .git, remove the CLI wiring, and the self-test still prints "landingGate self-test ok (unpack only; no repository here)" at exit 0. So the property is proved by a command a person runs from a checkout and NOT by the gate that protects main.
  The seat disclosed this itself, named this remedy, and judged it past its correction cap. That is why this is a lane, not a third round.

The rule to implement
  Fixture 7 creates its own throwaway git repository under the OS temp directory - init, one commit, enough for the CLI to resolve a sha - spawns the shipped file there, and removes it. It must not depend on the tree it runs from.
  A skip is the thing this lane exists to delete. If the repository cannot be created, the fixture FAILS and says why; it never passes quietly.

Acceptance
  Six properties. Each gets its own fixture or assertion and its own mutation, and each mutation must red for its property ALONE. If one reds two, one is untested. Paste what every mutation printed and the property it reds for.
  A FIXTURE THAT EXPECTS NO ERROR MUST STILL BE ABLE TO FAIL: F48 shipped one that stayed green when the code discarded its input, because a pass and a never-ran look the same. Prove your fixture RAN, not merely that nothing complained.
  1 With NO repository present, the self-test still prints ok at exit 0 at this commit. This is gate step 15's own context and the ordinary case; it needs a mutation that reds it.
  2 With NO repository present, removing the CLI wiring - const stop = null - reds the self-test. This is the property F47 left open and the reason for the lane.
  3 From a checkout, that same removal still reds, exactly as it does today. F47's coverage is not traded away for this one.
  4 A throwaway repository that cannot be created FAILS the self-test with a message naming why. Prove it by making the creation fail, and show it does not skip.
  5 The self-test leaves no repository behind, and a cleanup that cannot complete is REPORTED, not thrown. F47's fixture 5 already hit EBUSY when a launcher held its cwd, so this is not hypothetical.
  6 The fixture does not read the repository it was launched from: running it from this checkout and from a bare directory produces the same verdict.

Base
  The commit the work is based on is live main at issue. Your worktree is cut from the commit that carries this envelope. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/landingGate.mjs
  docs/log/f50.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  Fixtures 1 to 9 stay green and keep their meaning. Do not weaken one to make room.
  Steps still run through bash -e from a file, exit captured, never piped away (T14, T10).
  The gate still builds its own archive of the commit under test (T08) and is 21 of 21 at your commit.
  GATE_SHELL keeps its default of bash when unset.
  No new gate step; the count stays 21.
  The public landing Action runs this on Ubuntu with no secrets.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/landingGate.mjs --self-test, from the worktree AND from a copy in a directory with no .git
  node factory/tools/landingGate.mjs
  Six mutations, one per property, each pasted with its output.

Look hardest at
  A fixture that silently skips. That is exactly what you are here to remove, and the easiest way to ship it again is a try/catch that swallows a failure to create the repository.
  Leaving the temp repository behind on a failing path, or throwing a cleanup error into the report so a real finding is hidden behind it.
  If the gate reds ALL 21 steps at any commit, your bash is the WSL launcher and the red is not yours (T70). Say so in the log, use git-bash, do not spend calls proving it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate).
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  40 tool calls. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). On a decay sign: stop, print the signs, return.

Self-check
  Before returning, ask of each fixture whether it COULD have failed, and prove it with the mutation you pasted. Record in docs/log/f50.md what the self-check changed.

Return
  A few lines in docs/log/f50.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
