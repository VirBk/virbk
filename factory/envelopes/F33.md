ENVELOPE: F33 — Packet windowing and growth bands
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Split the packet gate step so a suspension can be narrower than its hypothesis; build the 80 per cent growth band AGENTS section 4 requires; give spark.mjs the rotation ledger.mjs has.

Acceptance
  Three parts, each proved by a command that fails on revert (T13).
  1. landingGate.mjs runs 22 steps, all green. packet.mjs --self-test keeps the invariants it already asserts and no longer calls checkErrors; checkErrors becomes a second gate step running packet.mjs --check. Paste both proofs: a busted prefix fails the invariant step and NOT the budget step; a tree over the seat cap fails the budget step and NOT the invariant step.
  2. sizeBudget.mjs prints a percentage-of-cap line for every budgeted file AND for the three context budgets (seatPacketKb, cpPacketKb, envelopeKb), bands anything at or over 80 per cent, exits 0. Only over-cap exits 1. --self-test covers 79 per cent silent, 80 per cent banded and exit 0, over cap exit 1.
  3. spark.mjs rotate moves absorbed and dropped sparks out of factory/sparks.json into docs/sparks/<YYYY-MM>.md, oldest first, leaving open sparks alone. --self-test covers it on a fixture: closed moved, open kept, second run moves 0, archive plus live file reconstruct the original item list byte-exact in original order.

Base
  f47edce61e055f107a2d49176d847f9f8daa68de
  Worktree from that SHA. Rebase onto live main before push. Push the branch writer/F33 only.

Holds
  factory/tools/packet.mjs
  factory/tools/sizeBudget.mjs
  factory/tools/spark.mjs
  factory/landing-checks.json
  docs/log/f33.md
  Changed file set equals this list; a stray file is a stop. landing-checks.json is a control-plane hold, handed to this lane for its duration because the lane runs ALONE — stated, not done silently.
  budgets.json is NOT yours: the band is 80 per cent of the caps already there, needing no new field. If you find it does, name the field in the log and stop.
  sparks.json is NOT yours: build rotate against a temp fixture. Running it live is the control plane's act at landing, as ledger.mjs rotate is.

Must stay true
  Coverage does not drop: every assertion step 16 ran before this lane still runs after it, in one of the two steps.
  A band schedules already-authorized work and never creates mutation authority; only size over ceiling fails the guard (D-54). An archive is frozen, verbatim, in original order: append, never edit, and nothing in it authorizes anything.
  rotate is idempotent, never touches an open spark, and the open-spark TTL that fails the gate keeps working. look still prints open only; close still refuses with an open spark. Read the archive before writing it: appending twice must not double a row (T65).
  No copied numbers: every count is pasted from the command that produced it, with the command (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/sizeBudget.mjs
  node factory/tools/sizeBudget.mjs --self-test
  node factory/tools/spark.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed, not a word about it (T56), and the two revert proofs. Long jobs: detached log, rc marker, poll in the foreground, never end the turn between polls.

Look hardest at
  A step that cannot fail: moving checkErrors out of --self-test and forgetting to wire --check in is a silent coverage hole, the exact hole this lane exists to close (T13, T14).
  A band that fails: exit non-zero at 80 per cent is a ceiling, not a band, and turns the gate red on AGENTS.md today.
  A rotate that loses a row: the reconstruction check is the point, not decoration.
  A value read from memory instead of the file that owns it (T04): item counts and byte counts come from the command, in the same act as the write.
  No law, circular, RFC or queue name here, so no fetch is owed. A tool-policy refusal is not a reason this envelope waits. Keystone: NONE — governance maintenance under D-54, protecting the reading path the keystone loop runs on.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak.
  Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29).
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first; variable work last. Paste prompt_cache_hit_tokens or cached_tokens (T45). Do not store answers.
  You echo a stamp, not your model (T69); the control plane fills the true id from the meter.

Spend cap
  Tool calls: 90 — the floor is 40 for up to three files, and each of this lane's three parts carries a self-test and a revert proof. A cap the Holds cannot be satisfied within is badly drawn, not violated: if 90 is wrong, return and say so.
  USD 0.25, then stop and return. Hitting the cap is a return, not a hang. Escalation is a new envelope on a stronger seat.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope — never a transcript or a compaction (T25, T26). A fresh same-tier instance beats a tired cheaper one (T28). On a decay sign: stop, print the signs, return.

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f33.md what it changed.

Return
  A few lines in docs/log/f33.md: what changed, the measuring command, no commit id of your own. Name the live drop rotate will produce when the control plane runs it, and say you did not run it.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
