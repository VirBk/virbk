ENVELOPE: F33 — split seat, after the correction cap
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Narrow one false claim in spark.mjs to what is true, make two assertions falsifiable, fix two sentences. Mostly deletion.

  The previous seat used its correction. Nothing it wrote is wrong except what is listed here: two reviews confirmed the gate split, the band, rotation, the rebase, and that coverage held. Do not redesign it or improve anything unlisted.

Acceptance
  This envelope DICTATES. A previous one said "either A or B", and that is how both defects got in.
  1. Delete the position machinery. rotate must NOT write `at`, and nothing may promise the archive can be read back into the live file to rebuild the original list. `at` indexes the list as it stood at that rotation, so a second productive rotation collides: archive the live store, absorb S14, rotate again, and S14 lands at slot 1 against S02 at slot 1, duplicate slots {1: 2}. The claim is unrecoverable, so it goes, along with the reconstruction assertion and the interleave guard that made it falsifiable.
  2. Say what IS true, in the comment and the archive header: the archive holds the closed rows in the order the file held them, the live file the open ones in that order, and rotation is not a reversible transform. No sentence may imply otherwise.
  3. Replace them with four assertions, each failing on revert (T13), each revert pasted:
     a. the archive holds exactly the closed rows in file order — sorting them fails it;
     b. the live file holds exactly the open rows in file order;
     c. two productive rotations with a close between leave no duplicate id in the archive — the case that broke item 1, and it must be exercised: the existing second-rotation probes move zero rows and assert nothing;
     d. rotate is idempotent and never touches an open spark (keep what exists).
  4. packet.mjs: the --root order assertion is satisfied by the live tree. Both spawns assert only exit 0 and "packet check ok", which the live repo prints too, so a --root whose VALUE is ignored passes green. Make the fixture over-cap so the trees give opposite verdicts. Prove it: parse --root, ignore its value, and the self-test must go red.
  5. sizeBudget.mjs: a string cap prints "n/a" and "no capKb" while banded() and pct() coerce it into a correct percentage — one run, two answers. Treat a non-number cap as missing, down item 4's path.
  6. docs/log/f33.md carries two false sentences. "the four changes above are what the self-check found" — they were the envelope's Acceptance list, dictated by a review; say what the self-check actually found. "archive plus live rebuilds the list in its own order" — false, and after item 1 also gone. Fix both, change nothing else; the 21-steps finding and the two-red analysis are correct and confirmed.

Base
  805be61eb64aa8c9e1e6e70f08da985666e6e6d9
  writer/F33 is at 156f40b and carries work that stays. Rebase onto that base, then do this. Prove the rebase with git range-diff, do not assert it. Push the branch only.

Holds
  factory/tools/packet.mjs
  factory/tools/sizeBudget.mjs
  factory/tools/spark.mjs
  factory/landing-checks.json
  docs/log/f33.md
  Changed set equals this list; a stray file is a stop. budgets.json, sparks.json, kitCheck.mjs are NOT yours.

Must stay true
  No assertion may be satisfied by construction. Twice this lane has shipped a property that holds only in the degenerate case. For every assertion you write or keep, name the mutation that makes it fail, and run it.
  Nothing verified good regresses: checkErrors byte-identical, the two gate steps failing independently both ways, the band failing only over ceiling, coverage intact.
  A false sentence in the diff is a blocking defect. Three of the six items are false sentences. Add none.
  docs/log/f33.md is at 99.0 per cent of its 8 KB cap and must not cross: item 1 should delete more prose than item 6 adds; if not, compress without weakening a claim. No copied numbers (T02).

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/sizeBudget.mjs
  node factory/tools/sizeBudget.mjs --self-test
  node factory/tools/spark.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed (T56), one revert proof per item-3 assertion, and the item-4 proof. The gate is 21 steps, two red (Size budgets, Writer Action), both red at base, neither file yours. Expected; do not chase it.

Look hardest at
  An assertion that cannot fail: the finding of two consecutive reviews here. Before writing a check, write down the change that breaks it, make it, watch it go red. Fixing a comment without the code, or the code without the comment. Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 45 — six bounded items, mostly deletion. USD 0.15, then stop.
  The correction is spent: a return that misses these items gets no further envelope on this tier.

Fuse
  Split on envelope done, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record what it changed. If it found nothing, say that.

Return
  Append to docs/log/f33.md: what each item changed and the command that measured it. No commit id.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
