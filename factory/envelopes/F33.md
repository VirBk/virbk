ENVELOPE: F33 — correction 1, the only one
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Fix the defects an independent review found in commit 860237e. Same Holds, same lane. The split, the band and the rotation are sound and reviewed; do not redesign them.

Acceptance
  1. BLOCKING. spark.mjs's reconstruction assertion is satisfied by construction. rotate partitions closed from open without carrying position, so archive-then-live equals the original list ONLY when every closed row precedes every open one — and the fixture is built exactly that way. The comment stating it as a general property is a false sentence in the diff. Demonstrated: close S14 leaving S13, S15, S16 open, rotate, and the rebuilt order is S12 S14 S13 S15 S16 against an original S12 S13 S14 S15 S16. Fix BOTH halves: interleave an open row before a closed one so the assertion is falsifiable, then either make rotate carry position, or narrow the claim and the comment to what is true. Either way the fixture must fail the old code and pass the new (T13).
  2. packet.mjs argument order. rootFrom scans all of argv but cmd is argv[2], so `--root <tree> --check` falls to the usage branch and exits 1, which a caller cannot tell from a failed check. Parse the flag out before dispatch so either order works, with a self-test assertion on both.
  3. Nothing pins the step you added. kitCheck rule G asks only that SOME gate step names each tool, which the --self-test step satisfies alone, so deleting the --check step from landing-checks.json reopens this lane's hole silently. kitCheck.mjs is not yours: assert it from packet.mjs --self-test, that landing-checks.json names a step whose run is `node factory/tools/packet.mjs --check`. Prove it fails when that step is removed from a fixture.
  4. sizeBudget.mjs line ~111. A row with a missing cap throws on capKb.toFixed(1) in line() before overCap can route it, so the failure the comment promises never prints. Exit stays non-zero, so it is a wrong message, not a silent pass. A child copying the kit with an older budgets.json reaches it. Print the row and fail with the named message.
  5. docs/log/f33.md. "These rows are the run before this log existed" is false on its own evidence: the file existed at the base and the pasted block already counts 59 budgets. The Self-check clause asks what the self-check changed and no sentence answers it. Fix both, keep the rest — the 21-steps finding and the two-red analysis were correct and are confirmed.

Base
  0295f0bd167b6c6b209ef87b2c309eee942f399b
  Your worktree is on writer/F33 at 860237e. Rebase onto that base first, then correct. The file sets are disjoint so the rebase is clean; prove it, do not assume it. Push the branch only.

Holds
  factory/tools/packet.mjs
  factory/tools/sizeBudget.mjs
  factory/tools/spark.mjs
  factory/landing-checks.json
  docs/log/f33.md
  Unchanged from the first envelope. budgets.json, sparks.json and kitCheck.mjs are NOT yours. The docs/sparks glob and the stale command in CONTROL_PLANE.md are the control plane's and already recorded.

Must stay true
  Nothing the review confirmed regresses: checkErrors stays byte-identical, the two gate steps keep failing independently, the revert probes keep failing, coverage does not drop. No assertion may be satisfied by construction: if a fixture cannot express the failure, the fixture is wrong, not the claim.
  No copied numbers: every count is pasted from the command that produced it (T02).
  A false sentence in the diff is a blocking defect. Two of the five items are false sentences; do not add more.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/packet.mjs --root <fixture> --check   (both orders)
  node factory/tools/sizeBudget.mjs
  node factory/tools/sizeBudget.mjs --self-test
  node factory/tools/spark.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed (T56). For item 1, paste the interleaved fixture failing the OLD behaviour and passing the new. The gate is 21 steps with two red, Size budgets and Writer Action, both red at base and neither file yours. Expected; do not chase it.

Look hardest at
  An assertion that cannot fail. That is the whole of this correction, and the first envelope warned in these words: a rotate that loses a row, the reconstruction check is the point, not decoration. It was decoration.
  Fixing the comment instead of the code, or the code instead of the comment. Item 1 needs both halves.
  A value read from memory rather than the file that owns it (T04).
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak.
  Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). The key is read from its store in the same act as the launch, not from an open shell (S16). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 50 — five bounded fixes in files you already know.
  USD 0.15, then stop.
  This is correction 1 and the cap at sweep tier is one. A second correction is not issued: the lane splits to a fresh seat.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope — never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record what it changed. That sentence is item 5.

Return
  Append to docs/log/f33.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
