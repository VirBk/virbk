ENVELOPE: F48 — CORRECTION 1 — A citation check tells a rev spec, a URL and a tree path apart (S25)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Where this lane stands
  Five of your six properties are APPROVED, and so is the design. The classifier is right, no git call was added, the check still runs from an archive with no repository, the changed file set is exactly the holds, the gate is 21 of 21, kit check is ok, and your pristine/final sha256 claim was re-run and matches. Your disclosure that P1's natural mutation reds H1, H2 and H3 together is kept and is the honest kind. Do not redo any of that. This is your ONE correction: sweep tier gets one, then the lane splits.

Blocking A — property 3 is not proved, and it is the one your envelope said was most likely to escape
  H3 expects the EMPTY list, and "no error" is also what you get when the token is never recognised as a citation at all. An independent review and the control plane found this separately. Driven at the objects, blob 60d9f34 restored after: inserting one line before the rest is sliced -
    if (tok.indexOf("/") !== -1 && tok.indexOf("/") < colon) continue;
  which is the natural bug of assuming a rev name holds no slash - makes writer/F45:docs/log/f45.md vanish from the extractor entirely, and node factory/tools/kitCheck.mjs --self-test still prints kitCheck self-test ok at exit 0. H3 cannot tell "classified and resolved" from "never seen".
  The remedy is the one your own file already uses for the other three: the held / wantHeld contract pins the exact classification for HEAD:factory/tools/packet.mjs, for a plain path and for a URL, which is why H1, H4 and H5 survive a discard. It carries NO slash-bearing rev. Extend it so the extractor's output for a rev whose name holds a slash is pinned by kind, token and path, and prove the discard mutation above now reds it.
  Then ask the same question of every remaining fixture whose expected value is the empty list, and say in the log which ones you checked and what you found. A fixture that expects nothing must still be able to fail.

Blocking B — a count in the log is not what the command prints
  docs/log/f48.md line 25 says "size budget ok — none over cap, 19 in the band". At your commit the command prints
    size budget ok — none over cap, 21 in the band:
  Re-run it and paste what it printed. AGENTS section 4: a count is pasted from the command that produced it, with the command, or omitted. This is the fifth uncommanded count in two sittings, and your own envelope calls a false sentence in the diff a blocking defect. While you are there, re-run every other number in that log and correct any that has drifted, rather than only this one.

Base
  Your worktree has been rebased by the control plane onto live main, which has moved: F47 landed. Read the first line of this file before you start: if it does not say CORRECTION 1, stop and say so (T81).

Holds
  factory/tools/kitCheck.mjs
  docs/log/f48.md
  Changed file set equals this list. A stray file is a stop.

Pushing after a correction
  The rebase has diverged your branch from the tip you pushed. You are authorised to run
    git push --force-with-lease origin writer/F48
  on YOUR LANE BRANCH ONLY. main is never forced and no other branch is yours to rewrite. If force-with-lease is itself rejected, stop and return: that means something else moved your branch.

Must stay true
  The five approved properties stay green. Do not weaken a fixture to make room.
  Rule H still scans docs/log/*.md and nothing else.
  The check still runs with no repository present and calls no git.
  kitCheck --self-test prints ok at exit 0, kitCheck prints kit check ok, and the gate is 21 of 21 at your commit.
  docs/log/f48.md stays under its 8 KB cap; it is already in the band, so correct numbers rather than adding prose.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/kitCheck.mjs --self-test
  node factory/tools/kitCheck.mjs
  node factory/tools/sizeBudget.mjs
  node factory/tools/landingGate.mjs
  The discard mutation above, pasted going red against your new assertion, and pasted green again after restore.

Look hardest at
  Pinning the classification so tightly that the fixture asserts the implementation rather than the property. What must be true is kind, token and path; the shape of the object is not the property.
  Any other fixture that expects the empty list. That is where this defect lives, and it has now cost this lane its one correction.
  If the gate reds ALL 21 steps at any commit, your bash is the WSL launcher and the red is not yours (T70, rewritten when F47 landed). Say so in the log, use git-bash, do not spend calls proving it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate).
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  25 tool calls for two corrections in two files. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). On a decay sign: stop, print the signs, return.

Self-check
  Before returning, ask of each fixture whether it COULD have failed, and prove it with the mutation you pasted. Record in docs/log/f48.md what the self-check changed.

Return
  A few lines added to docs/log/f48.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
