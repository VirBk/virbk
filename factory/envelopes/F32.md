ENVELOPE: F32 — The control plane reads the meter into the return
Authorization: issued by the control plane on the owner's word. Read AGENTS.md, BOARD.md, factory/traps.yaml once, then this envelope. Nothing else is required reading.

Work / acceptance
  A return can carry measured token counts that came from a meter, so factory/returns.json can hold a true row.
  1. Add to factory/tools/seatReturn.mjs a read-only command: node factory/tools/seatReturn.mjs meter <project-path>. It reads the newest session in the qwen usage record whose project equals <project-path>, and prints requests, inputTokens, outputTokens, cachedTokens and the absolute path of the file it read. The usage record is at $USERPROFILE/.qwen/usage_record.jsonl (HOME on POSIX); one JSON object per line, models keyed by model id. If the file or a matching session is absent, print that and exit 1. Never invent a number.
  2. Extend contracts/seat-return.v1.json with an optional object tokens: { in, out, cached, requests, source }, all non-negative numbers except source, a non-empty string naming the meter the numbers were pasted from.
  3. Make spendUsd optional exactly when tokens.source is present, and required otherwise. A meter that reports tokens and no cost cannot produce a truthful dollar, and a zero would be a durable false measurement in the store.
  4. --check-contract and --self-test still pass, and the self-test proves all three of: a return with neither spendUsd nor tokens is refused; a return with tokens and no spendUsd is accepted; a recorded row round-trips through spend.
  Acceptance: node factory/tools/seatReturn.mjs --self-test passes, and node factory/tools/seatReturn.mjs meter <this worktree> prints this lane's own numbers.

Base / branch
  da0d13a512954d1c30af4c2f3eef7fa774ced73e from git ls-remote origin refs/heads/main.
  The main records commit that carries this envelope adds this file and F32 board state only. Use the supplied isolated worktree of that issuance commit, branch writer/F32. Read live main again before push; if it moved, stop and report. Push writer/F32 only, never main.

Holds
  factory/tools/seatReturn.mjs
  contracts/seat-return.v1.json
  docs/log/f32.md
  Changed file set equals these three paths. A stray file is a stop. No .bak, no scratch file, no return.json left in the tree. Do not edit factory/returns.json: the store gains rows only through --record, and this lane records nothing.

Must stay true
  Every existing field, validation and self-test assertion in seatReturn.mjs survives. This adds; it does not replace.
  The contract runtime enum stays the live topology catalog, still checked by --check-contract.
  meter is read-only. It opens the usage record and nothing else, writes no file, and calls no network.
  No price table, no USD conversion, no token cap, no threshold. A price is a dated row in factory/decisions.json and is not this lane's to invent.
  A row already keyed by its content stays refused on a second --record.
  A false sentence in the diff or in the lane log is a blocking defect.

Verification
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/seatReturn.mjs --check-contract
  node factory/tools/seatReturn.mjs meter <the absolute path of this worktree>
  node factory/tools/seatReturn.mjs spend
  node factory/tools/kitCheck.mjs --self-test
  node factory/tools/sizeBudget.mjs
  git diff --check
  Paste what each printed, from the command that produced it.
  Negative proof: build a return with neither spendUsd nor tokens and show it refused; then the same return with a tokens block and show it accepted. Paste both.

Look hardest at
  A self-test that passes whether or not the optional-when rule works. Prove both directions.
  The usage record is append-only and holds every project. Match the project exactly; do not take the last line blindly.
  $USERPROFILE is Windows; HOME is POSIX. The gate runs on both.
  A meter reading is a paste, not a computation. If the record has no cost field, the lane does not produce one.
  Do not read factory/returns.json, factory/experiments.json or the ledger beyond what the commands above print.

Runtime
  pc-dashscope shape from factory/project.json: qwen-code, worktree, ff-only, owner-account, provider-prefix, helpMode=none.
  The launcher passes --model deepseek-flash against https://api.deepseek.com. Record that exact string as model in your return. Do not copy the topology id into the model field: a seat that echoes its stamp instead of its launcher is the open spark S10.
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Token in the process environment only. Never print a key.

Spend cap / fuse
  USD 0.10, 12 session turns, 25 tool calls, then stop and return. Hitting the cap is a return, not a hang.
  Sweep tier: one correction, then split. Stop on a named decay sign in factory/sessions.json.

Self-check
  Before returning, drive the list a reviewer of this lane would be given and fix what you find. Record in docs/log/f32.md what the self-check changed.

Return
  Brief docs/log/f32.md: what changed, each measuring command and what it printed, both halves of the negative proof, what the self-check changed, and the model string the launcher passed.
  Also print a JSON object matching the amended contract, with lane F32, branch writer/F32, and a tokens block read from the meter command.
  Commit subject: F32: the control plane reads the meter into the return.
  Push writer/F32. Never review or land yourself. Never touch main.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
