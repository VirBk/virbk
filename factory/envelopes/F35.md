ENVELOPE: F35 — the alumni scan reads a child's TRAPS
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make the scan open the file it already knows is there, and turn its rows into intakes the gate can judge.

Sibling
  F38 runs at the same time. node factory/tools/seat.mjs disjoint F35 F38 printed "2 and 2 holds, none shared". Do not touch anything outside your Holds; that proof is what lets you both run.

How this envelope is written
  D-59: this tier ships checks that test a different object than the one that runs. Every item NAMES THE MUTATION that must turn it red, and your return pastes it going red. Make the mutation faithful — one that breaks syntax or crashes the tool proves nothing.

Acceptance
  1. scan already sees a child's TRAPS file and never opens it. The A-TRAPS signal matches the path and reports structure, so a lesson a child wrote in prose reaches the parent only when a person reads it (T71). scan parses the rows of a child's TRAPS.md, TRAPS.yaml or TRAPS.yml and emits one candidate intake per row, in the shape contracts/intake.v1.json already validates. MUTATION: make the parser return no rows for a TRAPS file that has some — red.
  2. An intake without a check is refused (T24), and a child's prose row often names none. Split the two cases and never blur them. A row that names a check becomes a candidate intake carrying that check. A row that names none is COUNTED and REPORTED by id, and is NOT emitted as an intake, because the gate would refuse it and a refused intake is noise the owner learns to skip. The scan prints both counts. MUTATION: emit a checkless row as an intake — red, and say which check catches it.
  3. Every emitted candidate validates against contracts/intake.v1.json with no hand-editing. It carries the child it came from and enough provenance to find the row again in that child's file. If the contract needs a field for that, add it and say why in the log; do not invent a field the contract does not carry and do not loosen an existing one. MUTATION: emit a candidate missing a required field — red.
  4. scan stays read-only on the scanned tree. It opens a child's TRAPS file for reading and writes nothing there, ever. MUTATION: have scan write a file into the scanned tree — red.
  5. Drive it against a real tree, not only a fixture: factory/drops/ and this repository's own factory/traps.yaml are available to build a realistic TRAPS file from. Paste the counts the command prints.

Base
  ecf24e84a70efa0d67f94dd3429a37542202b38b
  Worktree from that base. Rebase onto live main before push and prove it with git range-diff. Push the branch writer/F35 only.

Holds
  factory/tools/alumni.mjs
  contracts/intake.v1.json
  docs/log/f35.md
  Changed set equals this list; a stray file is a stop. factory/traps.yaml, factory/lineage.json and factory/drops/ are NOT yours. Your sibling holds seatReturn.mjs and contracts/seat-return.v1.json — do not read them for ideas and do not touch them.

Must stay true
  An assertion tests what ships, not a plan or a branch no fixture reaches (D-59). For every check you write, name the mutation that breaks it, make it, watch it go red.
  A child never writes the parent kit and the parent never writes a child (T23). Read-only means read-only.
  An intake without a check is refused. This lane must not make the gate more permissive to raise its own yield.
  A contract that lists ids by hand is checked against the catalog that owns them (T64).
  No copied numbers (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/alumni.mjs --self-test
  node factory/tools/alumni.mjs scan <a tree you built>
  node factory/tools/intake.mjs on an emitted candidate
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and the four mutations going red. The gate is 21 of 21 green even with an envelope live; if a step is red, say which and why rather than working around it.

Look hardest at
  A parser that agrees with the fixture you wrote for it. Build one TRAPS file from this repository's own traps.yaml rows, which you did not write for this purpose, and parse that too.
  Blurring item 2. A candidate that carries an empty check is worse than one that is not emitted, because the gate refuses it after the owner has read it.
  Keystone: NONE — parent flywheel maintenance.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 60. USD 0.20, then stop.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f35.md what it changed. If it found nothing, say that.

Return
  docs/log/f35.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
