ENVELOPE: F35 — correction 1, the only one
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  One regex decides whether a child's row becomes a candidate or is held, and it picks the wrong match. Fix it, and stop the module running its CLI when something imports it.

What is NOT wrong
  Reviewed and confirmed at the objects: 75 rows read from this repository's own traps.yaml with 19 named and 56 held, all 19 passing intake.mjs unedited, read-only proved by a sha1 of the drive tree before and after a real run, the too-short check and the empty check each held for the right stated reason, and contracts/intake.v1.json correctly left alone because intake.mjs hardcodes its own key list and would refuse a new field. Do not redesign any of it.

Acceptance
  1. BLOCKING. splitCheck at alumni.mjs takes the FIRST case-insensitive match of "check is" or "check:", so ordinary prose beats the real directive. Driven at the objects on a row reading "Confirm the box. check is ticked before merge, always. Check is node factory/tools/kitCheck.mjs --self-test.":
       rule  -> "Confirm the box Confirm the box"
       check -> "ticked before merge, always. Check is node factory/tools/kitCheck.mjs --self-test"
     That candidate clears the twelve-character floor and passes node factory/tools/intake.mjs unedited, which is worse than being held: the owner acts on it. The same root cause has an inverse — a false hit early in a row can push the rule under its floor and a row with a perfectly good check is held as if it had none, indistinguishable in the printed list from a genuinely checkless row.
     Fix both halves of the match. Match "Check" case-sensitively, because the directive is written that way and incidental prose is not. And when a row carries more than one candidate directive, the LAST wins, because the directive ends a row by convention and prose precedes it.
     Two fixtures, and neither may be one you would write only to pass: a row with incidental "check is" prose BEFORE the real directive, and a row where the false hit would drop the rule under its floor. MUTATION: restore the case-insensitive first-match behaviour — red on both.
  2. Importing alumni.mjs runs its CLI. `node -e 'import("./factory/tools/alumni.mjs")'` prints the whole check pack, because the dispatch is at module top level and argv[2] defaults to "checks". Nothing imports it today, so this is latent, not live — and it is the same hazard F36 fixed in packet.mjs with an entry-point guard, which is T14: a gate step that cannot fail because importing it ended the process. Guard the dispatch the way packet.mjs does. MUTATION: remove the guard — red.
  3. docs/log/f35.md records what items 1 and 2 changed and the command that measured each. Its existing note that T71's rule text is now false stays: that is the control plane's fix at landing, not yours.

Base
  956085f4cda287cbd973fee53054f1640fcbaa5f
  Your worktree is on writer/F35 at 3c1dd95, cut before live main moved. Rebase onto that base first — the moved files are disjoint from yours — then correct. Prove the rebase with git range-diff. Push the branch only.

Holds
  factory/tools/alumni.mjs
  docs/log/f35.md
  Narrower than the first envelope: contracts/intake.v1.json is finished and is NOT yours. factory/traps.yaml is not yours either.

Must stay true
  An assertion tests what ships, not a plan or a branch no fixture reaches (D-59).
  A candidate carrying a junk check is worse than a row held, because the gate accepts it. When the two cases are in doubt, hold.
  The 75/19/56 numbers on this repository's own traps.yaml do not move unless you can say which row changed side and why.
  scan stays read-only on the scanned tree.
  No copied numbers (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/alumni.mjs --self-test
  node factory/tools/alumni.mjs intakes <a tree carrying the two fixtures>
  node factory/tools/intake.mjs on an emitted candidate
  node -e 'import("./factory/tools/alumni.mjs")'
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and both mutations going red.

Look hardest at
  A fixture built from the bug you just read. Write the two rows first, watch them fail on the current code, then fix it.
  Making the match so strict that a real child's directive stops being found. Say in the log which spellings you accept and which you refuse, and why refusing is the safe way to be wrong.
  Keystone: NONE — parent flywheel maintenance.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 40. USD 0.15, then stop.
  This is correction 1 and at sweep tier it is the only one.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record it in the log.

Return
  Append to docs/log/f35.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
