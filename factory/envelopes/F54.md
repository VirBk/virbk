ENVELOPE: F54 — CORRECTION 1 — a date UTC has not reached, and a sentence with three false clauses
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Correct the date the tool prints and the log's opening claim, and stop a copied guard from reading as a second proof.

Acceptance
  node factory/tools/seat.mjs --self-test (step 09) prints ok at exit 0, step 20
  stays ok, and the gate passes 22 of 22.
  ALL SEVEN PROPERTIES ARE PROVED and are not reopened: the review drove its
  own mutation for each and every one red, and P2, P3 and P6 survived the
  vacuity traps it built for them. This is what it found after that.

  C1  BLOCKING. The lane wrote 2026-09-23 in three places while date -u was
      2026-09-22 - the LOCAL date at +0800. AGENTS says dates come from
      date -u, and F41 exists for this. It is worse than a clock error: D-72's
      own at field is 2026-09-22, so the shipped tool prints on every run
        baseline 7292 bytes over 5 files (D-72, 2026-09-23)
      a citation that contradicts the row it cites (T76, rule H's family).
      Fix all three to 2026-09-22: seat.mjs:69 the comment, seat.mjs:75
      STORE_BASELINE.at, docs/log/f54.md:21. kitCheck rule I did not catch it
      because its scope is decisions rows and ledger rows - that gap is S31 and
      is NOT yours to close.
  C2  BLOCKING. One sentence in docs/log/f54.md carries three false clauses:
      "Every rendered recipe opens with the harness's non-repository context -
      eight comment lines ending #   node factory/tools/seat.mjs store."
      Measured: TEN of nineteen recipes carry it, not every one - goose,
      deepseek-openai, claude-code, git-bus, codespace, cursor-desktop,
      help-fork, help-collab and desktop-deepseek do not. None OPENS with it:
      in qwen-code the block starts at line 22 of 35. And storeStdin() returns
      SEVEN elements, not eight. Write what the commands print. Those nine
      recipes hand over no packet, so their omission is correct and the
      sentence should say so rather than claim a coverage it does not have.
  C3  The second guard at seat.mjs:826 is the SAME condition as the carrier
      guard at :795 with one word changed in its message. It cannot fire
      unless the older one fires too, so it adds no independent coverage for
      the store rule while reading like a second proof. Either make it
      independent - assert the store line over the packet-carrying set,
      computed separately - or delete it and say in the log that one guard
      covers both. A guard that cannot fail alone is D-61's shape.
  C4  The log says storeStdin() being called inside launchStdin() means "a new
      recipe cannot silently omit them". False, and the nine recipes in C2 are
      the proof: a recipe that calls neither launchStdin() nor packet.mjs
      omits both and nothing reds. Say what is actually guaranteed.
  C5  The empty-store branch - directory present, zero files - is reachable and
      undriven. Drive it. It is one line from the absent case and the two must
      stay distinguishable, which is the whole of P2.

Correction
    Pushing after a correction
      The rebase has diverged your branch from the tip you pushed. You are
      authorised to run
        git push --force-with-lease origin writer/F54
      on YOUR LANE BRANCH ONLY. main is never forced and no other branch is
      yours to rewrite. If force-with-lease is itself rejected, stop and
      return: that means something else moved your branch.

Base
  The commit CARRYING this envelope on live main; your worktree is rebased onto
  it before you start (T81). Rebase onto live main before push.

Holds
  factory/tools/seat.mjs
  docs/log/f54.md
  Changed file set equals this list. seatReturn.mjs and the contract are DONE;
  a diff against either is a stray file and a stop.

Must stay true
  - Every property stays proved and both steps stay ok.
  - Still no store CONTENT is read, copied or printed: name, size, count only.
  - The drift line still REPORTS and never gates (T82).
  - A false sentence in the diff is a blocking defect. This correction exists
    because four shipped.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/landingGate.mjs
  node factory/tools/seat.mjs store
  Paste the mutation, command and failing line for C3 and C5. For C1 and C2
  paste the command whose output you wrote the sentence from.

Look hardest at
  Writing a count you did not just measure. C2 is three wrong numbers in one
  sentence and C1 is a date read off the wrong clock; both are T04 - read the
  value in the same act as the write. Run the command, paste what it printed.
  Do not commit before running the gate: it archives the COMMIT, so a gate run
  on uncommitted work reports the previous tip. You hit that once already.

Runtime
  desktop-qwen. Writer seat only. Worktree only. Do not remap the control plane.

Spend cap
  Tool calls: 25. A cap is a return, not a hang.

Self-check
  Before returning, drive the list a reviewer would be given and fix what you find.

Return
  Append to docs/log/f54.md: what changed, the measuring command, no commit id.
