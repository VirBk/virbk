ENVELOPE: F54 — What reaches a seat, and what a seat costs, are both in the record (D-72)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make the launch name the non-repository context that reaches a seat, and make a return carry what its review cost.

Acceptance
  node factory/tools/seat.mjs --self-test       (gate step 09) and
  node factory/tools/seatReturn.mjs --self-test (gate step 20)
  both print ok at exit 0, and each property reds its named step under its own
  mutation. Read D-72 before you start; it is the ruling this implements.

  P1  The launch NAMES the non-repository context that will reach the seat:
      the store path, the file count, the total bytes, and each file with its
      own size. Measured before issue: ~/.qwen/memories is 7292 bytes over
      five files, and the qwen harness injects it ahead of the packet. It is
      in no packet, under no budget, in no gate, and a successor cannot
      discover it from the repository at all - that, not the cache, is why
      this lane exists.
  P2  The ABSENT case is a named zero, not silence. A store that is not there
      must print that it looked and found none. A silent pass cannot be told
      from a check that never ran (F49), and the absent case is the one that
      runs on every machine that is not this one.
  P3  The numbers are read from the store in the same act as the report (T04),
      never carried. Mutation: hardcode the count or the byte total. Step 09
      reds. This is the property the lane will lie about: a report that agrees
      with a constant agrees with its own bug (D-59).
  P4  Growth is VISIBLE, and visibility is all it buys. The store was 5718
      bytes over four files when sitting 13 opened and 7292 over five by
      02:38, because the F52 seat wrote to it mid-lane. Report the total
      against a stated number so a successor sees drift. Do NOT make this a
      new way to fail the gate - T82 says a band that only reports says so in
      those words rather than implying a check that is not there.
  P5  A return carries a REVIEW row: the reviewer's tier, its tokens and its
      tool uses. Measured this sitting: 173651 subagent tokens over 67 tool
      uses on F52, about 2 per cent of that lane's input tokens, recorded
      nowhere the session would outlive. contracts/seat-return.v1.json gains
      the field; seatReturn.mjs reads and validates it.
  P6  A review row present but empty is a NAMED REFUSAL, not a pass. A lane
      that was never reviewed and a lane whose review was not counted must be
      distinguishable in the record, or the field buys nothing. Mutation: make
      the empty row pass. Step 20 reds.
  P7  seatReturn.mjs gains the entry guard packet.mjs, route.mjs, alumni.mjs
      and envelopeCheck.mjs already carry, so importing an export no longer
      runs the CLI and exits. This blocked the control plane's own
      verification of F52; F51 did exactly this for envelopeCheck.mjs and is
      the pattern to copy. Mutation: remove it; an importing fixture is killed
      by process.exit.

Base
  The commit CARRYING this envelope on live main: a seat reads its envelope
  from its own worktree (T81). Rebase onto live main before push. Push the
  branch only.

Holds
  factory/tools/seat.mjs
  factory/tools/seatReturn.mjs
  contracts/seat-return.v1.json
  docs/log/f54.md
  Changed file set equals this list. A stray file is a stop. factory/tools/
  route.mjs is NOT yours; F53 is queued on it and on both of your tool holds,
  which is why F53 is not running beside you.

Must stay true
  - THIS LANE BANS NOTHING AND DELETES NOTHING. The store stays, the qwen-code
    harness stays, the reviewer stays. D-72 rules that the store is NAMED, not
    removed, because it demonstrably pays: the note it grew this sitting is an
    accurate lesson from a real incident.
  - Nothing reads, copies or prints the CONTENT of a store file: name, size and
    count only. A note may hold anything the owner typed.
  - The report must not break a launch on a machine that has no store, and must
    not need one to be tested.
  - A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/landingGate.mjs
  For each of P1-P7 paste the mutation, the command and the failing line.

Look hardest at
  D-69's question, which F52 answered well and is still the standard here:
  WHICH OBJECT DOES THIS MUTATION EDIT, AND IS IT THE OBJECT THE GATE INVOKES.
  Red is read from the exact command step 09 or 20 runs; a red from a fixture,
  a helper or a stripped copy proves nothing.
  P2 and P6 are the two that will ship untested, because both are the case
  where nothing is wrong and the honest answer is a printed zero. F52's own
  review found exactly this shape twice.
  A tool-policy refusal is not a reason this envelope waits.

Runtime
  desktop-qwen. Writer seat only. Do not remap the control-plane session.
  Worktree only. Hands: node factory/tools/hands.mjs, never a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first; variable
  work last. Paste prompt_cache_hit_tokens or cached_tokens.

Spend cap
  Tool calls: 45 — seven properties, three files. A cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope —
  never a transcript, never a compaction.

Self-check
  Before returning, drive the list a reviewer would be given and fix what you find.

Return
  A few lines in docs/log/f54.md: what changed, the measuring command, what the
  self-check changed, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
