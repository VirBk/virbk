ENVELOPE: F38 — correction 1, the only one
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Test the path the control plane will actually run, pin the whole contract instead of a quarter of it, and delete a citation to something that does not exist.

What is NOT wrong
  Reviewed and confirmed at the objects: sameModel is symmetric and transitive because it is equality over a canonical form; the deepseek-v4-pro fold is deliberate and matches the id map, and the router never sends the bare unversioned id to DashScope, so the T48 price collision is not reachable through code you hold. An absent, empty or null model is refused before record() is reachable. Recording twice refuses with "already recorded" and the key is content-derived. factory/returns.json is untouched and its one row still validates. Do not redesign any of it.

Acceptance
  1. BLOCKING. The CLI --record path is the ONLY production caller of record(), and it is dead code to every fixture. Driven at the objects: replacing `const out = record(storeRoot(), row, project);` with an object that says ok leaves
       node factory/tools/seatReturn.mjs --self-test      -> seatReturn self-test ok
       node factory/tools/seatReturn.mjs --check-contract  -> seat-return contract ok
     The whole meter check this lane exists to add can be removed and nothing notices. The self-test pins record() by calling it with an explicit meterFile the CLI never passes — a function tested thoroughly while the invocation that ships is unreached. This is the fifth lane running to ship this shape and the second in exactly this form, after F39's spawnSync. Drive the real CLI from the self-test: a return that agrees records, one that disagrees is refused, and a second record of the same return is refused, all through the command a control plane types. MUTATION: bypass record() in the CLI block as above — red.
  2. BLOCKING, a false sentence in the diff. docs/log/f38.md says the factory's record shows the unversioned id from the native base at `grok-f31-rev`, a different price. That ref does not exist: `git rev-parse --verify grok-f31-rev` fails and the string appears nowhere in the repository but that line. factory/returns.json holds one row, F32 on deepseek-flash, with no deepseek-v4-pro and no native-base price. The CLAIM is true and derivable by reading providerModelFor in route.mjs; the citation was invented. Delete it and derive the claim from the code, naming the function. Cite nothing you have not resolved.
  3. schemaErrors pins two of eight required fields. It asserts that required contains "measured" and excludes meterModel and spendUsd, and never checks lane, runtime, model, base, branch, changed or verify — any of the seven can be dropped from required and both commands stay green. Pin the full set. The fixture already had the shipped contract in hand and could have done this at no extra cost. MUTATION: drop each of the eight from required in turn — red each time.

Base
  23282d64e1fcdc16a301051cdf3aa66a552bfb50
  Your worktree is on writer/F38 at 2fe0db0. Rebase onto that base, then correct. Prove the rebase with git range-diff. Push the branch only.

Holds
  factory/tools/seatReturn.mjs
  contracts/seat-return.v1.json
  docs/log/f38.md
  Changed set equals this list; a stray file is a stop. factory/returns.json stays read-only and is not rewritten.

Must stay true
  An assertion tests what ships. A function pinned by a fixture that passes arguments the real caller never passes is not tested, it is described (D-59).
  Cite nothing you have not resolved. If a claim needs evidence you cannot produce, derive it from code you can name, or drop the claim.
  Everything the review cleared survives: the canonicalisation, the refusals before record(), T65's read-first key, the untouched store.
  No copied numbers (T02). A false sentence in the diff is a blocking defect; item 2 is one.

Verification
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/seatReturn.mjs --check-contract
  node factory/tools/seatReturn.mjs meter <a project path>
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and every mutation in items 1 and 3 going red.

Look hardest at
  A CLI test that builds its own arguments and calls the same function the old fixture called. Drive the entry point the way a control plane does, with the environment it will have.
  Replacing the invented citation with another claim you cannot resolve.
  Keystone: Dual-runtime closed loop — this lane is the spend-in-the-lane-log step, and item 1 is the difference between that step being checked and being described.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 45. USD 0.15, then stop.
  This is correction 1 and at sweep tier it is the only one.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record it in the log.

Return
  Append to docs/log/f38.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
