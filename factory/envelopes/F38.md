ENVELOPE: F38 — a return's model is checked against the meter
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Stop believing a seat about which model wrote the lane. Check the return's model against the harness meter, and refuse a return that disagrees with it.

Sibling
  F35 runs at the same time. node factory/tools/seat.mjs disjoint F35 F38 printed "2 and 2 holds, none shared". Do not touch anything outside your Holds; that proof is what lets you both run.

How this envelope is written
  D-59: this tier ships checks that test a different object than the one that runs. Every item NAMES THE MUTATION that must turn it red, and your return pastes it going red. Make the mutation faithful — one that breaks syntax or crashes the tool proves nothing.

Acceptance
  1. A seat cannot see its own model and echoes the envelope's Runtime stamp instead; the harness meter records the id that actually answered (T69). `meter <project-path>` already reads it. Make recording a return CHECK the return's model against the meter's model for that project: agreement records, disagreement refuses and names both ids. MUTATION: record a return whose model disagrees with the meter — red, naming both.
  2. A missing meter is not a passing check and not a crash. When no usage record exists for that project, recording says so and refuses, because a claim nobody measured is the thing this lane exists to stop. It never silently records an unchecked model. MUTATION: point recording at a project with no meter row and have it record anyway — red.
  3. The meter reports a provider id and a return may carry the name the owner picked. deepseek-flash and deepseek-v4.1-flash are the same model at two vendors, and an unversioned DashScope id is a different price (T48). Compare through the map that already owns that relation rather than by string equality, and refuse only a genuine disagreement. Say in the log which pairs you treat as agreeing and why. MUTATION: compare by raw string equality — red on a pair that legitimately agrees.
  4. contracts/seat-return.v1.json describes a return. If it needs a field to carry the meter's id alongside the launcher's, add it and say why; do not loosen an existing constraint to make a disagreement pass. The contract's id list is checked against the catalog that owns it (T64), and that check stays green. MUTATION: loosen a required field — red.
  5. factory/returns.json holds one row today and it is the only real measurement this factory has. Its existing row keeps validating and is not rewritten. MUTATION: change the stored row's shape — red.

Base
  ecf24e84a70efa0d67f94dd3429a37542202b38b
  Worktree from that base. Rebase onto live main before push and prove it with git range-diff. Push the branch writer/F38 only.

Holds
  factory/tools/seatReturn.mjs
  contracts/seat-return.v1.json
  docs/log/f38.md
  Changed set equals this list; a stray file is a stop. factory/returns.json is NOT yours — you may read it and must not rewrite it. factory/project.json and factory/runtimes.json are not yours either. Your sibling holds alumni.mjs and contracts/intake.v1.json — do not read them for ideas and do not touch them.

Must stay true
  An assertion tests what ships, not a plan or a branch no fixture reaches (D-59). For every check you write, name the mutation that breaks it, make it, watch it go red.
  The meter is read, never written (T67). seatReturn does not call a model and does not launch anything.
  A number is pasted from the command that produced it, with the command, or omitted (T02). This lane is about exactly that discipline, so it may not break it.
  Recording twice must not double a row; the key is the row's content, checked read-first (T65). That behaviour stays.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/seatReturn.mjs --check-contract
  node factory/tools/seatReturn.mjs meter <a project path>
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and the five mutations going red. The gate is 21 of 21 green even with an envelope live; if a step is red, say which and why rather than working around it.

Look hardest at
  A check that compares two fields you both wrote in the same act. The meter's id comes from the harness record on disk; the return's comes from the launcher. If your fixture produces both from one source, you have tested nothing (T04).
  Making a disagreement pass to get a green. A refused return is the correct outcome and the point of the lane.
  Keystone: Dual-runtime closed loop — this lane is the "spend pasted in the lane log" step, and it makes that step checkable rather than trusted.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 60. USD 0.20, then stop.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f38.md what it changed. If it found nothing, say that.

Return
  docs/log/f38.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
