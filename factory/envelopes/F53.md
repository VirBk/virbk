ENVELOPE: F53 — A check names the surfaces it scanned, and a fold is proved where it is consumed
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Move four assertions from the helper they sit on to the object that ships, and make the template carry the field D-72 created.

Acceptance
  node factory/tools/route.mjs --self-test  (step 08),
  node factory/tools/seat.mjs --self-test   (step 09) and
  node factory/tools/seatReturn.mjs --self-test (step 20)
  all print ok at exit 0, and each property reds its named step under its own
  mutation. Every item below was re-verified at the objects at this base, not
  copied from the review that found them.

  P1  THE FOLD IS PROVED WHERE IT IS CONSUMED. sameModel is pinned by one
      assertion, but record() - exported, shipped - does
      meter.ids.find((id) => sameModel(id, row.model)) and NOTHING drives it.
      Measured at this base: restoring the old fold reds exactly one clause,
      "two different models folded as one", and no fixture. So a return
      claiming an unknown model could be accepted against a meter naming a
      DIFFERENT unknown model with the suite green. Drive record() itself with
      two unrecognised ids. Mutation: restore the fold; your new fixture must
      red. Say in the log which clause is the consumption and which the helper.
  P2  THE GUARD MUST DETECT DUPLICATION, NOT ONLY DIVERGENCE. seat.mjs:761
      compares hostedModel(x) to HOSTED_IDS[x] || x - both sides from
      route.mjs, so it checks route against itself. A byte-identical map
      re-grown inside seat.mjs passes it; only a move in route's map reds. That
      is T75 able to return silently, in the file F52 removed it from. Assert
      seat.mjs holds no model map of its own. Mutation: re-grow a byte-identical
      local map and use it. Step 09 reds.
  P3  A CHECK NAMES THE SURFACES IT SCANNED. droppedAudit reports how many IDS
      it examined, not which OBJECTS, so emptying shippedSurfaces() and
      re-adding a dropped id leaves step 08 at ok - the audit says it examined
      six ids, truthfully, while scanning nothing. F49's empty-list lesson one
      level up: a count of inputs is not a count of surfaces. Report both, and
      fail on an empty surface list, naming it. Mutation: return [] from
      shippedSurfaces(). Step 08 reds.
  P4  SEAT EXERCISES BOTH ARMS OF THE T48 MAP. Its self-test drives only flash
      and passthrough; the pro arm went with the qwen row in F52 and lives in
      route.mjs alone. T48 is the trap that a native id must not reach
      DashScope, and seat.mjs prints the hosted recipe. Mutation: break the pro
      row in route's map. Step 09 reds.
  P5  THE TEMPLATE CARRIES THE REVIEW ROW. seatReturn.mjs --template emits no
      review field, so the thing a returner copies never mentions the row D-72
      requires and T84 checks: enabled but undiscoverable, the complaint D-72
      was written to answer. Add it with obvious placeholders. An unreviewed
      lane must still be able to omit it - the field stays optional and an
      EMPTY row stays a named refusal (T84). Mutation: drop it. Step 20 reds.

Base
  The commit CARRYING this envelope on live main; a seat reads its envelope
  from its own worktree (T81). Rebase onto live main before push. Push the
  branch only.

Holds
  factory/tools/seatReturn.mjs
  factory/tools/seat.mjs
  factory/tools/route.mjs
  docs/log/f53.md
  Changed file set equals this list. A stray file is a stop. Do not edit
  writer-paths.json, the contract or decisions.json; P5 adds a template row,
  not a contract change.

Must stay true
  - Everything F52 and F54 proved stays proved; all three steps stay ok.
  - The drift line still REPORTS and never gates (T82), and no store CONTENT is
    read or printed: name, size, count only (T83).
  - The qwen-code harness stays. D-68 dropped models, not the harness.
  - A false sentence in the diff is a blocking defect, and a COUNT you did not
    just measure is how the last two lanes each produced one. Run the command,
    paste what it printed (T04).

Verification
  node factory/tools/route.mjs --self-test
  node factory/tools/seat.mjs --self-test
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/landingGate.mjs
  For each of P1-P5 paste the mutation, the command and the failing line.

Look hardest at
  This lane IS D-69's question, so answering it loosely fails it outright:
  WHICH OBJECT DOES THIS MUTATION EDIT, AND IS IT THE OBJECT THE GATE INVOKES.
  Four of the five items exist because an assertion sat on a helper while
  nothing drove the shipped consumer.
  P1 will look done and not be: a fixture that calls sameModel again, under
  any name, is the defect restated - it must drive record() itself.
  COMMIT BEFORE RUNNING THE GATE. It archives the COMMIT, so a run over
  uncommitted work reports the previous tip; two seats and the control plane
  each hit that this sitting.

Runtime
  desktop-qwen. Writer seat only. Do not remap the control-plane session.
  Worktree only. Hands: node factory/tools/hands.mjs, never a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Paste
  prompt_cache_hit_tokens or cached_tokens.

Spend cap
  Tool calls: 35 — five properties, three files. A cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope.

Self-check
  Before returning, drive the list a reviewer would be given and fix what you find.

Return
  A few lines in docs/log/f53.md: what changed, the measuring command, what the
  self-check changed, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
