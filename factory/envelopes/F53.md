ENVELOPE: F53 — CORRECTION 1 — a check was swapped, not added, and the trade lost the real detector
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Put back the divergence clause P2 deleted, give the new scan a positive control, and make it name the limit it actually has.

Acceptance
  Steps 08, 09 and 20 all print ok at exit 0 and the gate passes 22 of 22.
  P1, P4 and P5 are PROVED and are not reopened - the review called them model
  answers to D-69, each driving the shipped consumer and each isolating. P3 is
  proved with one residual hole, C5. This is P2.

  C1  BLOCKING, AND IT IS A REGRESSION. P2 DELETED
      want("hosted-is-not-route-s-map-for-the-live-pick", ...) and replaced it
      with a source scan. The envelope said ASSERT seat.mjs holds no map of
      its own; it did not say replace, and Must-stay-true said everything F52
      and F54 proved stays proved. Reproduced by the control plane at the
      objects, mutation confirmed on disk first:
        const LOCAL_HOSTED = { "deepseek-flash": "deepseek-v4.1-flash-WRONG" };
        const hosted = LOCAL_HOSTED[writerModel] || writerModel;
      steps 08, 09 and 20 ALL EXIT 0, and node factory/tools/seat.mjs recipe
      aider then prints openai/deepseek-v4.1-flash-WRONG. That is T75 and
      D-67's exact harm - a launcher whose own copy disagrees with the router,
      which put three seats on the wrong api last sitting - and this lane
      removed its only detector. Restore that clause BESIDE the scan. Both
      must red: a DIVERGING local map, and a BYTE-IDENTICAL one.
  C2  The scan has no positive control, so its only observed outcome is
      "found nothing". Measured: replacing the matcher's ":" with ":::" so it
      can never match leaves step 09 at ok, exit 0. That is F48's shape - a
      fixture that cannot tell a clean file from a check that never ran. Run
      the same matcher against a synthetic string that CONTAINS the pair and
      assert it fires.
  C3  The scan claims more than it does. Its regex needs double quotes both
      sides and a bare colon between, and the review walked three one-line
      rewrites through it, each driving the live pick: single quotes,
      Object.fromEntries([[k,v]]), a computed key [K]: v. Do NOT chase every
      spelling - a source scan cannot win that race. NAME the limit, in the
      code and the log, the way T77 names the one it leaves uncovered. The ids
      are interpolated unescaped too, so the dot in deepseek-v4.1-flash is a
      wildcard; escape them.
  C4  False sentences in the log, all three measured by the review:
      (a) "a local copy cannot agree with route by hand and drift on the next
      change to either" - false, three local copies agree by hand and pass;
      (b) "The seam fixtures ... assert the surface count for the clean, dirty
      and empty cases" - there is no seam-empty-surfaces fixture;
      (c) P1's stated mutation reds SEVEN clauses, not the two listed, and the
      pre-existing suite already catches it - so P1 is proved, but by the
      review's isolating mutation and not by the one the log names. Say that.
  C5  P3's residual hole. droppedAudit counts a surface by its LABEL, not by
      whether anything was read, so two of three surfaces can carry text: ""
      and the check still reports "examined 6 id(s) over 3 surface(s)" and
      passes. That is P3's own lesson one notch up: a count of labels is not a
      count of scans. Count a surface only when it has text, and name any that
      arrive empty. Mutation: blank two surfaces' text. Step 08 reds.

Correction
    Pushing after a correction
      The rebase has diverged your branch from the tip you pushed. You are
      authorised to run
        git push --force-with-lease origin writer/F53
      on YOUR LANE BRANCH ONLY. main is never forced and no other branch is
      yours to rewrite. If force-with-lease is itself rejected, stop and
      return: that means something else moved your branch.

Base
  The commit CARRYING this envelope on live main; your worktree is rebased onto
  it before you start (T81). Rebase onto live main before push.

Holds
  factory/tools/seat.mjs
  factory/tools/route.mjs
  docs/log/f53.md
  Changed file set equals this list. seatReturn.mjs is DONE - P1 and P5 both
  cleared - and a diff against it is a stray file and a stop.

Must stay true
  - P1, P4, P5 stay proved; all three steps stay ok.
  - A check is ADDED to a check it does not strictly contain, never swapped for
    it. That is what this correction is about; do not repeat it in C5.
  - D-71, D-72, T83's name-size-count rule and T84 all keep working.
  - A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/route.mjs --self-test
  node factory/tools/seat.mjs --self-test
  node factory/tools/seatReturn.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste the mutation, command and failing line for C1 both ways, C2 and C5.

Look hardest at
  Your own harness. You caught it reporting P2 green over an unmutated file
  and wrote the rule: a green mutation run is a claim about the harness until
  the mutation is proved to be in the file. The control plane then made that
  same mistake twice verifying this lane. So for every mutation, print the
  changed bytes from disk BEFORE running the command, and paste that too.
  C1 is the whole lane: a scan catching only the spelling you thought of is how
  a deleted check looks green.

Runtime
  desktop-qwen. Writer seat only. Worktree only. Do not remap the control plane.

Spend cap
  Tool calls: 30. A cap is a return, not a hang.

Self-check
  Before returning, drive the list a reviewer would be given and fix what you find.

Return
  Append to docs/log/f53.md: what changed, the measuring command, no commit id.
