ENVELOPE: F47 — CORRECTION 1 — The gate names a shell that cannot run it (S24)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Where this lane stands
  Your six properties are APPROVED. An independent review drove one mutation per property at each property's own decision point and every one isolated; the changed file set is exactly the holds; the gate is 21 of 21; the self-test runs with no repository; the citations in docs/log/f47.md resolve. Your real drive of the System32 launcher, fixture 3's stand failing both -e and -c so the property-5 mutation cannot red it, and your own disclosure that fixture 4 had been red by two mutations until you re-anchored it, are all kept. Do not redo any of it. This is your ONE correction: sweep tier gets one, then the lane splits.

The defect, found independently by the review and by the control plane
  Every fixture exercises the library functions - shellStop, probeShell, shellDiagnostic, runSteps, verdict - and NOTHING exercises the lines at the bottom of the file that wire them into the command a person types. Driven at the objects, blob dc0384b to 8576c18 and restored: replacing
    const stop = shellStop(SHELL, workDir, logDir, spawnSync, sha);
  with
    const stop = null; void shellStop;
  leaves landingGate --self-test printing ok at exit 0, kitCheck --self-test printing ok, and the gate printing GATE PASSED, while the whole lane does nothing and S24 is open again. That is D-61 in its own words: the mutation must red at the PRODUCTION ENTRY POINT, not at a helper the fixture happens to reach. It is also F45's shape, which this envelope named as the thing to look hardest at.

Correction A — the entry point gets a fixture that could fail
  A fixture must spawn THE SHIPPED FILE AS A PROCESS and prove the CLI consults the probe BEFORE it archives the commit and before any step runs, asserting the diagnostic, no step line, and exit 2.
  The shell is a const today, so the entry point cannot be driven without editing the file. Make it selectable: read the shell from an environment variable, defaulting to bash when unset, and name that variable in docs/log/f47.md. The default path must behave exactly as today.
  Prove the fixture COULD fail: remove the wiring exactly as above, run the self-test, paste it red, restore, paste it green.

Correction B — the diagnostic keeps its cause when the shell cannot be spawned
  shellDiagnostic chooses between an exit status and a spawn error, and fixture 4's stand never sets an error, so the spawn branch has no fixture that could fail it. Driven: with const spawned = true the self-test still prints ok, while a missing shell degrades from
    spawn   spawnSync grok-no-such-shell-f47 ENOENT
  to a bare exit 1. No cause is exactly the invisibility S24 exists to close.
  Give that branch its own fixture, with a mutation that reds it ALONE.

Base
  Your worktree has been rebased by the control plane onto live main. Read the first line of this file before you start: if it does not say CORRECTION 1, stop and say so (T81).

Holds
  factory/tools/landingGate.mjs
  docs/log/f47.md
  Changed file set equals this list. A stray file is a stop.

Pushing after a correction
  The rebase has diverged your branch from the tip you pushed. You are authorised to run
    git push --force-with-lease origin writer/F47
  on YOUR LANE BRANCH ONLY. main is never forced and no other branch is yours to rewrite. If force-with-lease is itself rejected, stop and return: that means something else moved your branch.

Must stay true
  The six approved properties stay green. Do not weaken a fixture to make room.
  Steps still run through bash -e from a file, exit code captured, never piped away (T14, T10).
  The gate still builds its own archive of the commit under test (T08).
  --self-test prints ok at exit 0 and still runs where there is no repository.
  From git-bash the gate is 21 of 21 and prints GATE PASSED at your commit.
  The public landing Action runs this on Ubuntu with no secrets, where the new variable is unset; the default must pass there.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/landingGate.mjs --self-test
  node factory/tools/landingGate.mjs
  The two new mutations, each pasted with its output and the property it reds for, plus the removal-and-restore proof for Correction A.

Look hardest at
  A fixture that asserts the wiring by reading the file's own source text. That is a proxy for the property, not the property, and it passes if the call is present but unreachable. Spawn the process.
  If the gate reds ALL 21 steps at any commit, your bash is the WSL launcher and the red is not yours: that is S24, which this lane is booked for. Say so in the log, use git-bash, do not spend calls proving it.
  Making the shell selectable is a new way for the default to drift. The unset case is the ordinary case and it needs a fixture that could fail.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate).
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  25 tool calls for two corrections in one file. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). On a decay sign: stop, print the signs, return.

Self-check
  Before returning, ask of each NEW fixture whether it could have failed, and prove it with the mutation you pasted. Record in docs/log/f47.md what the self-check changed.

Return
  A few lines added to docs/log/f47.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
