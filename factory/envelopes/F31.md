ENVELOPE: F31 — The packet prefix is byte-stable, and a check proves it
Authorization: issued by the control plane on the owner's word to launch a writer seat and a reviewer. Read AGENTS.md, BOARD.md, factory/traps.yaml once, then this envelope. Nothing else is required reading.

Work / acceptance
  Add two assertions to the existing --self-test in factory/tools/packet.mjs:
  (a) in seatPacket, the envelope part is strictly last;
  (b) the rendered bytes before the envelope part are identical for two different lane ids.
  Acceptance: node factory/tools/packet.mjs --self-test still passes unchanged on this base, AND fails with a named message when seatPacket is reordered to put the envelope first. Demonstrate that failure and paste it. A check that cannot fail is a diary.
  Why: packet.mjs already orders static bytes first for a provider prefix cache (T45) and says so in a comment. Nothing fails if that order changes. Gate step 16 checks size, ledger leaks and MISSING; it does not check order.

Base / branch
  7a177682c51c335d93777338f751d96d8facf0a1 from git ls-remote origin refs/heads/main.
  The main records commit that carries this envelope adds this file and F31 board state only. Use the supplied isolated worktree of that issuance commit, branch writer/F31. Read live main again before push; if it moved, stop and report. Push writer/F31 only, never main.

Holds
  factory/tools/packet.mjs
  docs/log/f31.md
  Changed file set equals these two paths. A stray file is a stop. No new file in factory/tools/: kitCheck rule G reds a tool no gate step names. No edit to factory/landing-checks.json; gate step 16 already runs this self-test.

Must stay true
  The packet's output bytes do not change for any existing caller. seatPacket, cpPacket, render, boardSlice and trapsDigest keep their signatures and their current output.
  Every existing self-test assertion stays. This adds; it does not replace.
  No claim about cache hit rate, token savings or cost. cachedTokens in a return measures the hit. A number no command printed is not written.
  The prefix is stable within a sitting only: the board slice carries now and the traps digest changes when a trap is absorbed. Do not assert cross-sitting stability, and do not remove live state from the board slice to manufacture it.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs --check
  node factory/tools/packet.mjs cost
  node factory/tools/kitCheck.mjs --self-test
  node factory/tools/sizeBudget.mjs
  node factory/tools/renderBoard.mjs --check
  git diff --check
  Paste what each printed, from the command that produced it.
  Negative proof: temporarily reorder seatPacket to put the envelope first, run --self-test, paste the failure text, revert. The returned diff must not contain the reordering.

Look hardest at
  A test that passes on both the good tree and the broken one. Prove the negative or the lane is not done.
  seatPacket(root, null) has no envelope part; the two-lane comparison needs lane ids. An unissued lane yields a MISSING part — that is still the envelope part and still last.
  Comparing whole rendered strings always differs, because the envelope differs. Compare only the bytes before the envelope part.
  Do not open factory/returns.json, factory/experiments.json, the ledger or docs/ledger/. Not the reading path.
  A tool-policy refusal is not a reason this envelope waits.

Runtime
  pc-dashscope recipe: factory/tools/seat.mjs. From project.json: qwen-code, deepseek-v4.1-flash, dashscope, dashscope-payg, worktree, ff-only, owner-account, provider-prefix, helpMode=none.
  Owner-selected control plane is Claude desktop and launches the seat; stored controlPlane=grok-cloud stays unchanged. Writer seat only; do not remap the control-plane session. Print-mode in the worktree.
  Token in the process environment only. Never print a key.
  Prefix: AGENTS, BOARD, this envelope, tools first; variable work last. Paste prompt_cache_hit_tokens or cached_tokens if the meter reports them, or say it did not.

Spend cap / fuse
  USD 0.50, 10 session turns, 20 tool calls, then stop and return. Hitting the cap is a return, not a hang.
  Sweep tier: one correction, then split. Stop on a named decay sign in factory/sessions.json. No resume from prose, never from a transcript.

Self-check
  Before returning, drive the list a reviewer of this lane would be given and fix what you find. Record in docs/log/f31.md what the self-check changed.

Return
  Brief docs/log/f31.md: what changed, each measuring command and what it printed, the negative proof, what the self-check changed, the model, and spend and cache usage or a statement that the meter did not report them. No invented counts. No commit id of your own.
  Also print, at the end of your turn, a JSON object matching contracts/seat-return.v1.json with lane F31, runtime pc-dashscope, model, base, branch writer/F31, changed, verify, measured, spendUsd, and cachedTokens if reported.
  Commit subject: F31: the packet prefix is byte-stable, and a check proves it.
  Push writer/F31. Return the branch, the paths and the commands. Never review or land yourself.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
