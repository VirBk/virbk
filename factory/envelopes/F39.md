ENVELOPE: F39 — the cloud writer takes the router, and idles loudly
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  The GitHub writer Action stops being a second seat nobody asked for, stops hardcoding a provider the router retired, and stops failing where it should skip.

How this envelope is written
  D-59: this tier ships checks that pin the implementation instead of the property. Every item NAMES THE MUTATION that must turn it red, and your return pastes it going red. Make it faithful: a mutation that breaks the file syntactically or crashes the tool proves nothing.

Acceptance
  1. The cloud writer is opt-in; idle is the default. writer.yml fires on any push to main touching factory/envelopes/**, so issuing a lane launches a headless seat on writer/<LANE>, the branch a local worktree may already hold. On F33 that happened and a stale key stopped it, not design. Gate the job on `vars.CLOUD_WRITER == 'on'`; unset means idle. --self-test asserts writer.yml carries the guard. MUTATION: delete it — red.
  2. The cloud seat takes the router. ghaWriter hardcodes OPENAI_BASE_URL to DashScope and maps the model through its own HOSTED table, so it cannot take the native path route.mjs resolves for the local seat and D-51 made default. Export the resolver from route.mjs and call it: base URL, key NAME and model id from that one answer. MUTATION: hardcode DashScope back — red.
  3. The key it counts is the key it uses. hasWriterSecret() counts DEEPSEEK_API_KEY but the call reads only DASHSCOPE_API_KEY, so a repository holding just the DeepSeek secret stops idling and then fails on an empty key. The secret checked is the one the resolved path names. MUTATION: count a key the call does not pass — red.
  4. A missing secret is a loud skip, not a loud failure (T55). With no writer key in the environment, `ghaWriter run` prints why it is skipping and exits 0. It never exits non-zero for a secret that was never placed. MUTATION: exit 1 on the missing-secret path — red.
  5. The packet reaches the cloud seat on stdin. ghaWriter passes it as an argv string, which is T72 in the cloud path, surviving only because the runner is Linux. Hand it over on stdin. MUTATION: pass it as argv again — red.
  6. Two traps in factory/traps.yaml, in the shape F36 landed: id, kind, object, title, rule, each rule ending by naming its check (T24). One for item 1, object .github/workflows/writer.yml. One for items 2 and 3, object factory/tools/ghaWriter.mjs. Do NOT touch factory/sparks.json; absorbing is the control plane's act at landing.

Base
  9934c6c517e9bb8eace3f27d64ec14cd1bfccd7d
  Worktree from that base. Rebase onto live main before push and prove it with git range-diff. Push the branch writer/F39 only.

Holds
  .github/workflows/writer.yml
  factory/tools/ghaWriter.mjs
  factory/tools/route.mjs
  factory/traps.yaml
  docs/log/f39.md
  Changed set equals this list; a stray file is a stop. route.mjs is a control-plane hold, handed to this lane for its duration because the lane runs ALONE — stated, not done silently. project.json, writer-paths.json and sparks.json are NOT yours. Do not change what the router decides; only let a second caller ask it.

Must stay true
  No assertion satisfied by construction, none checked against the code that produced what it asserts. D-59 is the row.
  The Action never pushes main and never touches a branch other than writer/<LANE> (T55). assertNotMain stays.
  ghaWriter is hands, not judgment: it does not choose a model, it asks the router (T29).
  A new trap's object resolves in the tree, or is the star; packet.mjs --self-test enforces it and must stay green.
  No copied numbers (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/ghaWriter.mjs --self-test
  node factory/tools/ghaWriter.mjs watch
  node factory/tools/route.mjs
  node factory/tools/packet.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and the five mutations going red. Run item 4's skip with the keys cleared from the environment and paste the exit code. The gate is 21 steps with Writer Action red while an envelope is live; that one is expected.

Look hardest at
  A guard asserted by reading the YAML you wrote, in a way that agrees with itself. Assert the condition, break it on purpose.
  Making the router answer differently for the cloud than for the desktop. One resolver, one answer, two callers.
  An idle path that prints nothing. A silent skip and a broken Action look identical in a run list.
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 70 — five checks and two traps. USD 0.25, then stop.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f39.md what it changed. If it found nothing, say that.

Return
  docs/log/f39.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
