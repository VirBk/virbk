ENVELOPE: F39 — correction 1, the only one
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make the self-test reach the code that actually launches, and retire an assertion that can no longer pass.

What is NOT wrong
  The implementation is right and reviewed: the guard is at job level so GitHub skips the whole job, the router's answer is byte-identical at base and HEAD with only a providerModel field added, route.mjs is import-safe, assertNotMain still brackets the push, both traps resolve, and the changed set is exactly the five holds. Do not redesign any of it. Item 3's loud skip and item 1's guard are mutation-proved and stay as they are.

Acceptance
  1. BLOCKING. The self-test never reaches the code that ships. Its fixture drives only `run --dry-run`, which returns at line 167, or the missing-secret skip, which returns at line 148. Lines 182 to 194 — the real env object and the real spawnSync — are dead code to it, so four separate lines mutate green:
       OPENAI_BASE_URL: spec.base        -> a literal DashScope URL      self-test ok
       OPENAI_API_KEY: writerKey(answer) -> "sk-hardcoded-wrong-key"     self-test ok
       OPENAI_MODEL: spec.model          -> "some-other-model"           self-test ok
       input: prompt                     -> spec.args.concat(["-p", prompt])  self-test ok
     Two of those four the first envelope never named; the review found them by mutating the block rather than reading the plan. Drive the real launch path from a fixture — inject the spawner, or take it as an argument the way seat.mjs census takes its lister — and assert what the child is ACTUALLY given: base URL, key value, model id, argv, and that the packet arrives on stdin and not in argv. All four mutations above must go red, and your return pastes all four.
  2. The assertion "live envelopes present; watch would not be idle" is now T09, a check that cannot pass. AGENTS section 3 runs the landing gate from an archive taken BEFORE the envelope is deleted, so this sub-assertion is red at every landing of every lane, permanently. Before this lane its redness tracked a real hazard, because any push touching factory/envelopes/** fired the Action unconditionally; the job-level guard severed that. Rescope it to the property rather than the state: watch is idle when no envelope is live, driven against a fixture with envelopes and one without, both directions asserted. Do not delete the rest of --self-test. MUTATION: make watch print idle when an envelope IS live in the fixture — red.
  3. docs/log/f39.md pastes "none over cap, 8 in the band"; the command prints 9, the ninth being this log, which the act of writing it pushed into the band. Re-run and paste what it prints. Fix that sentence and change nothing else in the file.

Base
  9f26f42f4e6a8639dd929c2343bacc64bf831b2c
  Your worktree is on writer/F39 at 512e86b. Rebase onto that base, then correct. Prove the rebase with git range-diff. Push the branch only.

Holds
  factory/tools/ghaWriter.mjs
  docs/log/f39.md
  Narrower than the first envelope on purpose: writer.yml, route.mjs and traps.yaml are finished and are NOT yours this round.

Must stay true
  An assertion tests what ships, not a plan the shipping code is free not to honour. That is the whole of this correction, and it is the fourth lane in a row to need this said.
  Everything the review confirmed survives: the job-level guard, the router's unchanged answer, the loud skip at exit 0, assertNotMain, both traps.
  ghaWriter is hands, not judgment (T29). Injecting a spawner does not let it choose anything.
  No copied numbers (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/ghaWriter.mjs --self-test
  node factory/tools/ghaWriter.mjs watch
  node factory/tools/packet.mjs --self-test
  node factory/tools/sizeBudget.mjs
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and the five mutations going red. After item 2 the gate should be 21 of 21 green even with this envelope live; if step 13 is still red, say why rather than working around it.

Look hardest at
  An injected spawner the real entry point does not use. If the production path still builds its own env or argv, you have moved the dead code rather than tested it.
  A fixture that asserts the argv you just wrote. Assert the property: the packet is in stdin, its bytes are the packet's bytes, and no argument carries it.
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 45 — one fixture, one rescope, one sentence. USD 0.15, then stop.
  This is correction 1 and at sweep tier it is the only one.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f39.md what it changed. If it found nothing, say that.

Return
  Append to docs/log/f39.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
