ENVELOPE: F45 — CORRECTION 1 — A seat packet refuses a stale envelope (S23)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Already approved — do not touch
  All three items are APPROVED and were driven independently: the refusal writes ZERO bytes
  to stdout and exits 3 naming the difference and the remedy; an unresolvable comparison is a
  note that still emits, proved from an unpacked archive with no git; and stdout is
  byte-identical between the emitting and note paths at 32226 bytes. The gate is 21 of 21.
  Do not re-open any of that, and do not move the comparison out of the CLI path.

Work
  Give the common case — the envelope MATCHES main — a fixture that could fail.

Acceptance
  The two new fixtures cover no repository at all, and a repository where the copies DIFFER.
  Neither covers the case every ordinary launch takes: the comparison succeeds and the copies
  are IDENTICAL. That branch has no fixture that could fail it, and the consequence is the
  worst failure available to this tool. Driven by the control plane: replacing `if (same)
  return null;` with `if (false) return null;` leaves `--self-test` printing ok at exit 0,
  while `packet seat F45` on a clean, byte-identical worktree exits 3 with ZERO bytes on
  stdout and refuses. Every lane, every worktree, with no correction pending, would refuse
  and no seat could launch — and the self-test, the check and the landing gate all stay green.
  That is precisely what T09 and this envelope's own item 2 forbid, and it is D-62 again: the
  envelope named three mutations and never named the equality comparison as a property of its
  own. The control plane's envelope is the cause and the ledger says so.

  Two items. DICTATED.

  Item 1 — the identical case is a fixture, and it can fail.
  Add a git fixture in which the worktree's envelope and the copy on the tracked main ref are
  byte-identical. Assert, through the CLI a person types and not a helper: exit 0, the full
  packet on stdout, and stderr carrying NEITHER a refusal NOR a note — an ordinary launch is
  silent about the comparison it made.
  MUTATION THAT MUST TURN IT RED, at the production entry point: make the comparison always
  report a difference, `if (false) return null;`. `--self-test` must fail and name the
  property, that an identical envelope was refused and a working launch was killed.
  This must red for THIS item alone. The opposite mutation, never refusing, is already caught
  by the differing fixture, so after this correction BOTH directions of the comparison have
  their own red and neither stands in for the other.

  Item 2 — the log carries no uncommanded count.
  docs/log/f45.md says sizeBudget printed `70 budgets`. Run at this commit on a clean tree it
  prints `size budget — 71 budgets, bands at 80 per cent`; every KB and percent figure beside
  it in the log is correct, only the count is wrong. AGENTS section 4: a count comes from the
  command that produced it, with the command, or is omitted. A false sentence in the diff is
  a blocking defect. Paste the real line or drop the figure.

Base
  Rebase onto live main; read `git ls-remote origin refs/heads/main` yourself and do not take
  a sha from this sentence. Keep your commits, rebase, push the branch only. Branch writer/F45

Holds
  factory/tools/packet.mjs
  docs/log/f45.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours.
  F44 is in correction on factory/tools/seat.mjs; do not touch it.

Must stay true
  T61: the packet is the reading path and has a byte budget; nothing you add reaches stdout
  in any path. F31's prefix stays byte-stable. The exported builders the gate calls keep
  their behaviour and still need no git. `cp` and `--self-test` keep working. Gate 21 of 21.
  The three approved properties keep their reds: refusal writes nothing to stdout, an
  unresolvable comparison notes and emits, and the note never reaches stdout.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs seat F45
  node factory/tools/packet.mjs cp
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Ask of every branch in the comparison, not just the ones the first envelope named, what
  fixture could make it fail. The failure this correction closes was invisible to the
  self-test, the check and the gate at once, and it would have taken the whole factory's
  ability to launch a seat with it.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: native, https://api.deepseek.com/v1,
  key DEEPSEEK_API_KEY. Do not remap the control-plane session. Hands never call a model.

Spend cap
  20 tool calls, or USD 0.30, then stop and return. This is a correction, not a lane.
  This is the ONLY correction: a sweep-tier seat gets one, then the work splits.

Fuse
  Split on correction done, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive item 1's mutation yourself, restore byte for byte, and confirm `git hash-object` on
  the file matches before and after.

Return
  docs/log/f45.md: what changed, the command and what it printed, item 1's mutation pasted
  going red, and the identical-case fixture's own passing output, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
