ENVELOPE: F45 — A seat packet refuses a stale envelope the worktree still holds (S23)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make `packet seat <LANE>` refuse when the worktree's envelope is not the one on live main.

Acceptance
  Measured, and it cost a paid session this sitting. A correction envelope is written to main,
  but a seat reads its envelope from its own WORKTREE, which sits on the lane branch at the
  issuance commit and still holds the SUPERSEDED page; `git fetch` does not touch the working
  tree. A seat was launched, read the old envelope and worked to it, and nothing said so. It
  was caught by a person reading the envelope's first line. The failure looks exactly like a
  seat ignoring its instructions.

  Three items. DICTATED.

  Item 1 — the seat path compares, and refuses.
  In the `seat <LANE>` CLI path only, compare the envelope in the working tree against the
  same path at the local ref for live main (`refs/remotes/origin/main`). If they differ:
  write NOTHING to stdout, print to stderr that the worktree's envelope differs from the one
  on main and that a correction launch must rebase the worktree first, and exit non-zero.
  Writing nothing to stdout is the point: the launch line is a pipe into the harness, so an
  empty stdin is a loud failure rather than a seat working to the wrong page.
  MUTATION THAT MUST TURN IT RED, at the production entry point: drop the comparison so the
  path always emits. `node factory/tools/packet.mjs --self-test` must fail and name the
  property, that a stale envelope was handed to a seat.

  Item 2 — an absent comparison is a NOTE, never a refusal.
  If `refs/remotes/origin/main` does not resolve, or git is not available, or the envelope
  does not exist on main because the lane is newly issued and not yet pushed, the path prints
  a one-line note to stderr saying the comparison could not be made and why, emits the packet
  normally, and exits 0. A check that cannot pass is removed or skipped loudly (T09); this one
  must never turn a working launch into a dead one because a ref was missing.
  MUTATION THAT MUST TURN IT RED: make the unresolvable case refuse. The self-test must fail
  naming that a launch was refused for a comparison nobody could make. This must red for THIS
  item alone — if one mutation reds item 1 and item 2 together, one of them is untested (D-62).

  Item 3 — the packet's BYTES do not change.
  This is F31's assertion and it is why this lane is delicate: the bytes before the envelope
  are a provider prefix cache hit across the seats of one sitting, measured at 95 to 97 per
  cent this sitting. The refusal and the note go to STDERR and to the exit code. Nothing this
  lane adds may appear on stdout, in any path, including the note path.
  MUTATION THAT MUST TURN IT RED: print the note to stdout instead of stderr. The existing
  byte-stability assertion, or one you add, must fail naming that the packet's bytes moved.

Base
  ab398c3b625fb266dc3e87cdbde36b31d95763b1
  Worktree from that SHA. Read `git ls-remote origin refs/heads/main` before you push; if it
  moved, rebase onto it. Push the branch only. Branch writer/F45

Holds
  factory/tools/packet.mjs
  docs/log/f45.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours:
  the control plane writes the trap row at landing.
  F44 is your sibling on factory/tools/seat.mjs, proved disjoint; do not touch it.

Must stay true
  T61: the packet is the reading path and has a byte budget, and it sits at 99.8 per cent of
  26 KB today, so your change adds not one byte to stdout.
  The EXPORTED functions the gate calls keep their signatures and behaviour: gate steps 16 and
  17 must not start needing git. Put the comparison in the CLI path, not in the builders.
  `cp` and `--self-test` keep working unchanged. The gate stays 21 of 21.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/packet.mjs --self-test
  node factory/tools/packet.mjs seat F45
  node factory/tools/packet.mjs cp
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  The gate runs this tool from an unpacked ARCHIVE with no git at all, and CI checks out
  shallow. If your comparison runs anywhere the gate reaches, you have built a check that
  cannot pass, which is T09 and which this factory has already shipped once. Item 2 is the
  guard; prove it from the archive, not by reasoning about it.
  Ask of each assertion what it would still pass if the comparison were deleted entirely.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY.
  Do not remap the control-plane session. Hands never call a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  40 tool calls, or USD 0.60, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive all three mutations yourself, restore byte for byte, and confirm `git hash-object
  factory/tools/packet.mjs` matches before and after.

Return
  docs/log/f45.md: what changed, the command and what it printed, each mutation pasted going
  red and reding for its own item alone, and proof that stdout is byte-identical in both the
  emitting and the note paths, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
