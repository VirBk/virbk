ENVELOPE: F37 — the seat launch is checked
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Four claims about launching a seat, each with a check that fails when the claim is broken. Then write the two traps that pay for two of them.

How this envelope is written
  D-59: this tier ships checks that pin the implementation instead of the property, and such a check is invisible to your self-test and to the gate by construction. Every item below NAMES THE MUTATION that must turn it red, and your return pastes it going red. A check you cannot break on purpose is not done.

Acceptance
  1. The packet reaches a seat on stdin, never as an argv string. The recipes print `-p "$(cat packet.txt)"`; on Windows a cap-sized packet exceeds the command-line limit and the harness dies with "Argument list too long" before reading a byte. Every recipe naming packet.mjs prints a stdin carrier instead. --self-test asserts no recipe contains an argv substitution of the packet, and that every recipe naming packet.mjs names a stdin carrier. MUTATION: put the argv form back into one recipe — red.
  2. A writer key is read from the store that owns it, in the same act as the launch. The Windows lines set OPENAI_API_KEY from a bare `$env:NAME`, which is whatever was true when that terminal opened; after a rotation the User scope answers 200 and every already-open shell keeps answering 401. Windows lines read `[Environment]::GetEnvironmentVariable("NAME","User")`. POSIX lines are unchanged: there the shell environment is the store. --self-test asserts no Windows line assigns the key from a bare `$env:`. MUTATION: restore one — red.
  3. Holds are proved disjoint, not assumed. `node factory/tools/seat.mjs disjoint <LANE> <LANE>` reads the two lanes' holds from factory/board.json, exits 0 when no file is named by both and non-zero naming the overlap when one is. A glob and a path that the glob matches are an overlap. self-test covers a disjoint pair and an overlapping pair. MUTATION: make disjoint() return 0 unconditionally — red.
  4. A seat census comes from the process table, not a scheduler file. `node factory/tools/seat.mjs census` lists running writer seats from the platform's process listing. Take the lister as an injected argument so it is testable: census(lister) returns what the lister reports, and the entry point passes the platform command. self-test asserts census returns exactly the rows an injected fake lister yields, including none, and reads no file. MUTATION: have census read a JSON file — red.
  5. Two new traps in factory/traps.yaml, in the shape F36 landed: id, kind, object, title, rule. One each for items 1 and 2, object factory/tools/seat.mjs. Each rule ends by naming its check (T24). Do NOT touch factory/sparks.json; absorbing is the control plane's act at landing.
  6. factory/landing-checks.json: step 9 is named "Hosted DashScope model map" and now runs far more. Rename it for what the self-test proves. Add no step; it is already wired.

Base
  ae28aa99aa61d7db538bd8a14e1170a1cc714e24
  Worktree from that base. Rebase onto live main before push and prove it with git range-diff. Push the branch writer/F37 only.

Holds
  factory/tools/seat.mjs
  factory/landing-checks.json
  factory/traps.yaml
  docs/log/f37.md
  Changed set equals this list; a stray file is a stop. factory/sparks.json, factory/board.json and factory/tools/packet.mjs are NOT yours.

Must stay true
  No assertion may be satisfied by construction, and none checked against the code that produced what it asserts. Three lanes, three shipped; D-59 is the row.
  A new trap carries an object that resolves in the tree, or the star. packet.mjs --self-test enforces it and must stay green.
  seat.mjs does not call a model and does not read an API key (T29). A recipe is text it prints; item 2 changes what it prints, not what it does.
  No copied numbers (T02). A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs recipe qwen-code
  node factory/tools/seat.mjs disjoint F35 F38
  node factory/tools/seat.mjs census
  node factory/tools/packet.mjs --self-test
  node factory/tools/landingGate.mjs
  Paste what each printed (T56) and the four mutations going red. The gate is 21 steps with Writer Action red while an envelope is live; that one is expected.

Look hardest at
  A check that reads the recipe text you wrote and agrees with itself. Items 1 and 2 assert a literal string appearing or not appearing: write them against the rendered recipe output, and break them before you believe them.
  A census that cannot be tested because it shells out unconditionally. Inject the lister.
  Keystone: NONE — governance maintenance under D-54.

Runtime
  Live picks: qwen-code, deepseek-flash, writerPath auto, worktree, ff-only, provider-prefix. route.mjs printed path native, base https://api.deepseek.com/v1, key DEEPSEEK_API_KEY, off-peak. Writer seat only; do not remap the control-plane session (T19, T31). Print-mode in the worktree. Hands: hands.mjs, never a model (T29). You echo a stamp, not your model (T69).

Spend cap
  Tool calls: 70 — four checks, two traps and a rename. USD 0.25, then stop.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign (factory/sessions.json). A successor reads AGENTS, BOARD, this envelope, never a transcript (T25, T26).

Self-check
  Drive the list a reviewer would be given, fix what you find, record in docs/log/f37.md what it changed. If it found nothing, say that.

Return
  docs/log/f37.md: what each item changed and the command that measured it. No commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
