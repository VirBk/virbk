ENVELOPE: F44 — The census counts seats, not the processes one launch spawns (S22)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Make the census report one seat per launch, and name what it still cannot see.

Acceptance
  Measured, not theoretical: two qwen-code launches printed `6 running writer seat(s)`, a
  cli-entry wrapper plus --expose-gc children for each. F40 landed the program-position rule
  and did NOT fix this — every one of those six rows carries the harness token in program
  position, so all six are real harness processes and the count is still wrong by the process
  tree. T77 names the limit; this lane closes it. The two-writer ceiling and the dead-seat
  resume both read this number.

  Two items. DICTATED. Do not widen the harness list. Do not make census read a file (T04).

  Item 1 — a process whose parent is already counted is not a second seat.
  The platform lister gains the parent pid alongside the pid and the command line, and a row
  whose parent is itself a counted row is folded into it rather than counted again. On win32
  the lister is a PowerShell Get-CimInstance line that can select ParentProcessId in the same
  call; on other platforms `ps -eo pid=,ppid=,args=`. Keep the lister INJECTED: every fixture
  drives it through `--lister`, and that must not change.
  The rendered output names one row per seat and says how many processes it folded.
  MUTATION THAT MUST TURN IT RED, at the production entry point, in the `if (cmd === "census")`
  block: stop folding, so every matching row counts. `node factory/tools/seat.mjs --self-test`
  must fail and name the property, that one launch was counted as several. Your fixture must
  be a real launch's shape — a wrapper and its children sharing a parent chain — or it cannot
  fail for the right reason (D-62).

  Item 2 — the count is asserted, and so is the folding, and each reds alone.
  A fixture proves a property only if it could fail it. Item 1's fixture must distinguish
  folding from not-folding; add a second fixture of two INDEPENDENT launches, whose parents
  are not each other, which must count as two. One mutation must red the first alone and
  another the second alone: if a single mutation reds both, one is untested.
  MUTATION THAT MUST TURN IT RED: fold unconditionally, so any second row is swallowed. The
  two-independent-launches fixture must fail naming that two seats were counted as one.

  What this lane does NOT take, and must not: a process whose program merely lives inside a
  harness install directory still counts. T77 already names that limit and it stays named.

Base
  ab398c3b625fb266dc3e87cdbde36b31d95763b1
  Worktree from that SHA. Read `git ls-remote origin refs/heads/main` before you push; if it
  moved, rebase onto it. Push the branch only. Branch writer/F44

Holds
  factory/tools/seat.mjs
  docs/log/f44.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours:
  the control plane writes the trap row at landing.
  F45 is your sibling on factory/tools/packet.mjs, proved disjoint; do not touch it.

Must stay true
  T72's stdin carrier, T73's key-from-the-user-store lines, T77's program-position rule with
  its harness-label assertion, T78's disjoint argument-count refusal and T80's approval-mode
  scope all live in this file and all keep passing unchanged.
  The census still reads the process table through the injected lister and reads no file.
  `census` still accepts `--lister <command>` and `--root <tree>`.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs census
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Your fixture is the whole risk here. A fixture invented from what you imagine a process
  table looks like can pass while the real one does not fold, and the real shape is in this
  envelope's first paragraph and in `census` on this machine. Verify your fixture's BYTES
  before believing its verdict: a shell heredoc can silently collapse the backslashes in a
  Windows command line, which for this lane corrupts the very rows under test.
  Ask of each assertion what it would still pass if the folding were deleted entirely.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: path native, base
  https://api.deepseek.com/v1, key DEEPSEEK_API_KEY.
  Do not remap the control-plane session. Hands never call a model.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  40 tool calls, or USD 0.60, then stop and return.
  Hitting the cap is a return, not a hang. Escalation is a new envelope.

Fuse
  Split on envelope done, correction cap, spend cap, or a named decay sign
  (factory/sessions.json). Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive both mutations yourself, restore byte for byte, and confirm `git hash-object
  factory/tools/seat.mjs` matches before and after.

Return
  docs/log/f44.md: what changed, the command and what it printed, each mutation pasted going
  red and reding for its own item alone, and the live `census` output on this machine with
  its fold count, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
