ENVELOPE: F44 — CORRECTION 1 — The census counts seats, not processes (S22)
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Already approved — do not touch
  The fold is APPROVED, and the ancestor walk rather than the immediate parent is right and
  better than the envelope asked for. Both prescribed mutations reproduce and are
  distinguishable. Do not re-open that, do not widen the harness list, do not read a file.

Work
  Stop the walk erasing seats, and stop a torn process table silently reopening S22.

Acceptance
  The walk assumes a complete, acyclic, single-instant snapshot where every ancestor pid
  resolves to a row. This machine gives neither: Windows stamps ParentProcessId at creation
  and never clears it when the parent dies, and Get-CimInstance does not enumerate
  atomically. Both failures below were driven by the control plane through the CLI.

  Three items. DICTATED.

  Item 1 — a cycle never erases a seat, and the count is never zero while a counted row exists.
  Two counted rows whose parent pids point at each other — ordinary pid-reuse churn — each
  fold into the other and BOTH disappear: pid 8001 parented to 8002 and 8002 parented to
  8001, both conpty rows, prints `0 running writer seat(s)`. Worse than the over-count this
  lane fixes, because the two-writer ceiling and the dead-seat resume read this number: a
  false zero lets a third seat launch and lets a resume collide with a live session. When the
  walk returns to a pid already walked, the seat is the counted row with the LOWEST pid in
  that cycle, and the rest fold into it. Assert the invariant: if any counted row exists, the
  count is at least one.
  MUTATION THAT MUST TURN IT RED, at the `if (cmd === "census")` block: restore the old
  behaviour so a cycle returns the row itself and both fold away. `--self-test` must fail and
  name the property, that a cycle erased every seat. The invariant assertion must red too, on
  its own, when the count is allowed to reach zero with counted rows present.

  Item 2 — a torn chain is reported, not silently counted as another seat.
  A counted row whose parent exited between the enumeration and the write cannot be attached
  to its launch: a wrapper at pid 7000 plus a conpty row at 7002 whose parent 7001 is in no
  row prints `2 running writer seat(s)` for ONE launch — the S22 shape this lane was issued
  to close, reopened by churn. You cannot infer the missing link, so do not guess it. Count
  the row, since losing it is item 1's failure, and make the output SAY the chain is broken:
  name the row and the missing parent pid, and have the summary state how many rows have a
  broken chain and that the count may over-report. An honest number with its uncertainty
  named beats a confident wrong one.
  MUTATION THAT MUST TURN IT RED: drop the broken-chain reporting so a torn listing prints a
  bare count. `--self-test` must fail naming that a torn chain was not reported. This must red
  for THIS item alone: if one mutation reds items 1 and 2 together, one is untested (D-62).

  Item 3 — no uncommanded count in the log.
  docs/log/f44.md says the machine printed `3 running writer seat(s)` before this change; no
  command produced that 3, it is arithmetic over two pasted numbers. AGENTS section 4: a
  count comes from its command, or is omitted. Run it at the base commit and paste it, or
  delete it.

Base
  Rebase onto live main; read `git ls-remote origin refs/heads/main` yourself and do not take
  a sha from this sentence. Keep your commits, rebase, push the branch only. Branch writer/F44

Holds
  factory/tools/seat.mjs
  docs/log/f44.md
  Changed file set equals this list. A stray file is a stop. factory/traps.yaml is NOT yours.
  F45 is in review on factory/tools/packet.mjs; do not touch it.

Must stay true
  T72, T73, T77, T78 and T80 all live in this file and all keep passing. The census still
  reads the process table through the INJECTED lister and reads no file. `--lister` and
  `--root` keep working. The gate stays 21 of 21. The two probes the review cleared stay
  sound: a self-parent row resolves to one seat without looping, and two independent launches
  with interleaved pids stay separate.

Verification
  node factory/tools/seat.mjs --self-test
  node factory/tools/seat.mjs census
  node factory/tools/landingGate.mjs
  Paste counts from the command that produced them, never from memory.

Look hardest at
  Every fixture here supplies a fully connected, acyclic chain, kinder than the machine it
  models, and that is how both defects shipped green through a self-test and a gate. Ask of
  each NEW fixture whether it could fail its property, and write it as a real file: a shell
  heredoc collapses the backslashes and corrupts the rows under test.

Runtime
  desktop-qwen, writer seat, print mode in the worktree. project.json: harness qwen-code,
  model deepseek-flash, isolate worktree. route.mjs: native, https://api.deepseek.com/v1,
  key DEEPSEEK_API_KEY. Do not remap the control-plane session. Hands never call a model.

Spend cap
  25 tool calls, or USD 0.40, then stop and return. This is a correction, not a lane.
  This is the ONLY correction: a sweep-tier seat gets one, then the work splits.

Fuse
  Split on correction done, spend cap, or a named decay sign (factory/sessions.json).
  Successor reads AGENTS, BOARD, this envelope, not a transcript.

Self-check
  Drive both mutations yourself, restore byte for byte, and confirm `git hash-object` on the
  file matches before and after.

Return
  docs/log/f44.md: what changed, the command and what it printed, each mutation pasted going
  red and reding for its own item alone, and the cycle and torn-chain fixtures with what the
  census prints for each, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
