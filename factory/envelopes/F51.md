ENVELOPE: F51 — A verdict names the clause it found, and an imported tool does not run
Authorization: this envelope, issued by the control plane. Read AGENTS.md, then BOARD.md, then factory/traps.yaml once. Then do the work. Nothing else is required reading.

Work
  Two defects the F49 review cleared as non-blocking, plus one sentence F49 made false. All small, all real, none a design change.

What you cannot see from the file
  ONE. envelopeCheck.mjs collapses two different answers into one verdict label. A correction envelope whose force authorisation names ANOTHER lane's branch - the copy-the-last-envelope case, which the tool's own detail line calls out by name - renders as "correction with no clause found". A clause WAS found; it names the wrong branch. Driven at the production entry point against a real envelope naming writer/F48 under lane F99, the detail line is exactly right and the summary above it is wrong. render() only sets the clause flag when the branch matches, so a control plane reading the summary goes looking for a missing clause that is present.
  TWO. envelopeCheck.mjs has no entry guard. packet.mjs, in the same directory, gates its CLI with a test on process.argv[1]; envelopeCheck does not, so importing ANY export from it runs the live check against the importer's root and calls process.exit, killing the importing script before its own code runs. The F49 reviewer hit this and had to build a stripped copy to call checkEnvelope directly. Nothing in the gate imports it today - the manifest invokes it twice as a subprocess - so this is a foot-gun, not a live failure.
  THREE. landingGate.mjs still says in prose that a shell which cannot run the steps "reds all 21 steps". Since F49 the gate has 22. The count belongs to the manifest, not to a sentence, so say every step rather than a number that the next lane makes false again.

Acceptance
  Four properties. Each gets its own fixture and mutation, and each must red for its property ALONE. If one reds two, one is untested. Paste what every mutation printed and the property it reds for.
  A FIXTURE THAT EXPECTS NO ERROR MUST STILL BE ABLE TO FAIL: F48 shipped one that stayed green when the code discarded its input, because a pass and a never-examined look the same. Assert the positive fact too.
  1 A correction whose clause names another lane's branch renders a SUMMARY naming what was found - a clause for the wrong branch - and not the no-clause verdict. The detail line keeps its current wording.
  2 A correction with NO clause at all still renders the no-clause verdict. The two answers must stay distinguishable in the summary, which is the whole of item one; a fixture that only checks item one lets them collapse the other way.
  3 Importing envelopeCheck.mjs runs no check and does not exit: the fixture imports it and then executes a statement of its own, and that statement running IS the assertion. Removing the guard must red it.
  4 Invoking it as a command still behaves exactly as today - the self-test, the live run and gate step 22 unchanged. This is the ordinary case and it needs a mutation that reds it.

Base
  The commit the work is based on is live main at issue. The worktree is cut from the commit that CARRIES this envelope. Rebase onto live main before push. Push the branch only.

Holds
  factory/tools/envelopeCheck.mjs
  factory/tools/landingGate.mjs
  docs/log/f51.md
  Changed file set equals this list. A stray file is a stop.

Must stay true
  The gate is 22 of 22 at your commit and factory/landing-checks.json is untouched: the step count is not yours to change.
  envelopeCheck still calls no git, needs no repository, and runs no program on the check path.
  Every existing fixture in both files stays green and keeps its meaning. Do not weaken one to make room.
  landingGate's fixtures 1 to 12 keep their tags and their properties; item three is a sentence, not a behaviour.
  A false sentence in the diff is a blocking defect.

Verification
  node factory/tools/envelopeCheck.mjs --self-test
  node factory/tools/envelopeCheck.mjs
  node factory/tools/landingGate.mjs --self-test
  node factory/tools/landingGate.mjs
  Four mutations, one per property, each pasted with its output.

Look hardest at
  Fixing item one by renaming the no-clause verdict instead of adding a third answer. Then the two cases collapse the other way and property 2 catches you - which is why property 2 exists.
  An entry-guard fixture that proves nothing. If the fixture merely imports and the process happens to survive, it cannot fail; the statement after the import must be the assertion, and removing the guard must red it.
  Item three is prose. Do not invent a check for a comment; fix the sentence and say in the log that a reviewer reading it is the only check it has.
  If the gate reds EVERY step at any commit, your bash is the WSL launcher and the red is not yours (T70). Say so in the log, use git-bash, do not spend calls proving it.

Runtime
  desktop-qwen, stamped from factory/project.json (qwen-code, deepseek-flash, worktree isolate). The route is the DeepSeek direct api: writerPath is pinned native (D-67) and the launcher asks route.mjs.
  Writer seat only. Do not remap the control-plane session. Print-mode in the worktree.
  Prefix: AGENTS, BOARD, this envelope, tools — byte-stable, first. Variable work last.

Spend cap
  30 tool calls for three small items in two files. Hitting the cap is a return, not a hang.

Fuse
  Split on envelope done, correction cap, spend cap, or a decay sign (factory/sessions.json). On one: stop, print the signs, return.

Self-check
  Before returning, ask of each fixture whether it COULD have failed, and prove it with the mutation you pasted. Record in docs/log/f51.md what the self-check changed.

Return
  A few lines in docs/log/f51.md: what changed, the measuring command, no commit id of your own.

You run in print mode: the end of your turn is the end of your process, and nothing wakes you. Never end a turn waiting on a background task or a monitor; wait in the foreground with a bounded sleep loop, then print the full return.
