# F40 — both answering commands answer the question they were asked

## What changed

Two holds, against base `758ab12`, rebased onto live main for correction 1:

```
git diff --numstat 758ab12 HEAD -- factory/tools/seat.mjs
78      32      factory/tools/seat.mjs
```

`factory/tools/seat.mjs` — two defects, each fixed in the CLI block that ships, plus correction 1
below.

Item 1 (S17). The census counted a harness token anywhere on a command line, so `pip show
aider-chat` was a seat and a `Get-CimInstance … -- qwen` sweep was a qwen seat. The row rule moved
out of the exported `census()` helper and into the `if (cmd === "census")` block — the object the
gate step invokes, not a helper it reaches (D-61). The row's program is the first token of the
command line, or the first token after an interpreter (`node`, `node.exe`, `python`, `python3`,
`py` — `SEAT_INTERPRETERS`), and `SEAT_HARNESS` is applied to that, never to the whole line: a
harness token later in the line is a mention and is not counted. The old export is gone; the
self-test now drives `census --lister` through the CLI, so census still reads the process table
through the injected lister and reads no file (T04).

Item 2 (S20). `disjoint` answered about the first two lanes, dropped the rest, and exited 0 — a
green covering one pair of three. The `if (cmd === "disjoint")` block now refuses any lane count
but two, names the lanes it was given, and exits 1. The two-lane path, its lines and its exit code
are untouched.

## The two checks

Item 1 — the platform's own process listing:

```
node factory/tools/seat.mjs census
seat census — win32 process listing
  5044  qwen  5044 "C:\Program Files\nodejs\node.exe" …cli-entry.js --auth-type openai --model deepseek-flash --approval-mode yolo -p -
  21024  qwen  21024 "…\node_modules\@qwen-code\qwen-code\…\OpenConsole.exe" --headless --width 80 --height 24 …
  2 running writer seat(s)                                     rc=0
```

The five rows the envelope dictates, driven through the CLI with `--lister`, give three seats: the
qwen path as the program, `goose run`, and the script after an interpreter
(`17 node C:/tools/goose/run.py`). `9001 pip show aider-chat` and
`9002 powershell … Get-CimInstance Win32_Process -- qwen` are mentions and are not counted. The
self-test holds the run to the lister's rows and to `3 running writer seat(s)`, and a decoy pid
`4242` in the fixture's `factory/sessions.json` catches a census that reads a file instead. It also
now asserts each row's harness label — correction 1, item 1.

Item 2 — three lanes, refused, and two unchanged:

```
node factory/tools/seat.mjs disjoint F40 F41 F42
disjoint: compares two lanes and was given 3: F40, F41, F42     rc=1

node factory/tools/seat.mjs disjoint F40 F41
disjoint F40 / F41: 1 and 1 holds, none shared                  rc=0
```

## Correction 1

The review approved both items above at the CLI and found two defects the control plane had missed.

Item 1 — the harness label had no coverage. Every existing census case checked the seat count, that
pids 8324 and `C:/tools/goose/run.py` were present, and that 9001, 9002 and 4242 were absent; none
read the VALUE of a row's `harness` field, so a constant label in the shipping
`if (cmd === "census")` block passed them all. The self-test now reads the label off the CLI's own
rendering, per pid, and asserts qwen, goose and goose for 8324, 4521 and 17. Driven red at the CLI
block by putting the defect back, `harness: m[1].toLowerCase()` → `harness: "goose"`:

```
node factory/tools/seat.mjs --self-test
seat self-test failed
  a row's harness label was wrong: {"3":"running","17":"goose","4521":"goose","8324":"goose"}
rc=1
```

8324's qwen seat is rendered as goose and the failure names the property. Restored byte for byte:
`git hash-object factory/tools/seat.mjs` is `d9a1d0cdbe4b954ee3a617f95bfc1c2a2765c363` before and
after the mutation, and the working tree is clean.

Item 2 — a citation. This log previously said all gate steps were green "including step 13, which
the envelope expected red while the envelope is live". This envelope says nothing about step 13,
nothing about redness and nothing about liveness; the sentence attributed to it a prediction it
never made. The history is real and belongs to F39, which landed the writer Action's opt-in guard
before F40 was issued. T76 does not catch this: T76 checks that a cited path resolves and that a
date field is not ahead of UTC, and a claim a document never makes is outside both.

## Revert proof (T13)

The original two mutations were made in this tree, watched red at the CLI, and reverted byte for
byte; correction 1's mutation was driven the same way, above. Before the first and after the last,
`git hash-object factory/tools/seat.mjs` → `d9a1d0cdbe4b954ee3a617f95bfc1c2a2765c363`, `--self-test`
green, `git diff --numstat 758ab12 HEAD -- factory/tools/seat.mjs` back to `78 32`, and
`git status --porcelain --untracked-files=all` empty.

A — the program-position test put back to the whole line (`SEAT_HARNESS.exec(program)` →
`SEAT_HARNESS.exec(line)`), the mutation the envelope names:

```
seat self-test failed
  a mention was counted as a seat: seat census — injected process listing
  8324  qwen  8324 "C:\node\qwen-code\cli.js" -p -
  4521  goose  4521 goose run --recipe factory/recipes/x.json
  17  goose  17 node C:/tools/goose/run.py --once
  9001  aider  9001 pip show aider-chat
  9002  qwen  9002 powershell -NoProfile -Command Get-CimInstance Win32_Process -- qwen
  5 running writer seat(s)

  a harness named in an argument was reported as a seat: seat census — injected process listing
  … 5 running writer seat(s)
rc=1
```

B — the argument-count refusal dropped, so `rest[3]` is ignored again:

```
seat self-test failed
  a three-lane call was answered as a pair: disjoint F35 / F38: 2 and 2 holds, none shared

  the refused three-lane call did not name all three lanes
rc=1
```

B's first line is the defect itself: three lanes asked, a verdict about the first two printed,
exit 0.

## Landing gate

```
node factory/tools/landingGate.mjs
grok landing gate
  steps    21

  ..  01  Size budgets ... ok
  ..  09  Seat launch: packet on stdin, key from the store, holds, census ... ok
  ..  13  Writer Action idles; never pushes main ... ok
  ..  21  Experiments expire and are honest ... ok

GATE PASSED
```

Elided: the `commit` and `archive` lines, which carry this lane's own commit id, and the seventeen
rows between 01, 09, 13 and 21. All twenty-one steps green — 21 of 21 — with this envelope live,
step 13 included.

An environment note, because it cost a false red first: in this seat `bash` resolved to
`C:\Windows\System32\bash.exe`, the WSL launcher, so every step's `bash -e <script>` failed on the
script path and the gate reported twenty-one reds. With Git's bash on the path
(`C:\PROGRA~1\Git\bin\bash.exe -c "node factory/tools/landingGate.mjs"`) the paste above is what the
gate prints.

Self-check: correction 1's mutation reds the CLI and nothing else does; the row rule, the label read,
the lane-count refusal and the harness-label assertion all live in the command block the gate's step
09 invokes (D-61), and the census assertions read the CLI's rendered listing rather than the source
that produced it; no assertion reads the source it asserts about.
