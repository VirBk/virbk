# F50 — the entry-point fixture runs where the gate runs

## What changed

`factory/tools/landingGate.mjs` only.

- `fixtureDirs().done()` no longer swallows a failed removal: it calls
  `removeTree()`, which retries, REPORTS a removal it cannot finish and
  returns the reason. A throw there would hide the finding the fixture was
  reporting (T79). `rm` and `report` are injectable so a fixture can drive
  that path without waiting for the platform.
- `makeTempRepo()` lays out a throwaway git repository under the OS temp
  dir holding a copy of this tool and `experiment.mjs`, then `git init`,
  one commit, `rev-parse HEAD`. It never throws: a repository it cannot
  make comes back as an `error` string. `spawn` is injectable.
- `fixture7` no longer returns early when there is no repository — that
  early return was S24's hole. It builds its own, spawns the copy inside
  it as the process a person runs, and asserts on that. A repository that
  cannot be created is a FAILED fixture naming why; it never skips.
- `fixture10` (property 4) and `fixture11` (property 5's report clause).

In this file `[N …]` is the FIXTURE number, so the new assertions are
tagged `[7 own copy]`, `[7 cleanup]`, `[10 repo failure]`, `[11 cleanup]`.

## Commands, and what they printed

    $ node factory/tools/landingGate.mjs
    grok landing gate
      steps    21
      ..  15  Gate unpacks on this platform ... ok
      ..  (01-14, 16-21 ok)
    GATE PASSED
    (exit 0)

    $ node factory/tools/landingGate.mjs --self-test          # worktree
    landingGate self-test: could not remove C:\Users\acces\AppData\Local\Temp\grok-gate-f5-cxfTIL — EBUSY: resource busy or locked, rmdir '...\work'
    landingGate self-test ok
    (exit 0)

    $ cd /tmp/f50-bare && node factory/tools/landingGate.mjs --self-test
    # the same file copied into a directory with no .git
    landingGate self-test: could not remove C:\Users\acces\AppData\Local\Temp\grok-gate-f5-V5BFic — EBUSY: resource busy or locked, rmdir '...\work'
    landingGate self-test ok (unpack only; no repository here)
    (exit 0)

Both contexts print ok because fixture 7 now runs in both — that is the
lane. The EBUSY line both contexts print is property 5 in situ: fixture
5's real launcher still holds its cwd, `removeTree` reports it, and the
self-test still passes.

## Six mutations, one per property

Each applied on its own to a copy, `--self-test` run after, then
restored. M1/M2/M4/M5/M5b in a bare directory (the gate's own context),
M3/M6 in the checkout.

    baseline (bare)  landingGate self-test ok (unpack only; no repository here)   exit 0

    M1 the bare context demands a repository again         (property 1)
    ### M1 (bare) exit 1
    landingGate self-test failed
      [1 ordinary] the suite needs a repository

    M2 const stop = null; void shellStop;    (bare)        (property 2)
    ### M2 (bare) exit 1
    landingGate self-test failed
      [7 entry point] the CLI with GATE_SHELL=grok-no-such-shell-f47 came back exit 1, not 2
      [7 entry point] the CLI printed no unrunnable-shell diagnostic
      [7 entry point] the diagnostic did not name the shell GATE_SHELL asked for
      [7 entry point] the diagnostic did not name the cwd it probed in

    M3 const stop = null; void shellStop;    (checkout)    (property 3)
    ### M3 checkout exit=1
    landingGate self-test failed
      [7 entry point] the CLI with GATE_SHELL=grok-no-such-shell-f47 came back exit 1, not 2
      [7 entry point] the CLI printed no unrunnable-shell diagnostic
      [7 entry point] the diagnostic did not name the shell GATE_SHELL asked for
      [7 entry point] the diagnostic did not name the cwd it probed in

    M4 mkdtemp under a drive that is not there   (bare)    (property 4)
    ### M4 (bare) exit 1
    landingGate self-test failed
      [7 entry point] the throwaway repository could not be created — could not create the repository directory: ENOENT: no such file or directory, mkdtemp 'Z:\no-such-drive-f50\grok-gate-f7-repo-XXXXXX'
      [10 repo failure] the reason does not name what failed: could not create the repository directory: ENOENT: no such file or directory, mkdtemp 'Z:\no-such-drive-f50\grok-gate-f10-repo-XXXXXX'

    M5 removeTree becomes a no-op                      (property 5, clause a)
    ### M5 (bare) exit 1
    landingGate self-test failed
      [7 cleanup] the throwaway repository was left behind: C:\Users\acces\AppData\Local\Temp\grok-gate-f7-repo-7KWe3a

    M5b the report line dropped                        (property 5, clause b)
    ### M5b (bare) exit 1
    landingGate self-test failed
      [11 cleanup] a removal that failed was reported nowhere

    M6 the fixture spawns the launch tree's file       (property 6)
    ### M6 checkout exit=1
    landingGate self-test failed
      [7 own copy] the fixture spawned the tree it runs from, not its own copy
      [7 own copy] the child did not resolve the throwaway repository's commit

Each reds its own property's line and nothing else's. M1 is the mutation
that makes property 1 falsifiable: with no repository the self-test must
print ok at exit 0, and this is the change that breaks it.

## Self-check

Driven against a reviewer's list. What it changed:

- The wiring mutations were first run in a "fake repo" holding only the
  two tool files, where the self-test's own archive checks red as well
  (`archive missing factory/landing-checks.json`), so one property looked
  like several. Re-run from the real checkout, each reds its own lines
  only.
- The new tags were first `[4 repo failure]`, `[5 cleanup]`,
  `[6 own input]`, which collide with fixtures 4, 5 and 6 in a file where
  the number is the fixture. Renumbered to `[10 repo failure]`,
  `[7 cleanup]`, `[11 cleanup]`, `[7 own copy]`.
- A skip is the thing this lane exists to delete, so `makeTempRepo`'s
  failure is an `error` and `fixture7` FAILS on it. Property 4's mutation
  is the proof it cannot pass quietly.

## Spend

Tool calls after the context compaction: 26 through the final verification
(22 of them by the first push). The pre-compaction calls were dropped by
the compaction itself and cannot be counted — that share was the fixture
rewrite, the `removeTree`/`makeTempRepo` additions, the mutation driver,
and two mutation rounds. Cap 40.
