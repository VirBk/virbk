# F21

Shallow checkout for the landing gate.

Changed line in `.github/workflows/landing.yml`: the Checkout step `fetch-depth: 0` -> `fetch-depth: 1`. No YAML comment. Triggers, permissions, concurrency, Node version, archive isolation and checks untouched. `landingGate.mjs` resolves and archives the commit under test with `git rev-parse` + `git archive`, so it needs the checked-out HEAD, not its ancestry.

Measured:

```
$ git diff --check
(no output)

$ git diff -- .github/workflows/landing.yml
-          fetch-depth: 0
+          fetch-depth: 1

$ node factory/tools/sizeBudget.mjs
size budget ok

$ node factory/tools/renderBoard.mjs --check
BOARD.md matches factory/board.json

$ node factory/tools/sitting.mjs check
sitting check ok
```

Self-check: changed set equals the Holds (`.github/workflows/landing.yml`, `docs/log/f21.md`); `docs/log/landing-checkout.md` absent; no gate, board, runtime or reference edits. No defect found, nothing changed. Final gate `node factory/tools/landingGate.mjs --sha HEAD` runs on Ubuntu in the PR; not run here.

Model deepseek-v4.1-flash (pc-dashscope). Meter command: qwen --bare --auth-type openai --model deepseek-v4.1-flash --yolo --max-session-turns 12 --max-tool-calls 24 --max-wall-time 10m --chat-recording=false --output-format json (issued envelope on stdin). Final result, pasted by control plane: {"num_turns":9,"usage":{"input_tokens":195138,"output_tokens":2760,"cache_read_input_tokens":167808,"total_tokens":197898}}. Billed USD was not reported.

Control-plane landing: fresh independent review approved the writer return. `node factory/tools/landingGate.mjs --sha HEAD` on Ubuntu reported `steps    13` and `GATE PASSED`; [run 35483615467](https://github.com/VirBk/virbk/actions/runs/35483615467). Checkout used `--no-tags --depth=1`. Tested and landing trees matched; main advanced ff-only without a rebase. Envelope retired, board restamped, sitting closed. No elapsed-time improvement claimed.
