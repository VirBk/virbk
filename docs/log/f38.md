# F38 — a return's model is checked against the meter

Branch `writer/F38`. Seat `pc-dashscope`; the launcher passed `deepseek-v4.1-flash`. Base
`ecf24e8`. Keystone: the "spend pasted in the lane log" step of the dual-runtime loop, made
checkable.

## What changed

`factory/tools/seatReturn.mjs` — `--record` now refuses a return it cannot check.

1. **Recording checks the return's model against the meter (T69).** `--record` requires
   `--project <path>`, reads that project's meter, and requires the return's `model` to agree
   with an id the meter recorded there. Agreement stores that id as `meterModel` beside the
   launcher's claim in `model`; a disagreement refuses and names both. A seat cannot see its
   own model, so claim and measurement stay apart.
2. **No meter is a refusal — not a pass, not a crash.** `readMeter`'s `ok: false` refuses with
   the reason and the rows read and stores nothing; an unmeasured model is never recorded.
3. **Agreement goes through the map that owns the relation (T48, T75).** `sameModel()`
   compares `providerId()` of each side — `route.mjs`'s `resolve()` — so both sides are
   canonicalized and the compare is symmetric. Agreeing pairs:
   `deepseek-flash`/`deepseek-v4.1-flash`, `deepseek-v4-pro`/`deepseek-v4-pro-0813`,
   `qwen3-coder`/`qwen3-coder-plus`; every other id agrees only with itself. The unversioned
   DashScope id is **not** folded: the router never sends `deepseek-v4-pro` to that base, and
   the factory's record shows it from the native base at `grok-f31-rev`, a different price.
4. **The contract gained an optional field.** `contracts/seat-return.v1.json` gained
   `meterModel` (string, minLength 1) as a property only — never required, because a seat
   cannot know which id answered; `--check-contract` refuses a contract that requires it.
5. **The store's shape is checked where the landing looks.** `storeErrors()` checks
   `factory/returns.json` — per row the required fields, no unknown field, a 16-hex key, a
   parseable `at`, non-empty lane/runtime/model, a sha base, and the optional fields' types —
   in `--self-test` (gate step 20) and `--check-contract`, proved on bent copies.

`rowKey` appends `meterModel` only when present, so pre-F38 keys — the F32 row's included —
are untouched, recording twice does not double a row, and the F32 row still validates.

## What the commands printed

    node factory/tools/seatReturn.mjs --self-test
    seatReturn self-test ok                                          exit=0

    node factory/tools/seatReturn.mjs --check-contract
    seat-return contract ok                                          exit=0

    node factory/tools/seatReturn.mjs meter 'C:\Users\acces\virbk-seats\F33'
    source qwen usage_record.jsonl                                   exit=0
    session 2906ae36-3f6d-48be-b72d-49c8180c9dbb
    project C:\Users\acces\virbk-seats\F33
    models deepseek-flash
    requests 58
    inputTokens 3946270
    outputTokens 98823
    cachedTokens 3774592

Against a temp store and a temp meter whose row for `.../proj` records `deepseek-flash`:

    node factory/tools/seatReturn.mjs .../return.json --record --project .../proj
    return ok  F38  pc-dashscope  deepseek-v4.1-flash                 exit=0
    recorded fb8b39faff6dde13  rows 1  meter says deepseek-flash

    ... the same command again
    record refused                                                   exit=1
      already recorded: fb8b39faff6dde13

    .../wrong.json (model qwen3.7-flash), the same project
    record refused                                                   exit=1
      model disagreement: the return says qwen3.7-flash, the meter recorded deepseek-flash

    .../nometer.json (lane F41), project .../f38-meter/nobody
    record refused                                                   exit=1
      no meter for .../nobody: no session in .../usage_record.jsonl has project
      .../nobody (2 rows read)

## The five mutations

Both halves are driven: `wrong.json` is refused with both ids named, and `return.json` against
that row is recorded, `meter says deepseek-flash`; string equality would invert both (m3).

1. **A return whose model disagrees** — refused, exit 1, naming both ids (above). With
   `--project` absent it also refuses: `--record needs --project <path>; a model nobody
   measured is not stored`.
2. **A project with no meter row** — refused, exit 1, naming the file and the rows read;
   nothing stored (above).
3. **`sameModel` by raw string equality** (`return String(a) === String(b)`):

       node factory/tools/seatReturn.mjs --self-test
       seatReturn self-test failed                                  exit=1
         the router's map does not fold deepseek-flash and deepseek-v4.1-flash
         the router's map does not fold deepseek-v4-pro and deepseek-v4-pro-0813
         the router's map does not fold qwen3-coder and qwen3-coder-plus
         a return that agrees with the meter was refused: model disagreement: the
         return says deepseek-v4.1-flash, the meter recorded deepseek-flash
         the store holds 0 rows after one return and one re-record

4. **A required field loosened** — dropping `measured` from `required`, then adding
   `meterModel` to it:

       node factory/tools/seatReturn.mjs --check-contract
       seat-return contract failed                                  exit=1
         contracts/seat-return.v1.json does not require measured

       node factory/tools/seatReturn.mjs --check-contract
       seat-return contract failed                                  exit=1
         contracts/seat-return.v1.json requires meterModel — a seat cannot know it

5. **The stored row's shape changed** — renaming the F32 row's `model` to `modelId` in the
   real `factory/returns.json`:

       node factory/tools/seatReturn.mjs --check-contract
       seat-return contract failed                                  exit=1
         factory/returns.json row 1 has no model
         factory/returns.json row 1 has unknown fields: modelId

   Restored with `git checkout -- factory/returns.json`; `git diff --exit-code` prints nothing
   and the file is absent from this lane's diff. The self-test drives the same mutation on a
   copy of the shipped store, so the red does not depend on bending the real file.

## What the self-check changed

1. The mutation-3 rewrite crashed the self-test with a `TypeError` — a refusal left no row
   while the assertion read `rows[0].tokens`. Now guarded; the test reports the refusal.
2. Two failure messages were false on a zero-row store; they are row counts now, true either
   way.
3. `storeErrors` named a field without saying what was wrong ("row 1 model"). Now sentences
   ("lane is not a lane id"), and the blank-lane branch is asserted — reverting it fails the
   self-test.

## Landing gate

    node factory/tools/landingGate.mjs
    grok landing gate
      steps    21

      ..  01  Size budgets ... ok
      ..  02  Board is generated ... ok
      ..  03  Intakes pass the gate ... ok
      ..  04  PM picks match the catalog ... ok
      ..  05  Scope search names every container ... ok
      ..  06  Owner-ask list is closed ... ok
      ..  07  Sparks TTL and waivers untracked ... ok
      ..  08  Writer path clock and failover ... ok
      ..  09  Seat launch: packet on stdin, key from the store, holds, census ... ok
      ..  10  Sitting is one class of work ... ok
      ..  11  Live sitting class ... ok
      ..  12  Public landing Action stays green ... ok
      ..  13  Writer Action idles; never pushes main ... ok
      ..  14  Kit holds its own shape ... ok
      ..  15  Gate unpacks on this platform ... ok
      ..  16  Reading path shape ... ok
      ..  17  Reading path under its context budget ... ok
      ..  18  Ledger is a window, not a novel ... ok
      ..  19  Alumni scan is read-only and emits valid intakes ... ok
      ..  20  A return pastes the command ... ok
      ..  21  Experiments expire and are honest ... ok

    GATE PASSED

Elided: the `commit` and `archive` lines, carrying this lane's own ids.
