# F36 — a trap names its object

## What changed

Three files, against live main: `git diff --numstat origin/main` → `118  9  factory/tools/packet.mjs`,
`71  0  factory/traps.yaml`, `159  0  docs/log/f36.md`, this log's own row,
moving with every edit to it.

`factory/traps.yaml` — each row gained one line, `object:`, directly after `kind:`. 71 added
lines, nothing removed: no rule text moved or changed. The value is `"*"` (quoted, because a
bare `*` is a YAML alias) or a repository-relative path or glob.

```
node -e 'const fs=require("fs");const t=fs.readFileSync("factory/traps.yaml","utf8");const objs=[...t.matchAll(/^  object: "?(.+?)"?$/gm)].map(m=>m[1]);const star=objs.filter(o=>o==="*").length;console.log("rows",objs.length,"object *",star,"object a path",objs.length-star,"distinct paths",new Set(objs.filter(o=>o!=="*")).size)'

rows 71 object * 52 object a path 19 distinct paths 15
```

Policy, applied to the rule's own text: the first file or tool the rule names, resolved under
`factory/tools/` when the name is bare; `*` when the rule names no repository path, or names
one that is not in the tree (T16 and T17 name `node_modules`, T40 names `factory/.waiver`, T47
names `factory/help.json`; none is tracked, so those take `*`). Nothing is scored on the count.

`factory/tools/packet.mjs` — one shared `parseTraps` now reads a row for the digest, the object
resolver and the self-test, so no two of them can disagree about what the file says (T04);
`ruleNamesACheck` and `firstSentence` compress a row the gate already enforces; `objectResolves`
walks an object — exact path, glob segment, or trailing-slash directory — against the tree.

## The digest

```
node --input-type=module -e 'import {readFileSync} from "node:fs";import {trapsDigest} from "./factory/tools/packet.mjs";const t=readFileSync("factory/traps.yaml","utf8");const whole=trapsDigest(t.replace(/Check is /g,"check is "));const cut=trapsDigest(t);console.log("digest, cut off",whole.length,"digest, cut on",cut.length,"removed",whole.length-cut.length)'

digest, cut off 12261 digest, cut on 10559 removed 1702
```

D-52 estimated "about 2.7 KB" for this cut; digest to digest it is 1702, the earlier 1664 having
subtracted a rows-only join from a whole digest. Fifteen rows qualify,
found by their text and not by a list: T49 T54 T55 T56 T57 T58 T59 T60 T61 T62 T63 T64 T65 T66 T67.

Judged before cutting, and one row is worth a line. None of the fifteen opens its rule with
`Check is `, and none of the fifteen kept sentences is a fragment — all complete sentences, so
the mechanical cut is safe. Three kept sentences read as a headline rather than an instruction:
T55 "Idle is success.", T57 "A search product is not a container.", and T63 "The board is the
board." — in T63 the carrying sentence ("A plan file, a console page, or a README that restates
lane state drifts within a sitting.") is in the cut half. The acceptance names the first
sentence, so all fifteen are cut the same way; T63's index line is the thinnest of them.

No row leaves the digest and no row's rule is edited: the trap count still equals the digest
count, and the digest carries no `object`.

```
node --input-type=module -e 'import {readFileSync} from "node:fs";import {trapsDigest} from "./factory/tools/packet.mjs";const d=trapsDigest(readFileSync("factory/traps.yaml","utf8"));console.log("digest lines carrying an object field:",d.split("\n").filter(l=>/(^|\s)object:/.test(l)).length)'

digest lines carrying an object field: 0
```

## Revert proof (T13)

Each mutation was made in this tree, watched go red, and restored. Restores are byte copies;
after each one, `node factory/tools/packet.mjs --self-test` printed `packet self-test ok — shape
only`, rc=0, and `git diff --numstat` was back to `96 9` and `71 0`.

2a — every `object:` line deleted from `factory/traps.yaml`:

```
=== mutation strip  rc=1
packet self-test failed
  trap T01 has no object
  trap T02 has no object
    total output lines: 72
```

2b — T58's object rewritten to `factory/not-in-the-tree.md`:

```
=== mutation break  rc=1
packet self-test failed
  trap T58 object does not resolve: factory/not-in-the-tree.md
    total output lines: 2
```

3 — the digest compression reverted (`ruleNamesACheck(r.rule) ? firstSentence(r.rule) : r.rule`
→ `r.rule`):

```
=== mutation uncompress  rc=1
packet self-test failed
  digest did not compress T49: "T49 Two classes of work in one control-plane sitting: A sitting is one class of work. helpMode other than none plus a factory envelope (F*.md) is a split. Check is node factory/tools/sitting.mjs check."
  digest did not compress T54: "T54 Landing Action that needs a writer secret: Public landing CI runs landingGate.mjs with no secrets. Check is the landing-checks step Public landing Action stays green."
    total output lines: 16
```

Sixteen lines: the header and one row per compressed trap. `--check` cannot serve as this proof —
the packet sits under its cap with the compression off, so the step stays green — which is why
the assertion holds each digest line to `id + " " + title + ": " + rule` whole, or shorter
with no `Check is ` for the fifteen.
A tree whose traps name no check at all reds the step instead of passing vacuously.

## F31 stands

Nothing here touches the ordering. The packet still emits the envelope part last, and the two
`prefixBytes` assertions for two different lanes are unmodified; the new work adds a field the
digest never emits and narrows what fifteen rows of the traps part contain.

Deferred half (D-58) not built: `trapsDigest` still takes only the text, every lane gets the
same digest, no new parameter and no new gate step.

## Measurements

```
node factory/tools/packet.mjs cost
    10.5 KB  AGENTS.md
     2.8 KB  BOARD (live slice)
    10.3 KB  traps digest
    23.6 KB  seat packet, no envelope
    33.2 KB  control-plane packet
     5.7 KB  factory/envelopes/F36.md
```

The seat packet is under the 25.3 KB it measured at base. `node factory/tools/packet.mjs --check`
→ `packet check ok`, rc=0. `node factory/tools/sizeBudget.mjs` → `size budget ok — none over cap,
6 in the band`: the sixth is this log, over the band since that paste was taken — re-run it, as
step 14 is re-run below — with `90.9%  23.6 / 26.0 KB  seat packet` and `33.8%  16.2 / 48.0 KB
factory/traps.yaml`.

## Landing gate

```
node factory/tools/landingGate.mjs
grok landing gate
  steps    21

  ..  01  Size budgets ... ok
  ..  02  Board is generated ... ok
  ..  03  Intakes pass the gate ... ok
  ..  04  PM picks match the catalog ... ok
  ..  05  Scope search names every container ... ok
  ..  06  Owner-ask list is closed ... ok
  ..  07  Sparks TTL and waivers untracked ... ok
  ..  08  Writer path clock and failover ... ok
  ..  09  Hosted DashScope model map ... ok
  ..  10  Sitting is one class of work ... ok
  ..  11  Live sitting class ... ok
  ..  12  Public landing Action stays green ... ok
  ..  13  Writer Action idles; never pushes main ... FAILED
  ..  14  Kit holds its own shape ... ok
  ..  15  Gate unpacks on this platform ... ok
  ..  16  Reading path shape ... ok
  ..  17  Reading path under its context budget ... ok
  ..  18  Ledger is a window, not a novel ... ok
  ..  19  Alumni scan is read-only and emits valid intakes ... ok
  ..  20  A return pastes the command ... ok
  ..  21  Experiments expire and are honest ... ok

GATE FAILED
  Writer Action idles; never pushes main exit 1
```

Twenty of twenty-one steps green. Step 13 is the red one the envelope names as expected while
this envelope is live; nothing else is red. Step 14 is green now that this log is in the tree —
it fails when the board names a lane log that is not there. Elided from the paste: the `commit`
line and the temp `archive` path, both carrying this lane's own commit id, which the rebase
rewrites. Re-run after this line was added: the same twenty green and the same single red.

Self-check: both compress mutations and the star sweep red the new assertions; one more false
line, the revert proof's named comparison, is corrected above, and `factory/traps.yaml` was not
edited.
