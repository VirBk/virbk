# F37 — the seat launch is checked

## What changed

Three holds plus this log, against live main `9beb58b`:

```
git diff --numstat
1       1       factory/landing-checks.json
368     32      factory/tools/seat.mjs
12      0       factory/traps.yaml
```

`factory/tools/seat.mjs` — every recipe naming `packet.mjs` now prints one stdin carrier built by
`packetStdin(harness)`, so none hands the packet over as an argv string (T72); both Windows key
lines read the user store in the same act as the launch (T73). New:
`disjoint <LANE> <LANE>`, `census [--lister <command>]`, and `--root <tree>`, which lets the
self-test drive a command against a fixture tree a mutant cannot agree with. Holds are read from
`factory/board.json` (T04). The census reads no file: the lister is an argument, and the entry
point passes the platform's own listing. No model, no key (T29).

`factory/landing-checks.json` — step 9 renamed from `Hosted DashScope model map` to `Seat launch:
packet on stdin, key from the store, holds, census`. Same run, no step added, still 21 steps.

`factory/traps.yaml` — T72 and T73 appended, `kind: machine`, `object: "factory/tools/seat.mjs"`,
each rule ending in its check (T24): twelve insertions, no deletions, no stored rule edited and no
row moved. Base text `git show HEAD:factory/traps.yaml`, live text the tree, same `trapsDigest`:

```
node --input-type=module -e 'import {readFileSync} from "node:fs";import {execSync} from "node:child_process";import {trapsDigest} from "./factory/tools/packet.mjs";const b=execSync("git show HEAD:factory/traps.yaml",{encoding:"utf8"});const n=readFileSync("factory/traps.yaml","utf8");const B=trapsDigest(b),N=trapsDigest(n);console.log("traps.yaml bytes  base",Buffer.byteLength(b,"utf8"),"now",Buffer.byteLength(n,"utf8"));console.log("digest bytes      base",Buffer.byteLength(B,"utf8"),"now",Buffer.byteLength(N,"utf8"));console.log("digest rows       base",B.split("\n").filter(Boolean).length,"now",N.split("\n").filter(Boolean).length);'

traps.yaml bytes  base 16598 now 17377
digest bytes      base 10563 now 11094
digest rows       base 72 now 74
```

## The four checks

Item 1 — the rendered recipe, not the source that wrote it:

```
node factory/tools/seat.mjs recipe qwen-code
#   node factory/tools/packet.mjs seat <LANE> > packet.txt
#   qwen --auth-type openai --model <id> -p - < packet.txt
```

Item 2 — the same recipe's Windows block:

```
#   $env:OPENAI_BASE_URL="https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
#   $env:OPENAI_API_KEY=[Environment]::GetEnvironmentVariable("DASHSCOPE_API_KEY","User")
#   qwen --auth-type openai --model deepseek-v4.1-flash
```

Item 3 — the disjoint pair the envelope names, and the overlap that holds F39 back:

```
node factory/tools/seat.mjs disjoint F35 F38
disjoint F35 / F38: 2 and 2 holds, none shared            rc=0

node factory/tools/seat.mjs disjoint F37 F39
overlap F37 / F39: factory/traps.yaml                     rc=1
```

Item 4 — the platform's own process listing:

```
node factory/tools/seat.mjs census
seat census — win32 process listing
  9476  qwen  9476 "C:\Program Files\Git\bin\bash.exe" -c "… < F37-packet.txt …
  …                    (six more rows: the harness token, then its command line)
  7 running writer seat(s)                                rc=0
```

The self-test plants `factory/sessions.json` with pid 4242 in the fixture and holds the CLI run to
the lister's three rows, so a census reading a file reports a row no lister emitted.

## Revert proof (T13)

Each mutation was made in this tree, watched red, and reverted byte for byte: after the fourth,
`git hash-object factory/tools/seat.mjs` → `a1f2f7b2cb9a3c4241427826521fdcbb58ead47d`, the blob
from before the first, `git diff --numstat` back to `1 1`, `368 32`, `12 0`, self-test green.

A — the argv form put back into one recipe (`-p -` → `-p "$(cat packet.txt)"`):

```
seat self-test failed
  a recipe hands the packet over as an argv substitution: qwen-code desktop-qwen
  qwen-code carrier: got "#   qwen --auth-type openai --model <id> -p \"$(cat packet.txt)\" < packet.txt" want "#   qwen --auth-type openai --model <id> -p - < packet.txt"
rc=1
```

B — one Windows line back to a bare `$env:` read (`$env:OPENAI_API_KEY=$env:DASHSCOPE_API_KEY`):

```
seat self-test failed
  a Windows line takes the key from a bare $env:: #   $env:OPENAI_API_KEY=$env:DASHSCOPE_API_KEY #   $env:OPENAI_API_KEY=$env:DASHSCOPE_API_KEY
rc=1
```

Both lines name the alias too, because `desktop-qwen` is the same recipe object as `qwen-code`.

C — `disjoint` returns 0 unconditionally (`process.exit(report.status)` → `process.exit(0)`):

```
seat self-test failed
  two lanes sharing factory/traps.yaml read as disjoint
  a glob and a path the glob matches read as disjoint
  * read as disjoint from a named path
  a lane with no holds read as a proven pair
  a lane missing from the board read as a proven pair
  a tree with no board read as a proven pair
rc=1
```

Six, not one: the proof runs through the CLI against a fixture board, so a mutant in the exit
path, the loader, the glob comparison or the empty-hold guard all lands here.

D — the census reads a JSON file instead of its lister (`lister()` →
`readFileSync(join(root,"factory","sessions.json"))`):

```
seat self-test failed
  census rows: got [] want [{"pid":8324,…},{"pid":4521,…},{"pid":17,…}]
  census did not report the lister's first row: seat census — injected process listing
  0 running writer seat(s)
  census did not report the lister's whole listing: …
rc=1
```

## Measurements

```
node factory/tools/packet.mjs cost
    10.5 KB  AGENTS.md
     3.2 KB  BOARD (live slice)
    10.8 KB  traps digest
    24.6 KB  seat packet, no envelope
    34.2 KB  control-plane packet
     5.9 KB  factory/envelopes/F37.md
```

`node factory/tools/packet.mjs --check` → `packet check ok`, rc=0. `sizeBudget.mjs` → `size budget
ok — none over cap, 7 in the band`, with `94.7%  24.6 / 26.0 KB  seat packet` and this log the
seventh at `98.9%  7.9 / 8.0 KB`. The seat packet is 1.4 KB under its cap; the two new rows add
531 bytes of digest.

## Landing gate

```
node factory/tools/landingGate.mjs
grok landing gate
  steps    21

  ..  01  Size budgets ... ok
  ..  02  Board is generated ... ok
  ..  03  Intakes pass the gate ... ok
  ..  04  PM picks match the catalog ... ok
  ..  05  Scope search names every container ... ok
  ..  06  Owner-ask list is closed ... ok
  ..  07  Sparks TTL and waivers untracked ... ok
  ..  08  Writer path clock and failover ... ok
  ..  09  Seat launch: packet on stdin, key from the store, holds, census ... ok
  ..  10  Sitting is one class of work ... ok
  ..  11  Live sitting class ... ok
  ..  12  Public landing Action stays green ... ok
  ..  13  Writer Action idles; never pushes main ... FAILED
  ..  14  Kit holds its own shape ... ok
  ..  15  Gate unpacks on this platform ... ok
  ..  16  Reading path shape ... ok
  ..  17  Reading path under its context budget ... ok
  ..  18  Ledger is a window, not a novel ... ok
  ..  19  Alumni scan is read-only and emits valid intakes ... ok
  ..  20  A return pastes the command ... ok
  ..  21  Experiments expire and are honest ... ok

GATE FAILED
  Writer Action idles; never pushes main exit 1
```

Twenty of twenty-one steps green, step 09 now running the new self-test under its new name. Step 13
is the red one the envelope names as expected while this envelope is live. Elided from the paste:
the `commit` line and the two `archive` lines, each carrying this lane's own commit id, which the
rebase rewrites. Re-run at the commit carrying this line: the same twenty green and the same single
red.

Self-check: the four named mutations red the four checks and nothing else does; the vacuity guards
(a recipe list that came back empty, a zero-length key-line set, the empty-hold and missing-lane
exits, the decoy pid) are there because a rule that passes by finding nothing is the defect D-59
names; no assertion reads the source that produced what it asserts.
