# F22

Kit holds its own shape.

Changed: `factory/tools/kitCheck.mjs` (new, seven rules, each one a live defect it found), the
gate step `Kit holds its own shape`, `factory/packages.json` (was not valid JSON), the F15 hold
that named a file nobody wrote, `factory/lineage.json` parent, and the parent name in live law.
`factory/tools/hands.mjs` reads the parent from lineage instead of matching a literal.

Measured, first run against the tree as it stood:

```
$ node factory/tools/kitCheck.mjs
kit check failed
  A factory/packages.json does not parse: Expected ',' or ']' after array element in JSON at position 1767 (line 23 column 5)
  C lane F15 holds docs/log/f15.md, which is not in the tree
  D lineage parent VirBk/Grok is not the board remote VirBk/virbk
  E factory/CONSOLE.md:47 restates lane state; the board is the board
  E factory/CONSOLE.md:87 restates lane state; the board is the board
  E factory/CONSOLE.md:89 restates lane state; the board is the board
  F landing step names landingGate.mjs, which is not in the tree
  G factory/tools/kitCheck.mjs is named by no gate step
  G factory/tools/landingGate.mjs is named by no gate step
```

The F line was the check being wrong, not the tree: a bare filename inside a grep pattern is not
a path. Fixed in the same file; a path now has to contain a slash.

```
$ node factory/tools/kitCheck.mjs
kit check ok

$ node factory/tools/kitCheck.mjs --self-test
kitCheck self-test ok

$ node factory/tools/hands.mjs --self-test
self-test ok
```

Self-check changed: the F rule, after it produced a false positive on its own first run.
The E rule excludes `docs/`, the board, the register and the drops: those are history and pastes,
not live law.

Traps T59, T63. Decisions D-41, D-42.
