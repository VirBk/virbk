# F39 — the cloud writer takes the router, and idles loudly

## What changed

Four holds plus this log, against live main `9934c6c`:

```
git diff --numstat
3       0       .github/workflows/writer.yml
265     43      factory/tools/ghaWriter.mjs
73      16      factory/tools/route.mjs
12      0       factory/traps.yaml
```

`writer.yml` — the `seat` job's first line is `if: vars.CLOUD_WRITER == 'on'`: unset is idle and idle
is a success, so a secret that was never placed cannot read as a broken Action (T55). No writer secret
in `landing.yml`; no `git push` here.

`route.mjs` (the CP hold this lane runs alone) — the answer gains `providerModel`, the id the answered
base expects, mapped on the dashscope path only, unknown or already-hosted passing through (T48).
`resolve` is exported and `resolveNow(env)` is the whole resolve branch; the CLI is one caller of it
behind an `isEntry` test. Same picks, same path, reason, base and key NAME.

`ghaWriter.mjs` — `HOSTED`, `hostedModel` and `hasWriterSecret` are gone. `run` asks `resolveNow()`
once and takes base, key NAME and model from that one answer (T29), counting the key its own call
passes. A missing key prints two `skip loudly:` lines and exits 0. The prompt goes on stdin, `-p -`
(T72), and `assertNotMain` still guards a `refs/heads/writer/<lane>` push.

`traps.yaml` — T74 (`.github/workflows/writer.yml`) and T75 (`factory/tools/ghaWriter.mjs`) appended,
each rule ending in its own check (T24); twelve insertions, no deletion, `sparks.json` untouched.

## Item 1 — the guard is evaluated, not searched for

The job-level `if:` is read out of the committed YAML and run, not searched for as text:

```
node <harness>/guard.mjs
writer.yml job guard: vars.CLOUD_WRITER == 'on'
  vars {}                     -> launches: false
  vars {"CLOUD_WRITER":""}    -> launches: false
  vars {"CLOUD_WRITER":"off"} -> launches: false
  vars {"CLOUD_WRITER":"on"}  -> launches: true
rc=0
```

`--self-test` runs the same `new Function("vars", …)` idiom, an unevaluable guard pushed as an error;
M1 deletes the condition.

## Items 2, 3 and 5 — driven through the runner's own CLI

`--self-test` drives `run`, `run --dry-run` and `watch` in a fixture tree: clock pinned off-peak,
`ROUTE_NATIVE` pinned, every writer key deleted, so no probe and no live pick moves the answer (T04).
Five reaches: native plan, hosted plan, native skip, hosted skip, and a DashScope key on the native
path, which must not plan.

Item 5's carrier, launch elided by `--dry-run` (`prompt` dropped by the pipe):

```
DEEPSEEK_API_KEY=sk-not-a-real-key ROUTE_NATIVE=up node factory/tools/ghaWriter.mjs run --dry-run
{ "lane": "F39", "branch": "writer/F39", "path": "native", "reason": "off-peak",
  "base": "https://api.deepseek.com/v1", "keyName": "DEEPSEEK_API_KEY", "keyPresent": true,
  "model": "deepseek-flash", "command": "qwen",
  "args": ["--auth-type","openai","--model","deepseek-flash","--yolo","-p","-"],
  "argvBytes": 53, "stdinBytes": 31613 }
```

53 bytes of argv against 31613 of stdin, `-p -` last. The self-test asserts the same at the child: the
element after `-p` is `-`, the child's stdin is exactly the envelope's bytes, no argv element is longer
than a flag, and the key value never appears in the plan. Items 2 and 3 ride that drive.

## The five mutations (T13)

Scratch tree = `git ls-files | grep -v '^factory/envelopes/'`, 110 files, so only the lane's own check
can go red; pristine restore before each.

```
bash <harness>/run.sh
scratch files: 110
=== baseline (no mutation)
self-test ok
rc=0
=== M1  delete the guard
self-test failed
  writer.yml launches the cloud seat with no guard; unset must stay idle
rc=1
=== M2  hardcode DashScope and keep a private id map in launchSpec
self-test failed
  native plan base https://dashscope-intl.aliyuncs.com/compatible-mode/v1 want https://api.deepseek.com/v1
  native plan model deepseek-v4.1-flash want the native id deepseek-flash
rc=1
=== M3  skip decision counts DASHSCOPE while the native call passes DEEPSEEK
self-test failed
  native plan: run --dry-run printed no plan: "skip loudly: writer secret missing: DEEPSEEK_API_KEY for path native (off-peak)…
  a DashScope key launched a native seat: run --dry-run printed a plan
rc=1
=== M4  a secret that was never placed fails the job
self-test failed
  a missing writer key must not fail the job: rc 1
  a missing writer key must not fail the job: rc 1
  the wrong-key skip must exit 0: rc 1
rc=1
=== M5  the packet handed over as one argv string
self-test failed
  the prompt carrier is not stdin: ["--auth-type","openai","--model","deepseek-flash","--yolo","-p","# ENVELOPE: F99 — fixture lane…
  the plan carries 4637 bytes of argv
  an argv element is longer than a flag: # ENVELOPE: F99 — fixture lane
rc=1
=== restored
```

M4 names three lines: the skip is reached from two fixtures, and the wrong-key reach is a separate
exit. Each mutation reds its own item's check; the baseline is green in the same tree.

## Item 4 in the tree

The lane's envelope is live, so `run` reaches the secret check with no fixture, keys cleared:

```
env -u DEEPSEEK_API_KEY -u DASHSCOPE_API_KEY -u OPENAI_API_KEY ROUTE_NATIVE=up node factory/tools/ghaWriter.mjs run
skip loudly: writer secret missing: DEEPSEEK_API_KEY for path native (off-peak)
skip loudly: nothing was launched and this is not a job failure (T55)
rc=0
```

## Measurements

```
ROUTE_NATIVE=up node factory/tools/route.mjs
family  deepseek
model  deepseek-flash
providerModel  deepseek-flash
path  native
reason  off-peak
base  https://api.deepseek.com/v1
key  DEEPSEEK_API_KEY
rc=0

node factory/tools/ghaWriter.mjs watch    → issued F39                          rc=0
node factory/tools/route.mjs --self-test  → self-test ok                        rc=0
node factory/tools/packet.mjs --self-test → packet self-test ok — shape only    rc=0
node factory/tools/packet.mjs --check     → packet check ok                     rc=0
node factory/tools/sizeBudget.mjs         → none over cap, 8 in the band        rc=0
```

The two new objects resolve in the tree (the packet's shape check); seat packet 25.0 / 26.0 KB.

## Landing gate

```
node factory/tools/landingGate.mjs
grok landing gate
  steps    21
  ..  01  Size budgets ... ok
  ..  02  Board is generated ... ok
  ..  03  Intakes pass the gate ... ok
  ..  04  PM picks match the catalog ... ok
  ..  05  Scope search names every container ... ok
  ..  06  Owner-ask list is closed ... ok
  ..  07  Sparks TTL and waivers untracked ... ok
  ..  08  Writer path clock and failover ... ok
  ..  09  Seat launch: packet on stdin, key from the store, holds, census ... ok
  ..  10  Sitting is one class of work ... ok
  ..  11  Live sitting class ... ok
  ..  12  Public landing Action stays green ... ok
  ..  13  Writer Action idles; never pushes main ... FAILED
  ..  14  Kit holds its own shape ... ok
  ..  15  Gate unpacks on this platform ... ok
  ..  16  Reading path shape ... ok
  ..  17  Reading path under its context budget ... ok
  ..  18  Ledger is a window, not a novel ... ok
  ..  19  Alumni scan is read-only and emits valid intakes ... ok
  ..  20  A return pastes the command ... ok
  ..  21  Experiments expire and are honest ... ok

GATE FAILED
  Writer Action idles; never pushes main exit 1
```

Elided: the `commit` and `archive` lines, carrying this lane's own ids. Step 13 is the expected red
while this envelope is live — the gate runs the self-test in its own archive, where live
`factory/envelopes/F39.md` makes `watch` print `issued F39`, not `idle`. In the tree that red is:

```
node factory/tools/ghaWriter.mjs --self-test
self-test failed
  live envelopes present; watch would not be idle
  watch should print idle, got "issued F39\n"
rc=1
```

## Self-check

Drove the reviewer's list: the guard is a condition, evaluated and broken on purpose in M1, not a text
twin; a skip is loud and exits 0; the plan's fields, argv shape and stdin bytes come from a real child
run, not from the source that produced them; `assertNotMain` guards a `refs/heads/writer/<lane>` push;
one `resolveNow` answers cloud and desktop alike.
