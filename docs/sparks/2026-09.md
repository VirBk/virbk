# Sparks 2026-09

Archived from factory/sparks.json. History, not the reading path. One JSON line per spark, in the order the file held them. Rotation is not reversible: this file holds the closed rows, the live file the open ones, and neither rebuilds the other.

{"id":"S01","kind":"trap","claim":"A control-plane sitting is one class of work; a second class is a split.","object":"factory/sessions.json","whyNotLaw":"No check yet that a multi-repo transcript fails a sitting.","status":"absorbed","openedOn":1,"absorbedAs":"T49"}
{"id":"S02","kind":"lesson","claim":"A wall-clock is not a control-plane fuse.","object":"factory/sessions.json","whyNotLaw":"Named decay signs already cover rot; hours would be an invented clock.","status":"dropped","openedOn":1,"because":"T25 already names decay signs. A wall-clock would be a second clock."}
{"id":"S03","kind":"trap","claim":"A hosted or private-index code search that returns zero is not a ZERO.","object":"factory/tools/scopeSearch.mjs","whyNotLaw":"The search product is not a container. GitHub search of Virbos was empty; the objects were not.","status":"absorbed","openedOn":2,"absorbedAs":"T57"}
{"id":"S04","kind":"trap","claim":"A control plane that will issue or drop reads factory/CONTROL_PLANE.md.","object":"factory/sessions.json","whyNotLaw":"successorReads names sparks but not CONTROL_PLANE.md; CONSOLE does. Drift, not yet a check.","status":"absorbed","openedOn":2,"absorbedAs":"T58"}
{"id":"S05","kind":"lesson","claim":"An owner paste of a missed spark is a lead, not law.","object":"factory/CONTROL_PLANE.md","whyNotLaw":"No check that a successor treated a chat paste as AGENTS.","status":"dropped","openedOn":2,"because":"T50 already rules that the repository is the record and a fact from chat or notes is a diary; a second row would be the same law twice."}
{"id":"S06","kind":"trap","claim":"A session that confirms it understands still invents and leaves the loop; reading files or alumni is not a hold.","object":"factory/sessions.json","whyNotLaw":"Landing checks do not read chat. No command fails a spoken confirmation. Until a return must paste a command or omit, absorbing this is a diary.","status":"absorbed","openedOn":2,"absorbedAs":"T56"}
{"id":"S07","kind":"trap","claim":"A return validates spendUsd and cachedTokens and then discards the row, so no command sums spend or cache hits across lanes.","object":"factory/tools/seatReturn.mjs","whyNotLaw":"Returns so far are prose in lane logs; a store with no rows is a fixture, and the cap it would enforce has no dated row in factory/decisions.json.","status":"absorbed","openedOn":5,"absorbedAs":"T65"}
{"id":"S08","kind":"trap","claim":"A seat with shell access acts to the limit of the instructions it can read, not the limit of its envelope Holds; a session launched in the control-plane checkout has no Holds at all and will land and push.","object":"factory/templates/ENVELOPE.md","whyNotLaw":"The control plane compares the changed file set to Holds only at the return, after the seat has already acted. No command bounds a running seat, and no check names where a seat may be launched.","status":"absorbed","openedOn":8,"absorbedAs":"T68"}
{"id":"S09","kind":"trap","claim":"The spend and cache numbers a return requires already exist in the harness meter, but the seat cannot see them, so spendUsd is self-reported as zero or omitted and factory/returns.json stays empty.","object":"factory/tools/seatReturn.mjs","whyNotLaw":"No command reads a harness meter into a return. Five seat runs produced zero rows, and the reading is the control plane's work, not the seat's.","status":"absorbed","openedOn":8,"absorbedAs":"T67"}
{"id":"S10","kind":"trap","claim":"A seat echoes the envelope Runtime stamp as its model: two seats reported deepseek-v4.1-flash while one was in fact running deepseek-flash, so a return's model field records the claim, not the run.","object":"contracts/seat-return.v1.json","whyNotLaw":"Nothing records the model the launcher passed, so no check can compare a return's model against it. Until the launcher's pick is written down, absorbing this is a diary.","status":"absorbed","openedOn":8,"absorbedAs":"T69"}
{"id":"S11","kind":"trap","claim":"The control plane hands the owner commands in a shell dialect the owner's terminal rejects: PowerShell 5.1 has no && and the parent traps.yaml has no row for the host shell, though Otto's TRAPS.md has carried one since 2026-09.","object":"factory/traps.yaml","whyNotLaw":"No command reads what the control plane types at the owner. The kit's own gate steps run through bash from a file and seat.mjs already prints a Windows variant, so the checkable surface is not obviously the one that failed.","status":"absorbed","openedOn":8,"absorbedAs":"T70"}
{"id":"S12","kind":"trap","claim":"alumni.mjs scan measures structure and never opens a child's TRAPS.md, so a lesson a child paid for and wrote down in prose cannot reach the parent by measurement.","object":"factory/tools/alumni.mjs","whyNotLaw":"The scan found three structural signals on Otto and missed the shell trap sitting in line 7 of its TRAPS.md. What a portable intake of a prose trap would check is not yet named.","status":"absorbed","openedOn":8,"absorbedAs":"T71"}
{"id":"S13","kind":"trap","claim":"The qwen-code seat recipe passes the packet as an argv string, and on Windows a 33 KB packet exceeds the command-line limit: the seat dies with 'Argument list too long' before it reads a byte.","object":"factory/tools/seat.mjs","whyNotLaw":"Observed once on F33's launch. The fix is stdin rather than argv, but no check yet proves a recipe can carry a packet of cap size on the platform the control plane sits on.","status":"absorbed","openedOn":10,"absorbedAs":"T72"}
{"id":"S14","kind":"trap","claim":"An issuance commit pushed to main without a skip-ci marker turns the gate red and launches the GHA writer on the lane just issued, onto the same branch a local worktree already holds.","object":".github/workflows/writer.yml","whyNotLaw":"On F33 the cloud seat started headless with yolo on writer/F33 and was stopped only by a 401 on a stale DashScope secret. The F31 sitting knew this and wrote it as a ledger sentence, not a check.","status":"absorbed","openedOn":10,"absorbedAs":"T74"}
{"id":"S15","kind":"trap","claim":"A router exists and one caller ignores it: ghaWriter.mjs hardcodes the DashScope base URL and maps the model through its own HOSTED table, so the cloud seat cannot take the native path route.mjs resolves for the local seat.","object":"factory/tools/ghaWriter.mjs","whyNotLaw":"It also counts DEEPSEEK_API_KEY as having a writer secret but never passes it to the call, so a repo holding only that secret stops idling and then fails on an empty key, which is the loud skip T55 requires turned into a loud failure.","status":"absorbed","openedOn":10,"absorbedAs":"T75"}
{"id":"S16","kind":"trap","claim":"A writer key read from a process environment is a value carried from an earlier act, not read from the store that owns it: after a rotation the User scope answers 200 and every already-open shell keeps answering 401.","object":"factory/tools/seat.mjs","whyNotLaw":"Cost this sitting two wrong diagnoses and nearly a handover command that could not work. T04 names the shape and no recipe reads the key from its store in the same act as the launch.","status":"absorbed","openedOn":10,"absorbedAs":"T73"}
{"id":"S17","kind":"trap","claim":"The seat census matches any command line containing qwen, aider, goose or opencode as a word, so an unrelated process reads as a running writer seat.","object":"factory/tools/seat.mjs","whyNotLaw":"Caught live during F37's review: the reviewer's own 'pip show aider-chat' was reported as two running aider seats. A census that over-reports is a census that gets ignored, and no check distinguishes a harness process from a mention of one.","status":"absorbed","openedOn":10,"absorbedAs":"T77"}
{"id":"S18","kind":"trap","claim":"A seat cited evidence that does not exist: a lane log named a git ref as the source of a price claim, and the ref appears nowhere in the repository except the sentence claiming it.","object":"docs/log","whyNotLaw":"The underlying claim was true and derivable from route.mjs, so a reviewer reading for plausibility passes it. T02 covers a copied number and T56 a missing command; neither covers an invented reference, and nothing checks that a ref or path a log cites resolves.","status":"absorbed","openedOn":10,"absorbedAs":"T76"}
{"id":"S19","kind":"trap","claim":"Half a fix can ship untested when one mutation reds for the other half's reason: splitCheck is case-sensitive AND last-wins, and only case-sensitivity has a fixture.","object":"factory/tools/alumni.mjs","whyNotLaw":"Restoring first-match while keeping case-sensitivity leaves alumni --self-test green, because no fixture row carries two capital-C directives. The envelope named one mutation combining both halves, and it reds on the first half alone.","status":"absorbed","openedOn":10,"absorbedAs":"T79"}
{"id":"S20","kind":"trap","claim":"seat.mjs disjoint is pairwise and silently drops every argument past the second: 'disjoint F40 F41 F42' prints a verdict about F40/F41 and exits 0, so a control plane proving three siblings gets a green that covers one pair of the three.","object":"factory/tools/seat.mjs","whyNotLaw":"Caught on the first act of sitting 11, by a control plane running the command the board's Now line cites for a mutual-disjointness claim about three lanes. The claim is true here - all three pairs were run by hand and each printed none shared - but the named command cannot mean what the sentence says. No check drives an extra argument, and the tool's own doc comment says 'disjoint <LANE> <LANE>' while the CLI accepts more without complaint.","status":"absorbed","openedOn":11,"absorbedAs":"T78"}
{"id":"S22","kind":"trap","claim":"The census counts processes, not seats: two qwen-code launches produced six rows, a cli-entry wrapper plus --expose-gc children each, and it printed 6 running writer seat(s) when the true number was two.","object":"factory/tools/seat.mjs","whyNotLaw":"Observed live when sitting 11's two siblings were launched, with the real number known. F40 does NOT fix this: that lane puts the harness token in program position, and every one of the six rows has it there, so the count stays wrong by the process tree after F40 lands. The two-writer ceiling and the dead-seat resume both read this number. A seat is a process group, or the row a launch recorded, not a matching command line.","status":"absorbed","openedOn":11,"absorbedAs":"T77"}
{"id":"S23","kind":"trap","claim":"A correction envelope is written to main, but a seat reads its envelope from its own worktree, which still holds the superseded page; git fetch does not touch the working tree, so the seat runs the old envelope silently.","object":"factory/tools/seat.mjs","whyNotLaw":"Caught live this sitting by reading the envelope's first line after launching: the F42 correction seat was started against the original envelope, stopped, and relaunched only after the worktree was rebased onto main. packet.mjs reads factory/envelopes/<LANE>.md from the tree it runs in, so the failure is invisible and looks like a seat ignoring its instructions. Nothing in seat.mjs, the recipe or the packet warns that a correction launch must rebase the worktree first, and the cost is a whole paid session spent against a stale page.","status":"absorbed","openedOn":11,"absorbedAs":"T81"}
{"id":"S24","kind":"trap","claim":"T70 says gate steps run through bash from a file and are unaffected by shell dialect. On this machine that is false: a seat whose bash resolves to the WSL launcher gets GATE FAILED on all 21 steps, at any commit.","object":"factory/traps.yaml","whyNotLaw":"Hit by two seats this sitting, F40 and F43, each spending calls proving the red was not its own; the F43 seat ran a control at live main's tip, got the same 21 reds and correctly handed classification to the control plane. The same gate passes 21 of 21 from git-bash at the same commit. A trap that states a hazard does not apply is worse than no trap, because a seat reads it and trusts it, and the gate prints step names rather than stderr so the cause is invisible from inside a seat.","status":"absorbed","openedOn":11,"absorbedAs":"T70"}
{"id":"S25","kind":"trap","claim":"T76 reads any backticked token with a slash and a source extension as a TREE PATH, so a git revision spec like HEAD:factory/tools/packet.mjs fails the gate although it resolves - git rev-parse prints its blob.","object":"factory/tools/kitCheck.mjs","whyNotLaw":"It blocked F45 at landing on a record whose claim was true and whose citation resolved, costing a control-plane documentation fix because the lane's one correction was already spent. T76 was written this sitting to catch invented evidence and it is now rejecting real evidence in a form the factory uses often, since a lane log pinning a blob is exactly the kind of citation T76 wants. A path citation and a rev spec are different objects and the rule cannot tell them apart.","status":"absorbed","openedOn":11,"absorbedAs":"T76"}
{"id":"S27","kind":"trap","claim":"The seat packet is at 99.9 per cent of cap with four trap rows due, but D-58's trigger has NOT fired: AGENTS sections 4 and 8 still restate what the digest carries in the same packet - T01, T02, T03, T25, T26, T27, T39 - about 700 bytes.","object":"AGENTS.md","whyNotLaw":"This is the shed D-58 says to make BEFORE firing its trigger, found by the sixth board-prose cap fight in two sittings. It was not taken on the spot for a reason worth recording: two seats were running against a byte-stable prefix that carried 96.8 per cent of input on cache last sitting, and rewriting AGENTS mid-flight busts that prefix for both seats and for any correction round. Order: land F47 and F48, shed second, absorb third. What keeps it a spark is that the shed and the four rows roughly cancel. If the packet is back over 90 with nothing left to cut, D-58's trigger HAS fired and scoping becomes a lane that also owns F31's byte-stable assertion and must price the prefix change at the measured cache rates.","status":"dropped","openedOn":12,"because":"Taken, at the moment S27 itself named: F47 and F48 landed and absorbed, no seat was running, so rewriting AGENTS cost no seat its byte-stable prefix. AGENTS.md went 10792 to 10374 bytes and the seat packet 100.4 to 98.8 per cent of cap, both from node factory/tools/sizeBudget.mjs. What was shed was only what the traps digest carries in the SAME packet: the copied-counts line, which is T02 word for word, is gone; edit-in-place and no-SESSION_LOG point at T03 and T26; archive at 80 per cent points at T01; the session-is-a-fuse and reviewer-never-resumed sentences point at T25 and T27; the eureka-is-a-spark sentence, which is T39, is gone. No rule was weakened and none was moved to a file a seat does not read. D-58's trigger has therefore STILL not fired, and the next control plane that finds the packet over 90 per cent should look for duplicate before it books per-lane scoping. No new trap: gate step 17, reading path under its context budget, already fails a packet over cap, which is the check T24 asks for."}
