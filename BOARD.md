# BOARD

Generated from `factory/board.json`. Do not hand-edit this file. Restamp with `node factory/tools/renderBoard.mjs`.

## Now

CONTROL PLANE SEATED, Claude Code on the desktop, sitting 11; tenure inherited is D-60. F40 through F43 all landed. F44 and F45 ARE ISSUED as siblings at base ab398c3, proved disjoint, and both wait on the owner to launch. F44 takes S22: the census counts PROCESSES, six rows for two launches, which F40 did not fix because every one of those rows carries the harness token in program position; it folds a launch process tree into one seat and asserts both halves separately. F45 takes S23, which cost a paid session here: a correction envelope is written to main while a seat reads its envelope from its worktree, so the seat silently works to the superseded page. The seat path now refuses and writes nothing to stdout, so an empty stdin is a loud failure, while an unmakeable comparison is a NOTE and not a refusal, because a check that cannot pass is T09. S21 and S24 are the sparks still without a lane and are next. D-62 is the rule this sitting paid most for: a mutation per PROPERTY not per item, a universal claim proved at the member most likely to escape it, and a fixture proves a property only if it COULD fail. Reviews are separate processes (D-59); the Sonnet permission is D-61 and expires with this sitting. Dates come from date -u, now 2026-09-21. Ceilings do not move; compact instead. Otto and Virbos stay read-only.

Remote: VirBk/virbk. References: VirBk/Otto, VirBk/virbos.

## Gates

| ID | State | Title |
| --- | --- | --- |
| G0 | open | Synthetic data only |
| G1 | closed | Real product data in systems (never in git) |
| G2 | closed | Live alerts / client messages |
| G3 | closed | Downstream writes |
| G4 | closed | Production deploy |

No envelope opens a gate.

## Lanes

| ID | State | Hold | Title |
| --- | --- | --- | --- |
| F0 | landed | factory/, AGENTS.md | Stand up the factory kit |
| F1 | landed | none | Owner specs land |
| F2 | landed | AGENTS.md §1 and §5 | Name the product paragraph and never-move rules |
| F3 | landed | factory/tools/seat.mjs, docs/log/f3.md | First cheap writer envelope |
| F4 | landed | factory/lineage.json | Parent factory lineage loop |
| F5 | landed | factory/sessions.json | Session fuse protocol |
| F6 | landed | factory/project.json, factory/tools/hands.mjs | Hands split; PM-pickable topologies |
| F7 | landed | factory/CONSOLE.md | Owner console and token mix |
| F8 | landed | factory/drops/, factory/lineage.json | Alumni drops for Otto and Virbos |
| F9 | landed | factory/project.json, factory/runtimes.json | GitHub contribute options |
| F10 | landed | factory/project.json, factory/tools/hands.mjs | Cloud CP deploys the writer terminal |
| F11 | landed | factory/project.json, factory/runtimes.json | Prompt cache as a native prefix, orchestrated |
| F12 | landed | factory/project.json, factory/runtimes.json | Token-only writer, no Claude Code |
| F13 | landed | factory/project.json, factory/runtimes.json | Sidecar factory for an ongoing product |
| F14 | landed | factory/writer-paths.json, factory/tools/route.mjs | Owner-picked writer ladder and path router |
| F15 | dropped | factory/tools/seat.mjs | Map hosted writer model to a real DashScope id |
| F16 | landed | factory/tools/seat.mjs, docs/log/f16.md | Hosted recipes print the live DashScope model id |
| F17 | landed | factory/project.json, factory/tools/seat.mjs, docs/log/f17.md | DashScope DeepSeek Flash on the live seat |
| F18 | landed | factory/sparks.json, factory/tools/sitting.mjs, factory/traps.yaml, docs/log/f18.md | Close F15; absorb S01 as T49; drop S02 |
| F19 | landed | .github/workflows/landing.yml, factory/landing-checks.json | Public-repo landing Action |
| F20 | landed | .github/workflows/writer.yml, factory/tools/ghaWriter.mjs | GitHub Action writer runner |
| F21 | landed | .github/workflows/landing.yml, docs/log/f21.md | Shallow checkout for the landing gate |
| F22 | landed | factory/tools/kitCheck.mjs, factory/packages.json, factory/lineage.json, docs/log/f22.md | Kit holds its own shape |
| F23 | landed | factory/tools/landingGate.mjs, docs/log/f23.md | Landing gate runs where the control plane sits |
| F24 | landed | factory/tools/packet.mjs, AGENTS.md, factory/budgets.json, docs/log/f24.md | The packet: a reading path with a byte budget |
| F25 | landed | factory/tools/ledger.mjs, factory/tools/renderBoard.mjs, docs/log/f25.md | Board ledger is a window, not a novel |
| F26 | landed | factory/tools/alumni.mjs, factory/lineage.json, contracts/intake.v1.json, docs/log/f26.md | Alumni contribute by measurement |
| F27 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json, docs/log/f27.md | A return pastes the command |
| F28 | landed | factory/tools/ghaWriter.mjs, factory/tools/seat.mjs, docs/log/f28.md | The seat is launched with the packet |
| F29 | landed | factory/tools/seatReturn.mjs, factory/returns.json, docs/log/f29.md | Spend and cache hits survive as rows |
| F30 | landed | factory/tools/experiment.mjs, factory/experiments.json, factory/tools/landingGate.mjs, docs/log/f30.md | A rule can be suspended on purpose, with an expiry |
| F31 | landed | factory/tools/packet.mjs, docs/log/f31.md | The packet prefix is byte-stable, and a check proves it |
| F32 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json, docs/log/f32.md | The control plane reads the meter into the return |
| F33 | landed | factory/tools/packet.mjs, factory/tools/sizeBudget.mjs, factory/tools/spark.mjs, factory/landing-checks.json, docs/log/f33.md | Packet windowing and growth bands |
| F34 | landed | AGENTS.md, factory/OPERATING_MODEL.md | AGENTS carries the orchestration model and sheds the provisional group |
| F35 | landed | factory/tools/alumni.mjs, contracts/intake.v1.json | Alumni scan reads a child TRAPS.md |
| F36 | landed | factory/traps.yaml, factory/tools/packet.mjs, docs/log/f36.md | A trap names its object (scoping deferred, D-58) |
| F37 | landed | factory/tools/seat.mjs, factory/landing-checks.json, factory/traps.yaml | The seat launch is checked: disjoint holds, a process-table census, a cap-sized packet, a key from its store |
| F38 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json | A return's model is checked against the meter |
| F39 | landed | .github/workflows/writer.yml, factory/tools/ghaWriter.mjs, factory/tools/route.mjs, factory/traps.yaml | The cloud writer takes the router, and idles loudly instead of failing |
| F40 | landed | factory/tools/seat.mjs | The census names a running seat, not a mention of one (S17) |
| F41 | landed | factory/tools/kitCheck.mjs | A record cites only what resolves, and dates only what UTC has reached (S18) |
| F42 | landed | factory/tools/alumni.mjs | The TRAPS parser: last-wins proved, quoted scalars, an explicit refusal honoured (S19) |
| F43 | landed | factory/tools/seat.mjs | A print-mode recipe names its approval mode, or the seat writes nothing |
| F44 | issued | factory/tools/seat.mjs | The census counts seats, not the processes one launch spawns (S22) |
| F45 | issued | factory/tools/packet.mjs | A seat packet refuses a stale envelope the worktree still holds (S23) |

## Holds

- AGENTS.md / factory rules — control plane
- factory/board.json — control plane
- factory/lineage.json — control plane
- factory/project.json — control plane
- factory/envelopes/ — control plane
- contracts/seat-return.v1.json — control plane
- contracts/intake.v1.json — control plane
- contracts/project.v1.json — control plane
- factory/sessions.json — control plane
- factory/CONSOLE.md — control plane
- factory/CONTROL_PLANE.md — control plane
- factory/owner-ask.json — control plane
- factory/sparks.json — control plane
- factory/drops/ — control plane
- factory/writer-paths.json — control plane
- factory/tools/route.mjs — control plane
- factory/concerns.json — control plane
- factory/budgets.json — control plane
- factory/landing-checks.json — control plane

## Keystones

- Dual-runtime closed loop: spec → runtime-stamped envelope → cheap writer return → strong review → land → spend in the lane log
- Parent flywheel: copy → run → return intake → Grok absorbs under size budgets

## Shelf

- Kit overlay onto Otto or Virbos — trigger: Quiet week, no running seats, owner word. Pin a kit SHA. Never a GitHub fork.

## Ledger

Live window. Older rows are in `docs/ledger/`, which is history, not the reading path.

- 2026-09-20 F38 landed from writer/F38 at b0535a7, fast-forward, after one correction, and it is the last booked lane of this sitting. Recording a return now checks the model the launcher claimed against the id the harness meter recorded, through the router's canonical form rather than string equality, and refuses a disagreement, an absent meter, and a second recording of the same return. The correction did three things. It drove the CLI the control plane actually types, because record()'s only production caller was dead code to every fixture: bypassing that call left both self-test and check-contract green, which is D-59's shape for the fifth time and F39's form for the second. It pinned all eight required contract fields where two were pinned; every one of the eight now reds both commands when dropped, verified here field by field. And it deleted a citation to a git ref that never existed, replacing it with the function in route.mjs that carries the claim, which does exist. That invented citation is the finding worth carrying out of this sitting: the claim was true and derivable, so a reviewer reading for plausibility passes it, and neither T02 nor T56 covers evidence that was made up rather than mis-copied. S18.
- 2026-09-20 Control plane handed back at the owner's word after seven lanes. The tenure is D-60 and nothing of it is left in the session: what landed, what the reviews cost and bought, the four things this control plane got wrong, how a seat is actually launched on this machine, the Sonnet reviewer permission and that it expires with the sitting, and the state a successor inherits. T71's rule was rewritten because F35 made it false — a scanner that never opens a child's TRAPS file was the trap, and the scanner now opens it — and the row now names its check, which it never did. F40, F41 and F42 are booked for the three open sparks, mutually disjoint at the files so two may be siblings, with traps.yaml kept out of their holds on purpose so they do not contend and the control plane writes each trap row at landing. The one finding to read first is S18: a lane log cited a git ref that never existed as evidence for a claim that was independently true, which is the shape a reviewer reading for plausibility waves through, and neither T02 nor T56 covers evidence that was invented rather than mis-copied.
- 2026-09-20 F40 and F41 issued together at base a3514a9, the first siblings of sitting 11, proved disjoint by three commands rather than one: node factory/tools/seat.mjs disjoint compares two lanes and silently drops every argument past the second, so the single three-lane call the previous Now line rested on printed a verdict about one pair and exited 0. That is S20, and F40 takes it as its second item alongside S17. F41 takes S18. Both envelopes are the first written to D-61, which retires D-59's no-tier-change reading because its own falsifier fired twice, and puts in its place the narrower rule the five instances actually share: an acceptance item names its mutation AT THE PRODUCTION ENTRY POINT, the CLI block a person types, not a helper a fixture reaches. F41's ruling was cut to fit its hold before issue rather than after: a live scan of docs/log found five path citations that do not resolve, every one of them legitimate prose — a deleted envelope, a forward reference to factory/help.json, a child's path, a deliberate absent-file example, and one genuinely dead citation in f21.md — so the lane is dictated to seed an allowlist of exactly those five with a reason each and to STOP rather than edit a record outside its holds, and its date check is scoped to the at FIELD because board.json's ledger legitimately narrates the string 2026-09-21 inside the row recording the mis-dating. Two control-plane errors this issuance, both caught by the objects and both recorded because the next one should not repeat them. The seating commit 758ab12 was pushed with a gate that was red: the Now line it carried put the seat packet at 26.0 KB against a 26 KB budget, and the gate had been run on the commit BEFORE it rather than on an archive of it, which is the whole reason AGENTS section 3 says a fresh archive of the commit. Compacting the Now line fixed it, ceilings did not move (D-52, D-54, D-57). And the first issuance commit failed kitCheck because factory/packages.json still said queued while the board said issued, which is T63 catching two files holding the same state, in the control plane's own hand. Two findings that have no spark because the store is full at five open and refuses a sixth, which is the back-pressure working: first, the census counts PROCESSES, not seats — the two launches above produced six rows, a cli-entry wrapper plus --expose-gc children each, and printed 6 running writer seat(s) when the true number was two, which F40 does NOT fix because every one of those rows has the harness token in program position; second, S21, the gated seat-packet band measures the packet with no lane and so without the envelope, printing 99.7 per cent of 26 KB for a context that reaches the writer at 31.6 KB. Both seats launched on the native DeepSeek path, base https://api.deepseek.com/v1 and model deepseek-flash, the key read from the User store in the same act as the launch and verified at 200 against /models before a launch was spent (T73). This session's tool policy allowed both spawns, where D-60 records roughly one in four; it is a classifier, not a rule.
- 2026-09-20 Both sibling seats were launched and both returned nothing written, and the defect is the launch line rather than the seat. qwen-code's --approval-mode defaults to requiring approval for every file edit and shell command; in print mode there is no terminal to approve, so every write is refused silently. The recipe in factory/tools/seat.mjs has never carried the flag, which means no seat this factory has launched from that line could write unless something else supplied it, and the failure is invisible until a seat tries. Both seats diagnosed it themselves and refused to fake a return, the F41 seat writing that the lane has no return to read and should be re-seated rather than reviewed, which is the behaviour the factory wants from a cheap seat and is worth recording as such. Spend pasted from node factory/tools/seatReturn.mjs meter: F40 27 requests, 958342 in, 31963 out; F41 24 requests, 705121 in, 15894 out; deepseek-flash on both, the meter agreeing with the launcher's claim, which is F38's check doing its job on a lane that never landed. Relaunch with --approval-mode yolo was refused by this session's tool policy as Create Unsafe Agents, and so was the safer --approval-mode auto, so under T35 the line went to the owner and was not retried a third time. The read-only pass was not free of value: the F41 seat independently verified that all five allowlist paths the envelope seeds are genuinely absent and that no sixth citation reds on a hand pass, naming factory/help.example.json as the file that exists where factory/help.json does not, and it named the trap owed at landing.
- 2026-09-21 F41 landed from writer/F41 at 8ccc04d, fast-forward, APPROVED ON THE FIRST RETURN with no correction, which makes two such lanes in this factory and both were written to a dictating envelope that named its mutation. A record's citation is now checked rather than trusted: a backticked path a lane log cites resolves in the commit under test or sits in an allowlist of five with a one-line reason each, an allowlist entry that DOES resolve fails as a stale exemption, and a date field in decisions or the board ledger is never ahead of the UTC date read in the same act as the comparison, while prose is left unscanned so a record may narrate a date it does not claim. S18 absorbed as T76. Two things the control plane had to add to the lane's own evidence rather than take on trust. The date check has two halves and the lane proved one: UTC rolled to 2026-09-21 during the sitting, which is the very date the ledger row narrating the mis-dating mentions, so the tree's green did not prove that prose stays unscanned. Driven separately here and by the reviewer at 2099-01-01: the date in a ledger row's event stays green, the same date in that row's at field reds. That is S19's shape caught before it shipped rather than after. And the control plane's first census drive on F40 printed two seats where the envelope dictated three, one command short of a blocking finding, until the fixture itself turned out to be the defect: a shell heredoc collapsed the backslashes so the row under test was never the row the envelope named. A fixture is an object like any other and is verified at its bytes before its verdict is believed. The review, a separate Sonnet process per D-59 and not an in-session agent, reproduced every command in the lane log verbatim, drove all four envelope mutations plus the prose half and an allowlist reversal on real tracked files, and looked for a sixth instance of the D-59 shape without finding one. It reported three forms the dictated rule cannot see and they are written into T76 rather than left in a transcript: a path with a line suffix after the extension, an unbackticked path, and an extension outside the seven listed. Spend pasted from node factory/tools/seatReturn.mjs meter and recorded as row a89b727587deab5d: 45 requests, 3177103 in, 44461 out, 3095296 cached, deepseek-flash, the meter agreeing with the launcher's claim. The cache carried 97.4 per cent of the input, which is the byte-stable prefix paying for itself. The runtime recorded is desktop-qwen and NOT the pc-dashscope the envelope stamped: the stamp was the control plane's error, since this is a Claude desktop control plane running qwen-code on the native DeepSeek path, and recording the stamp would have put a false measurement in returns.json permanently.
- 2026-09-21 F40 returned at d493c05 and the review says CORRECTIONS NEEDED after approving both acceptance items at the CLI, which is the seventh lane in a row where an independent review found a blocking defect the control plane's own verification had missed. Finding one is a coverage hole rather than a wrong answer: the lane correctly deleted base main's row-equality assertion, because that one reached the helper instead of the CLI, and replaced it with checks on the seat COUNT, on a pid being present and on rows 9001 and 9002 being absent. Nothing asserts the VALUE of a row's harness field, so replacing harness with a hardcoded goose in the shipping census block leaves node factory/tools/seat.mjs --self-test printing ok at exit 0 while the live census renders pid 8324's qwen seat as goose. Driven by the control plane after the review named it. That is D-59's family arriving by a new route: not an assertion built from the producer, but an assertion that simply stops short of the property, left behind when a correct deletion was not matched by a correct replacement. Finding two is the invented citation again, in the same sitting that absorbed S18 as T76: docs/log/f40.md says all twenty-one steps were green including step 13, which the envelope expected red while the envelope is live, and the envelope says nothing about step 13, nothing about redness and nothing about liveness. The history behind the sentence is real and belongs to F39, which landed that fix before F40 was issued. The lane log narrated from the sitting's memory rather than from its own envelope. T76 does not catch it and the control plane states that rather than implying the new trap covers it: T76 checks that a cited PATH resolves and that a DATE FIELD is not ahead of UTC, and a claim attributed to a document that the document does not make is outside both. Correction 1 takes the harness label and the sentence, and is the only correction this sweep-tier seat gets.
- 2026-09-21 F40 landed from writer/F40 at 4ea6da9, fast-forward, after one correction, and it closes the sitting's two siblings. The census now counts a harness in program position, so a reviewer's own pip show aider-chat no longer reads as two running aider seats, and disjoint refuses a call naming more lanes than it compares instead of answering about the first pair and exiting 0. S17 absorbed as T77, S20 as T78. The correction is the finding worth carrying. The review approved both acceptance items at the CLI and then found a hole neither the envelope nor the control plane had named: the lane correctly deleted base main's row-equality assertion, because that one reached the helper rather than the command that ships, and replaced it with checks on the seat COUNT, on a pid being present and on two rows being absent. Nothing asserted the VALUE of a row's harness field, so hardcoding that label left node factory/tools/seat.mjs --self-test printing ok at exit 0 while the live census rendered pid 8324's qwen seat as goose. That is D-59's family by a new route, and it is the one to watch for next: not an assertion built from the producer, but an assertion left short of the property when a correct deletion was not matched by a correct replacement. The second finding was the invented citation again, in the same sitting that absorbed S18 as T76 and three commits after T76 landed: the lane log attributed to the envelope a prediction about gate step 13 that the envelope never made, narrating from the sitting's memory of F39's T09 fix rather than from its own envelope. The control plane states plainly that T76 does not catch it, because T76 checks that a cited PATH resolves and that a DATE FIELD is not ahead of UTC, and a claim attributed to a document that the document does not make is outside both; the lane log now says that in its own words. Verified at the objects before landing: git range-diff over the rebase shows both original commits replayed identically with only the two correction commits new, a blob sweep finds nothing outside the two holds, the harness-label mutation reds at the CLI block and the file restores to hash d9a1d0c, and the gate is 21 of 21 from a fresh archive of the lane commit. Spend recorded as row 40aa5b3d7c19a70b: the correction session alone is 23 requests, 1613003 in, 42043 out, 1540864 cached, deepseek-flash, the meter agreeing with the launcher's claim, and the cache carried 95.5 per cent of the input. The seat returned 24 tool calls against a cap of 20 and disclosed the overrun rather than hiding it, spending it on discovering that the control plane had rewritten its envelope under it; AGENTS says a cap a lane's holds cannot be satisfied within is a badly drawn cap, and this one was drawn without allowing for the re-read the rewrite forced. Writing the three trap rows this sitting owed put the seat packet at 100.5 per cent of its 26 KB cap, and D-58's deferral trigger, the packet at 90 per cent with no duplicate left to shed, was read before anything was shaved. It has not fired: there was a duplicate to shed. A trap whose rule names a check compresses in the digest to its FIRST SENTENCE, which is D-52's second remedy, so the fix was to lead each new rule with the rule itself and let the evidence follow the first period. traps.yaml grew from 20870 to 20903 bytes while the seat packet fell from 26.1 to 25.9, which is the mechanism paying out: detail written for a person reading the file costs a seat nothing. Per-lane scoping stays deferred and the trigger stands unfired, with the lane that eventually takes it still owing F31's cacheable-prefix assertion, now measured at 95.5 and 97.4 per cent hit rates on these two lanes.
- 2026-09-21 F42 and F43 issued together at base cafac16, the sitting's second pair of siblings, proved disjoint at the files before either envelope was written. F42 takes S19, the last open spark that had a lane, and it is a correction of a half-proved fix rather than new ground: F35 landed splitCheck as case-sensitive AND last-wins with a fixture for the first half only, so restoring first-match while keeping case-sensitivity leaves the self-test green. The envelope therefore dictates a mutation PER ITEM that reds for that item alone, and says so in those words, because a single mutation that reds two items leaves one untested. It also takes the two gaps F35 named and did not take: a quoted YAML scalar carrying its quote characters into the id, the kind and the title, which YAML permits a child to write anywhere, and a child's kind of reject being mapped to portable so an explicit do-not-absorb signal is inverted into a candidate intake. F43 exists because this sitting paid for it twice. Both seats were launched from the qwen-code recipe exactly as written, and both read their packet, analysed correctly and wrote nothing, because that harness defaults to requiring approval for every edit and shell command and a print-mode seat has no terminal to approve at. The recipe has never named a mode, so no seat launched from that line could ever write. F43 names the mode on the one harness whose help output the control plane actually ran, and DICTATES that the three uninstalled harnesses declare the gap instead of guessing, because a guessed flag fails silently in exactly the way the qwen line just did and T07 is the trap for a recipe remembered rather than read. Two things about the issuance itself belong in the record. The tool policy refused to write F43's envelope through a shell heredoc, reading the flag named in the prose as an attempt to create an unsafe agent; the envelope was written with a file tool instead and its text now describes the option rather than spelling it, which is a constraint on how this control plane can document a launch line, not a safety judgement about the lane. And the seat packet went over its 26 KB cap again on the Now line and was compacted three times to 1471 characters to fit, which is the fourth time this sitting that the board's own prose has been the binding constraint on a writer's context.
- 2026-09-21 F42 and F43 both returned CORRECTIONS NEEDED on the same shape, and the control plane's own envelopes are the cause, which is D-62. D-61 had already narrowed the rule to: an acceptance item names its mutation at the production entry point. Both envelopes obeyed it and both still let an untested property ship, because the rule counts mutations per ITEM and an item can carry several properties. F42's item 3 carried three: a reject row is held, the match is case-insensitive, and the refusal is read before the floors. One mutation was named, the first was proved, and the control plane drove the other two itself after the review named them: dropping the toLowerCase in portableOf, and swapping the refusal check to sit after the check floor, each leaves alumni --self-test printing ok at exit 0, because the only reject fixture is already lowercase and already clears every floor. That is precisely the defect F42 existed to correct in F35, reproduced one level up by the envelope that ordered the correction. F43's item 3 carried a scope claim, every recipe the list command prints, and the implementation read scope as every recipe rendering a stdin carrier; the envelope's own words, its carrier line, invited that. goose renders no carrier, a property that predates the lane, so goose is invisible to the check while being one of the three recipes item 2 names, and deleting goose's unverified note leaves seat --self-test green while the same deletion on aider or opencode correctly reds. Both seats named their own hole in the lane log in plain words rather than hiding it, and that is the behaviour to keep and to say so: a cheap seat that reports the gap it could not close is worth more than one that papers it. It is still not closing it, and either lane could have returned for a split instead of shipping around it. The general form, and the one to carry, is that a fixture proves a property only if the fixture COULD fail it: a lowercase-only fixture cannot prove a case-fold, a reject row clearing every floor cannot prove an ordering against the floors, and a scope sampled at a convenient member cannot prove a universal claim. Both corrections are issued; each adds fixtures that can fail and changes no shipping behaviour, except F43's rescoping and the false premise in its code comment that a recipe with no stdin carrier is not a print-mode launch, a sentence whose counterexample sat inside its own diff. F42 also carries a log correction under AGENTS section 4: a pasted 18388 bytes with no command behind it, where the full intakes output is 18434 and the trap rows alone are 17997, so the figure is either commanded or deleted; the substantive claim it decorated was independently reproduced and is true.
- 2026-09-21 F42 landed from writer/F42 at a0c9baa, fast-forward, after one correction, and S19 is absorbed as T79, which empties the last spark that had a lane. The TRAPS parser now proves last-wins with a mutation that reds for last-wins alone, strips one matching quote pair at the two ends only so an apostrophe inside a rule survives, and honours a child's kind of reject by holding the row and naming it rather than inverting the refusal into a candidate intake. The correction is the part worth reading. The lane had proved its headline property and left two others of the same item unproven, and the control plane's envelope was the cause: D-61 counts mutations per ITEM and that item carried three properties. Dropping the toLowerCase in portableOf, and reading the refusal after the check floor, each left the self-test green, because the only reject fixture was already lowercase and already cleared every floor. Both are now red against fixtures built to fail them, T12 for the fold and T13 for the ordering, each reding for its own property alone, all driven by the control plane at the objects and the file restored to hash 274b5f0. That is D-62, which this lane and F43 together paid for: a fixture proves a property only if the fixture COULD fail it. The lane also deleted an uncommanded 18388 bytes from its log under AGENTS section 4, keeping the claim it decorated, which was independently reproduced and is true. Spend recorded as row e8320818ecec1e7e: 46 requests, 3217606 in, 44392 out, 3116032 cached, deepseek-flash, meter agreeing with the launcher, 96.8 per cent of the input carried by cache. One operational cost is recorded as S23 rather than left in a transcript: the first launch of this correction ran against the SUPERSEDED envelope, because a correction envelope is written to main while a seat reads its envelope from its worktree, which still sat at the issuance commit; git fetch does not touch the working tree. It was caught by reading the envelope's first line after launch, the seat was stopped with the census back at zero, both worktrees were rebased, and each was confirmed to hold CORRECTION 1 before relaunching.
- 2026-09-21 F43 landed from writer/F43 at 75f5d1d, fast-forward, after one correction, and it closes every lane booked this sitting. A print-mode launch line now names the harness's approval mode, absorbed as T80, and the lane exists because this sitting paid for it twice: two seats were launched from a recipe that had never named one, and both read their packet, did correct work in their heads and wrote nothing, because the harness default requires approval for every edit and shell command and a print-mode seat has no terminal to give it. The correction is where the lane got better than its envelope. The control plane dictated only that scope stop being the stdin carrier, which had made goose invisible to the check while being one of the three recipes the envelope named. The seat delivered that and two things nobody asked for: it strips the unverified declaration out of the text before testing whether a recipe is in scope, so the declaration cannot pull its own recipe into or out of the rule, which is the circularity the rescoping invited; and it asserts the in-scope set is non-empty, so the whole rule cannot pass by covering nothing. Both are the D-62 question asked unprompted, and they are the first time this sitting a sweep seat has closed a hole the envelope did not name. Four mutations driven by the control plane at the objects, each naming its own recipe: goose, aider and opencode each red on their note being stripped where goose was green before the correction, and the qwen carrier reds naming both qwen-code and its desktop-qwen alias. git range-diff proves all three commits replayed identically across the rebase main's own movement forced. Spend recorded as row 3662c93dcb6ac0de: 46 requests, 2885716 in, 77885 out, 2793472 cached, deepseek-flash, meter agreeing with the launcher, 96.8 per cent of input carried by cache. The lane's own log carries a finding the control plane is keeping rather than waving through: the gate reported all 21 steps red inside the seat, the seat ran a control at live main's tip, got the same 21 reds, concluded the red was not its own and handed classification up rather than guessing. That is the behaviour to keep. The cause is the seat's bash resolving to the WSL launcher, the same gate passing 21 of 21 from git-bash at the same commit, and it makes a sentence in T70 false. S24.
- 2026-09-21 F44 and F45 issued together at base ab398c3, proved disjoint, and they take two of the four sparks that ended this sitting's booked work with no lane. F44 takes S22: the census counts processes rather than seats, six rows for two launches, and F40 landing did NOT fix it because every one of those rows carries the harness token in program position, so T77 names it as a limit and this lane closes it by folding a launch's process tree through the parent pid the lister already can report. F45 takes S23, the one that cost a paid session here: a correction envelope is written to main while a seat reads its envelope from its own worktree, which still sits at the issuance commit holding the superseded page, and git fetch does not touch the working tree. The seat path will refuse and write NOTHING to stdout, because the launch line is a pipe into the harness and an empty stdin is a loud failure, while a comparison that cannot be made is a NOTE and not a refusal, since a check that cannot pass is T09 and this factory has shipped one before. F45 is the delicate one and the envelope says so: the gate runs packet.mjs from an unpacked archive with no git, CI checks out shallow, and F31's byte-stable prefix is a 95 to 97 per cent cache hit this sitting, so nothing the lane adds may reach stdout in any path. Issuing the pair put the seat packet over its 26 KB cap again and the shed was found where D-58 says to look before firing its trigger. The traps digest had grown from 11.4 KB to 12.3 as this sitting wrote five trap rows, and a rule that names a check compresses to its FIRST SENTENCE, so four rules from the previous sitting, T72 T73 T74 and T75, were carrying 200 to 256 characters of evidence into every seat's digest because each was written as one sentence with no internal period. Splitting the rule from its evidence in each took the packet from 100.1 per cent to 97.8 while traps.yaml GREW to 22925 bytes. That is the second time this sitting the same mechanism has paid: detail written for a person reading the file costs a seat nothing, and per-lane scoping stays deferred with its trigger unfired.
