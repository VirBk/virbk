# BOARD

Generated from `factory/board.json`. Do not hand-edit this file. Restamp with `node factory/tools/renderBoard.mjs`.

## Now

CONTROL PLANE HELD by a Claude Code desktop session; sitting 12 open. Tenure inherited: D-63. This sitting's rulings: D-64 (Sonnet reviews, deepseek-flash writes, both expiring with it) and D-65 (the control plane does not stop for a click; AGENTS section 5 and the owner-ask list still bind). Read D-62 before issuing any code lane: a mutation per PROPERTY not per item, a universal claim proved at the member most likely to ESCAPE it, and a fixture proves a property only if it COULD fail. F47 LANDED after one correction and S24 is absorbed by REWRITING T70, which had said gate steps were unaffected by shell dialect. F48 LANDED after one correction and S25 is absorbed by rewriting T76, whose first sentence is unchanged so the cached prefix holds. The hold on F46 and F49 is RELEASED. F49 will take the gate from 21 steps to 22, so no lane may be in flight whose envelope pins 21 when it lands. F46 and F50 are ISSUED as siblings, proved disjoint. F46 takes S21. F50 takes the gap F47's own correction could not close: its entry-point fixture needs a repository, so it is skipped where the gate runs the self-test from an unpacked archive, and the wiring can still be removed with every automated check green. F49 stays queued and takes the gate from 21 steps to 22, so nothing may be in flight whose envelope pins 21 when it lands. Open sparks: S21, S25, S26, S27 the packet shed D-58 wants BEFORE its trigger fires, S28 the qwen seat's memory store outside git. A correction launch rebases the worktree first (T81), and the correction envelope authorises force-with-lease on its own lane branch only, never main. The tool policy refuses a launch unpredictably; hand the owner the line (T35). Dates from date -u. Ceilings do not move. Sitting 11's six worktrees still stand; removing them is an owner ask (D-65).

Remote: VirBk/virbk. References: VirBk/Otto, VirBk/virbos.

## Gates

| ID | State | Title |
| --- | --- | --- |
| G0 | open | Synthetic data only |
| G1 | closed | Real product data in systems (never in git) |
| G2 | closed | Live alerts / client messages |
| G3 | closed | Downstream writes |
| G4 | closed | Production deploy |

No envelope opens a gate.

## Lanes

| ID | State | Hold | Title |
| --- | --- | --- | --- |
| F0 | landed | factory/, AGENTS.md | Stand up the factory kit |
| F1 | landed | none | Owner specs land |
| F2 | landed | AGENTS.md §1 and §5 | Name the product paragraph and never-move rules |
| F3 | landed | factory/tools/seat.mjs, docs/log/f3.md | First cheap writer envelope |
| F4 | landed | factory/lineage.json | Parent factory lineage loop |
| F5 | landed | factory/sessions.json | Session fuse protocol |
| F6 | landed | factory/project.json, factory/tools/hands.mjs | Hands split; PM-pickable topologies |
| F7 | landed | factory/CONSOLE.md | Owner console and token mix |
| F8 | landed | factory/drops/, factory/lineage.json | Alumni drops for Otto and Virbos |
| F9 | landed | factory/project.json, factory/runtimes.json | GitHub contribute options |
| F10 | landed | factory/project.json, factory/tools/hands.mjs | Cloud CP deploys the writer terminal |
| F11 | landed | factory/project.json, factory/runtimes.json | Prompt cache as a native prefix, orchestrated |
| F12 | landed | factory/project.json, factory/runtimes.json | Token-only writer, no Claude Code |
| F13 | landed | factory/project.json, factory/runtimes.json | Sidecar factory for an ongoing product |
| F14 | landed | factory/writer-paths.json, factory/tools/route.mjs | Owner-picked writer ladder and path router |
| F15 | dropped | factory/tools/seat.mjs | Map hosted writer model to a real DashScope id |
| F16 | landed | factory/tools/seat.mjs, docs/log/f16.md | Hosted recipes print the live DashScope model id |
| F17 | landed | factory/project.json, factory/tools/seat.mjs, docs/log/f17.md | DashScope DeepSeek Flash on the live seat |
| F18 | landed | factory/sparks.json, factory/tools/sitting.mjs, factory/traps.yaml, docs/log/f18.md | Close F15; absorb S01 as T49; drop S02 |
| F19 | landed | .github/workflows/landing.yml, factory/landing-checks.json | Public-repo landing Action |
| F20 | landed | .github/workflows/writer.yml, factory/tools/ghaWriter.mjs | GitHub Action writer runner |
| F21 | landed | .github/workflows/landing.yml, docs/log/f21.md | Shallow checkout for the landing gate |
| F22 | landed | factory/tools/kitCheck.mjs, factory/packages.json, factory/lineage.json, docs/log/f22.md | Kit holds its own shape |
| F23 | landed | factory/tools/landingGate.mjs, docs/log/f23.md | Landing gate runs where the control plane sits |
| F24 | landed | factory/tools/packet.mjs, AGENTS.md, factory/budgets.json, docs/log/f24.md | The packet: a reading path with a byte budget |
| F25 | landed | factory/tools/ledger.mjs, factory/tools/renderBoard.mjs, docs/log/f25.md | Board ledger is a window, not a novel |
| F26 | landed | factory/tools/alumni.mjs, factory/lineage.json, contracts/intake.v1.json, docs/log/f26.md | Alumni contribute by measurement |
| F27 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json, docs/log/f27.md | A return pastes the command |
| F28 | landed | factory/tools/ghaWriter.mjs, factory/tools/seat.mjs, docs/log/f28.md | The seat is launched with the packet |
| F29 | landed | factory/tools/seatReturn.mjs, factory/returns.json, docs/log/f29.md | Spend and cache hits survive as rows |
| F30 | landed | factory/tools/experiment.mjs, factory/experiments.json, factory/tools/landingGate.mjs, docs/log/f30.md | A rule can be suspended on purpose, with an expiry |
| F31 | landed | factory/tools/packet.mjs, docs/log/f31.md | The packet prefix is byte-stable, and a check proves it |
| F32 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json, docs/log/f32.md | The control plane reads the meter into the return |
| F33 | landed | factory/tools/packet.mjs, factory/tools/sizeBudget.mjs, factory/tools/spark.mjs, factory/landing-checks.json, docs/log/f33.md | Packet windowing and growth bands |
| F34 | landed | AGENTS.md, factory/OPERATING_MODEL.md | AGENTS carries the orchestration model and sheds the provisional group |
| F35 | landed | factory/tools/alumni.mjs, contracts/intake.v1.json | Alumni scan reads a child TRAPS.md |
| F36 | landed | factory/traps.yaml, factory/tools/packet.mjs, docs/log/f36.md | A trap names its object (scoping deferred, D-58) |
| F37 | landed | factory/tools/seat.mjs, factory/landing-checks.json, factory/traps.yaml | The seat launch is checked: disjoint holds, a process-table census, a cap-sized packet, a key from its store |
| F38 | landed | factory/tools/seatReturn.mjs, contracts/seat-return.v1.json | A return's model is checked against the meter |
| F39 | landed | .github/workflows/writer.yml, factory/tools/ghaWriter.mjs, factory/tools/route.mjs, factory/traps.yaml | The cloud writer takes the router, and idles loudly instead of failing |
| F40 | landed | factory/tools/seat.mjs | The census names a running seat, not a mention of one (S17) |
| F41 | landed | factory/tools/kitCheck.mjs | A record cites only what resolves, and dates only what UTC has reached (S18) |
| F42 | landed | factory/tools/alumni.mjs | The TRAPS parser: last-wins proved, quoted scalars, an explicit refusal honoured (S19) |
| F43 | landed | factory/tools/seat.mjs | A print-mode recipe names its approval mode, or the seat writes nothing |
| F44 | landed | factory/tools/seat.mjs | The census counts seats, not the processes one launch spawns (S22) |
| F45 | landed | factory/tools/packet.mjs | A seat packet refuses a stale envelope the worktree still holds (S23) |
| F46 | issued | factory/tools/sizeBudget.mjs | The packet band names the context that reaches a writer (S21) |
| F47 | landed | factory/tools/landingGate.mjs | The gate names a shell that cannot run it, instead of 21 false reds (S24) |
| F48 | landed | factory/tools/kitCheck.mjs | A citation check tells a rev spec, a URL and a tree path apart (S25) |
| F49 | queued | factory/templates/ENVELOPE.md, factory/tools/envelopeCheck.mjs, factory/landing-checks.json | A correction envelope authorises the force its own rebase forces (S26) |
| F50 | issued | factory/tools/landingGate.mjs | The entry-point fixture runs where the gate runs, not only from a checkout |

## Holds

- AGENTS.md / factory rules — control plane
- factory/board.json — control plane
- factory/lineage.json — control plane
- factory/project.json — control plane
- factory/envelopes/ — control plane
- contracts/seat-return.v1.json — control plane
- contracts/intake.v1.json — control plane
- contracts/project.v1.json — control plane
- factory/sessions.json — control plane
- factory/CONSOLE.md — control plane
- factory/CONTROL_PLANE.md — control plane
- factory/owner-ask.json — control plane
- factory/sparks.json — control plane
- factory/drops/ — control plane
- factory/writer-paths.json — control plane
- factory/tools/route.mjs — control plane
- factory/concerns.json — control plane
- factory/budgets.json — control plane
- factory/landing-checks.json — control plane

## Keystones

- Dual-runtime closed loop: spec → runtime-stamped envelope → cheap writer return → strong review → land → spend in the lane log
- Parent flywheel: copy → run → return intake → Grok absorbs under size budgets

## Shelf

- Kit overlay onto Otto or Virbos — trigger: Quiet week, no running seats, owner word. Pin a kit SHA. Never a GitHub fork.

## Ledger

Live window. Older rows are in `docs/ledger/`, which is history, not the reading path.

- 2026-09-21 F45 returned at 7a828e5 and the review says CORRECTIONS NEEDED on a defect worse than the one the lane closes. The two new fixtures cover no repository at all, and a repository whose copies DIFFER. Neither covers the case every ordinary launch takes, where the comparison succeeds and the copies are IDENTICAL, so that branch has no fixture that could fail it. Driven by the control plane at the objects: replacing if (same) return null with if (false) return null leaves packet --self-test printing ok at exit 0, while packet seat F45 on a clean byte-identical worktree exits 3 with ZERO bytes on stdout and refuses. Every lane, every worktree, with no correction pending, would refuse and no seat could launch, and the self-test, the check and the landing gate would all stay green. That is exactly what T09 and the envelope's own item 2 forbid, and the envelope is the cause: it named three mutations, drop the refusal, make the unresolvable case refuse, and leak the note to stdout, and never named the equality comparison itself as a property. D-62 for the third and fourth time in one sitting, now in both siblings at once. The review also found the log claiming sizeBudget printed 70 budgets where it prints 71, every KB and percent figure beside it being correct, which is the fourth uncommanded count this sitting. What the review cleared, having driven it: the refusal writes nothing to stdout and exits 3 naming the difference and the remedy, an unresolvable comparison notes and emits proved from an unpacked archive with no git, stdout is byte-identical between the two paths at 32226 bytes, and the gate is 21 of 21.
- 2026-09-21 F45 landed from writer/F45 at 8d14688, fast-forward, after one correction plus a control-plane documentation fix, and S23 is absorbed as T81. A seat now compares the envelope in its worktree with the copy on live main and refuses when they differ, writing NOTHING to stdout so the harness gets an empty stdin and dies loudly, while a comparison that cannot be made is a note that still emits, proved from an unpacked archive with no git. The correction is the one to read. The lane's fixtures covered no repository at all and a repository whose copies differ, and never the case every ordinary launch takes, where the copies are IDENTICAL. Forcing the comparison to report a difference left --self-test, the check and the landing gate ALL green while every lane and every worktree would refuse with zero bytes on stdout and no seat in the factory could launch. Driven by the control plane at the objects before the correction and after: it now names that an identical envelope was refused and a working launch killed, and both directions of the comparison have their own red. The envelope was the cause for the fourth time this sitting, having named three mutations and never the equality itself. Landing also cost a control-plane documentation fix rather than a second correction, and the reason is worth the room: the gate went red on T76, the trap F41 landed this same sitting to catch invented citations, because the log cited the git revision spec HEAD colon the packet path, which RESOLVES - git rev-parse prints the blob and it matches the hash the log pins - but T76 reads any backticked token with a slash and a source extension as a tree path. A true citation in a form this factory uses often was rejected by the trap written to protect citations. The seat's one correction was spent, so the control plane rewrote the line to cite the command instead, which AGENTS section 4 prefers anyway, and recorded that it did so. That false-positive class is S25. Spend recorded as row a387304fc267d8be: 33 requests, 2385838 in, 98563 out, 2220544 cached, deepseek-flash, meter agreeing with the launcher.
- 2026-09-21 F44 landed from writer/F44 at 9d7d83d, fast-forward, after one correction plus a control-plane documentation fix, and S22 is absorbed by REWRITING T77 rather than adding a row, because F44 closed the limit T77 itself named - the same move T71 got when F35 made it false. The census now counts one seat per launch however many processes it spawns: the cycle fixture, two counted rows each the other's parent, prints 1 seat folding 1 where before the correction it printed 0 while two live harness processes existed, and a row whose parent is in no row is counted AND named with the pid it could not reach, the summary saying how many chains are broken and that the count may over-report. The correction's own root cause is the one to carry: every fixture in that file supplied a fully connected acyclic chain, kinder than the machine it models, which is how both defects passed a self-test and a gate. Two operational findings came out of landing it. The seat finished the correction, was rejected non-fast-forward on the push, correctly refused to rewrite a shared ref on its own authority and handed the call up; it was right to, and the divergence was the control plane's own rebase, which T81's remedy forces. That is S26: a correction envelope should authorise force-with-lease on its OWN lane branch and say main is never forced. And the lane log arrived at 10.2 KB against an 8 KB budget, failing gate step 01 with the seat's correction cap spent, so the control plane cut 21 identical WSL-red gate lines to one, elided repeated conpty paths and compressed explanation the trap row already carries, keeping every command, every mutation output and the seat's own disclosure that its first assertion could not fail. That disclosure is worth the room: the seat asserted the torn-chain report with a pattern the seat row itself satisfied, found it could not fail, and re-anchored it on the report's own words. A sweep seat applying D-62 to its own work, unprompted, is the best thing this sitting produced. Spend recorded as row 7ae5c7a505ad6bf4: 48 requests, 3024209 in, 149075 out, 2850560 cached, deepseek-flash, meter agreeing with the launcher.
- 2026-09-21 Sitting 11 closed at the owner's word after six lanes landed: F40, F41, F42, F43, F44 and F45. The tenure is D-63 and nothing of it is left in the session - what landed, what the reviews cost and bought, the four things this control plane got wrong, how a seat is actually launched on this machine, and the state a successor inherits. The finding of the sitting is D-62, and it is sitting 10's finding one level deeper. D-61 had narrowed the rule to: an acceptance item names its mutation at the production entry point. Every envelope this sitting obeyed that and four still shipped an untested property, because the rule counts mutations per ITEM while an item carries several properties. Six independent reviews found six blocking defects the control plane's own verification had missed, six for six, and every one traced back to an envelope that named the mutations it could imagine and left the ordinary case unnamed. The worst was F45, where the fixtures covered no-repository and copies-differ but never copies-identical, so forcing the comparison to report a difference left the self-test, the check AND the landing gate green while every launch in the factory would have refused with empty stdout. What replaces the old reading: a mutation per PROPERTY, a universal claim proved at the member most likely to ESCAPE it, and a fixture proves a property only if the fixture COULD FAIL IT. The tier did not change and the evidence now supports that: deepseek-flash produced six landed lanes, took correction accurately every time it was told exactly what to do, twice closed holes the envelope had not named, and once caught its own assertion failing to be falsifiable and re-anchored it unprompted. F46 to F49 are booked for the four open sparks, proved disjoint by six pairwise commands rather than one call that would have answered about a single pair. Three of those four are a rule that is now wrong or incomplete - T70, T76 and T81's own remedy - which is what a factory looks like when its checks are new enough to still be learning their own boundaries. Nothing is running, no envelope is issued, no experiment is open, the gate is 21 of 21, and the Sonnet reviewer permission expired with the sitting.
- 2026-09-21 F47 and F48 issued together at base d4427d7, sitting 12's first siblings, proved disjoint by six PAIRWISE runs of seat.mjs disjoint over the whole queue rather than one call that answers about a single pair. F47 takes S24: gate steps spawn bash -e with a Windows script path and cwd, so under the WSL launcher every one of the 21 reds at any commit, with stderr going to a log file so a print-mode seat sees only step names. The envelope's load-bearing instruction is the probe's FORM - a file in logDir run as bash -e with cwd workDir - because a probe shaped bash -c true succeeds under that launcher and would leave every red in place, which is T09 shipped as a fix. F48 takes S25 and one class more: the rev spec F45's log cited and rule H rejected although git rev-parse prints its blob, plus a backticked https URL ending .md which, measured at issue with the shipping regex, comes back as a citation while CONTROL_PLANE.md section 2 tells the control plane to record exactly such a URL. Same defect, same function, so the board title was widened rather than a second lane booked. Both envelopes carry SIX acceptance properties with one mutation each that must red for that property ALONE - D-62 applied at issue for the first time - and in both the ordinary case is named as its own property with a fixture that could fail it, which is what F45's fixtures lacked. Both came in over the 6 KB one-page cap at 7289 and 7005 bytes and were cut to 6088 and 6115 by deleting narrative, never properties.
- 2026-09-21 F47 landed from writer/F47 at de42916, fast-forward, after one correction, and S24 is absorbed by REWRITING T70 rather than adding a row - the move F44 got when it closed the limit T77 named. T70 had said gate steps are unaffected by shell dialect; two seats last sitting spent calls proving a 21-step red was not their own. The gate now probes its shell in a step's OWN form before the first step and exits 2 with one diagnostic naming the shell, the probe, the cwd and the shell's own stderr. The rewritten rule carries a Check is sentence, so the digest compresses it to its first sentence and the seat packet fell from 99.8 to 99.6 per cent of cap while traps.yaml GREW - the third time that mechanism has paid. The correction: the review and the control plane drove the same mutation separately and found every fixture exercising the library functions while NOTHING exercised the lines wiring them into the command a person types. Replacing the CLI's shellStop call with a null left --self-test ok at exit 0, kitCheck ok and GATE PASSED while the lane did nothing - D-61 in its own words, and F45's shape reproduced by the very envelope that named F45 as the thing to look hardest at. The seat went past the correction: GATE_SHELL makes the entry point drivable, a fixture spawns the SHIPPED FILE as a process and asserts the probe ran before the archive was unpacked, and it added a NINTH property nobody dictated - with GATE_SHELL unset the gate still spawns bash. Second sitting running in which a sweep seat closed a hole the envelope did not name. It disclosed rather than hid that that fixture needs a repository and is SKIPPED where the gate runs the self-test from an unpacked archive; verified at the objects, with no .git the wiring can be removed and the self-test still prints ok at exit 0. So the property is proved by a command a person runs from a checkout and NOT by the gate that protects main. The correction cap was spent, section 3 says split, and the remainder is F50 with the seat's own remedy. Spend row be6db4db03bb9df6, two sessions read from the meter: 44 requests 5881141 in 107417 out 5687936 cached, then 21 requests 1835850 in 75158 out 1644032 cached; cache carried 95.0 per cent.
- 2026-09-21 F48 landed from writer/F48 at 211469b, fast-forward, after one correction, and S25 is absorbed by rewriting T76 - the trap written last sitting to catch invented citations, which was itself rejecting real ones. Rule H now holds a citation with its KIND: no colon is a tree path, a colon splits at the FIRST colon, a remainder opening with two slashes is a URL it does not check, otherwise the left side is a revision spec whose RIGHT side is resolved against the tree. No git call was added and the envelope said why at the object: rule H runs in the gate from an unpacked archive that is not a repository. T76's FIRST sentence was left untouched on purpose, because the digest compresses a rule naming a check to that sentence and the packet prefix is byte-stable. The correction is the one to read, and the review and the control plane drove the same mutation separately. H3's fixture expected the EMPTY list, and a fixture that expects nothing cannot tell a token correctly classified from a token never recognised at all: inserting one line that discards a token whose slash precedes its colon - the natural bug of assuming a rev name holds no slash - made writer/F45:docs/log/f45.md vanish from the extractor while kitCheck --self-test still printed ok at exit 0. That was property 3, the exact member the envelope NAMED as most likely to escape and still did not prove. The remedy was already in the lane's own file: the held contract pins kind, token and path for a plain rev, a plain path and a URL, which is why those three survived a discard, and it carried no slash-bearing rev. It does now, and the discard reds it. The review found a second defect the control plane missed, the fifth uncommanded count in two sittings: the log said 19 in the size band where the command prints 21. Spend row c81530d8bf574984, two sessions read from the meter: 38 requests 4150238 in 201037 out 3914240 cached, then 20 requests 1678295 in 71767 out 1541376 cached; cache carried 93.6 per cent. One cost carried rather than left in a session: docs/log/f48.md finished at 8051 bytes against an 8192 cap because the correction asked the seat both to SAY which fixtures it had audited and to stop adding prose. Those two instructions fight; the next such envelope says which wins.
- 2026-09-21 F46 and F50 issued together at base 0d86cf8, proved disjoint over the whole remaining queue. F46 takes S21: the band printed as seat packet measures the packet with NO lane and so without the envelope, which is the byte-stable cacheable prefix budgets.json budgets separately at 26 and 6 KB, while packet.mjs seat F47 printed 31.9. One label, two numbers 5.7 KB apart, and a control plane reading the green one can hand a seat 22 per cent more context than it just checked. The envelope forbids moving either cap - that is a decision row, not a lane - and asks instead that the prefix be labelled as the prefix and that the total a seat actually receives be reported and AGREE with what packet.mjs prints, the agreement being the property rather than the formatting. It also names the empty case as its own property, because a landing deletes the envelope so the path with nothing to measure is the one that runs most often, and a silent pass there is indistinguishable from a check that never ran. F50 takes what F47's correction could not: its fixture 7 spawns the shipped file and proves the probe is wired into the CLI, but it needs a repository to resolve a sha and SKIPS without one, which is exactly how gate step 15 runs it. Verified at the objects before the envelope was written: in a directory with no .git the wiring can be removed and the self-test still prints ok at exit 0. The seat disclosed that itself and named this remedy. Both envelopes again carry six properties with one isolating mutation each, and both carry F48's lesson in their own words - a fixture that expects no error must still be able to fail.
